# Coherence: streaming-provider-dispatches-record-no-token-usag (#1857)

Tier L, technical track — the `fr` row class is omitted (no PRD). Outcomes are the four staged
Desired-outcome bullets from jstoup111/ai-conductor#1857. The ADR layer engages on six non-deleted
`.docs/decisions/adr-*` files in the change set: two authored here and four additively amended.
Every counterpart id was confirmed against the real stories and plan files, and every criterion
quote was verified as an exact substring of its cited task body.

Two rows carry a negative verdict, both deliberate and both waived in
`.docs/coherence-waivers/streaming-provider-dispatches-record-no-token-usag.md`:

- `outcome-3` — the totals-line denominator rework is out of scope by operator direction and is
  filed as jstoup111/ai-conductor#1863. Story 8 makes tokens and cost derive from the same
  three-valued classification of the same dispatches, which delivers the bullet's first branch in
  the ordinary case; the residual — a line that still reads as a total when some dispatch genuinely
  could not be measured — is not delivered here, so the row is `gap` rather than credited.
- `criterion:disposition-negative:30` — the codex envelope re-probe (Task 9.1) is honestly
  `outside-diff`: its truth depends on the installed codex CLI version, which no commit in this
  diff controls. The row's verdict is `covered`, because coverage genuinely is satisfied — Task 9.1
  owns the criterion and the row's quote is grounded in that task's body. Only the diff-locality
  disposition is negative. That is the assumption `adr-2026-08-24-streaming-dispatch-requests-the-machine-envelope`
  records, and Task 9.1 exists to observe it rather than to assume it.

The §4d cross-layer consistency pass found no contradiction and no oscillation. The two pairs that
warranted grounding are recorded in the Notes for `outcome-2` and `adr-2026-08-19-live-provider-stream-observation`.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-2, story-4, story-5 | covered | Reported totals account for every dispatch regardless of path: Story 1 records usage on the streaming path (tasks 22-23), Story 2 makes one dispatch member serve every path (tasks 4, 10, 15-19), Story 4 wires the observation seam (tasks 1, 7, 11), Story 5 requests the envelope on every non-REPL dispatch (tasks 3, 5, 9, 10). |
| outcome | outcome-2 | story-8 | covered | An unmeasurable dispatch is visibly unmeasured, never folded into a total: Story 8's three-valued classification, delivered by task 23.1, which also asserts a usage-absent dispatch is still counted as unmetered. Cross-layer check — the bullet says "never silently folded into a figure presented as a total" while this feature changes no formatter; it holds because `formatFeatureUsageTotal` already emits the unmetered and cost-unmetered counts, so preserving that behavior satisfies the bullet without a reporting change. The unchanged half is asserted by task 23.1's rollup case. |
| outcome | outcome-3 | story-8 | gap | outcome-3 — the reporting rework that would make the money figure state its denominator is out of scope by operator direction (2026-08-24) and filed as jstoup111/ai-conductor#1863. Story 8 delivers the shared-classification half; the line still presents a subset figure as a total when measurement genuinely fails. Waived. |
| outcome | outcome-4 | story-6, story-7, story-8 | covered | Steps that already report usage keep reporting it, and nothing acquires a fabricated cost: Story 7's enforcement guards (tasks 12-14.1, 15.1) including the autonomous-unchanged regression (14.1), Story 6's REPL preservation (tasks 3, 6, 9, 16), Story 8's no-invented-cost rule (task 23.1). |
| story | story-1 | task-22, task-23, task-23.1, task-24 | covered | All seven criteria mapped: usage recorded on both providers (22), failure and unparseable paths (23), signal-kill (24), metering classification (23.1). |
| story | story-2 | task-4, task-10, task-15, task-16, task-17, task-18, task-19 | covered | All six criteria mapped: one adapter dispatch body (4, 10), call sites repointed (15, 16), wrapper substitution retired (17), delegator (18), and the source-level absence check (19). |
| story | story-3 | task-19, task-20, task-21 | covered | All seven criteria mapped: member removed from the contract (19), the five loader cases (20), reference plugin (21). |
| story | story-4 | task-1, task-2, task-7, task-8, task-11 | covered | All six criteria mapped: the consumer field and its no-authority doc (1), the REPL constraint (2), delivery and buffered-absence (7), throwing callback (8), codex parity (11). |
| story | story-5 | task-3, task-5, task-6, task-9, task-9.1, task-10, task-23, task-23.1 | covered | All seven criteria mapped: claude and codex argument lists (3, 9), classifier input (5, 10), installed-CLI re-probe (9.1), missing terminal record (23), no-usage-fields (23.1), oversized prompt (6). |
| story | story-6 | task-3, task-6, task-9, task-16, task-23.1 | covered | All five criteria mapped: no envelope in REPL args (3, 9, 16), positional prompt and attached stdin (6), unmetered REPL contributing no cost (23.1). |
| story | story-7 | task-12, task-13, task-14, task-14.1, task-15, task-15.1 | covered | All nine criteria mapped: codex sandbox configuration (12), fresh sessions (13), self-host resolution (14), autonomous-unchanged regression (14.1), branch-session entry (15), availability consult and exhausted ladder (15.1). |
| story | story-8 | task-23.1, task-24 | covered | All seven criteria mapped: the four classification cases (23.1) and the partial-observation rule (24). |
| story | story-9 | task-25, task-25.1, task-26 | covered | All six criteria mapped: live burn and child observability (25), observation silence is not a stall (25.1), the before/after visibility comparison (26). |
| task | task-1 | story-4 | covered | Infrastructure task; supplies the stream-consumer seam every Story 4 check consumes. |
| task | task-2 | story-4 | covered | Constrains the REPL against a live consumer so the pair cannot express a meaningless dispatch. |
| task | task-3 | story-5 | covered | Selects the claude envelope from one condition rather than per call site. |
| task | task-4 | story-2 | covered | Collapses the claude dispatch body so one implementation serves every mode. |
| task | task-5 | story-5 | covered | Derives the classifier's envelope argument from the same condition, closing the usage-discarding branch off the REPL. |
| task | task-6 | story-5 | covered | Applies stdin prompt delivery to every non-REPL dispatch, preserving the REPL's positional form. |
| task | task-7 | story-4 | covered | Delivers stream observations on every claude dispatch that supplies a consumer. |
| task | task-8 | story-4 | covered | Contains a throwing consumer callback so observation cannot fail its dispatch. |
| task | task-9 | story-5 | covered | Selects the codex envelope from one condition in the single argument path. |
| task | task-9.1 | story-5 | covered | Verify-only; observes the installed codex CLI envelope rather than assuming it parses. |
| task | task-10 | story-2 | covered | Collapses the codex dispatch body and its classifier input. |
| task | task-11 | story-4 | covered | Delivers stream observations on every codex dispatch that supplies a consumer. |
| task | task-12 | story-7 | covered | Pins every unattended codex sandbox and approval entry across the merge of the two argument bodies. |
| task | task-13 | story-7 | covered | Applies fresh-session enforcement once, at the single dispatch entry. |
| task | task-14 | story-7 | covered | Keeps self-host executable resolution and auth handoff reachable from the unified path. |
| task | task-14.1 | story-7 | covered | Regression guard; an autonomous step's output and usage are unchanged by unification. |
| task | task-15 | story-2 | covered | Repoints the collaborative and branch-session call site onto `invoke`. |
| task | task-15.1 | story-7 | covered | Proves the pre-dispatch availability consult survives, including the exhausted-ladder case. |
| task | task-16 | story-6 | covered | Repoints the free-form REPL call site, preserving plain text and its terminal. |
| task | task-17 | story-2 | covered | Refactor; retires the wrapper's dispatch substitution, which is the routing that produced the defect. |
| task | task-18 | story-2 | covered | Refactor; points the attribution-lane delegator at the one dispatch member. |
| task | task-19 | story-3 | covered | Refactor; removes `invokeInteractive` from the contract once nothing calls it. |
| task | task-20 | story-3 | covered | Loosens plugin validation to `invoke` alone and pins all five load cases. |
| task | task-21 | story-3 | covered | Refactor; brings the reference provider plugin onto the revised contract. |
| task | task-22 | story-1 | covered | Proves a streaming step's dispatch records usage end to end on both providers. |
| task | task-23 | story-1 | covered | Covers the failed and unparseable dispatch paths, which must record no usage. |
| task | task-23.1 | story-8 | covered | Keeps metering three-valued and forbids any invented cost. |
| task | task-24 | story-8 | covered | Prevents partial observations from becoming a usage record. |
| task | task-25 | story-9 | covered | Delivers live burn and child observability to a streaming step. |
| task | task-25.1 | story-9 | covered | Keeps observation silence from reading as a stall. |
| task | task-26 | story-9 | covered | Verify-only; records the before/after operator-visibility comparison the ADR assumption requires. |
| adr | adr-2026-08-24-one-dispatch-member-on-the-provider-contract | story-2, story-3, story-4 | covered | Authored here. Story 2 delivers the one called member, Story 3 the contract removal and loosened plugin validation, Story 4 the stream-consumer seam. |
| adr | adr-2026-08-24-streaming-dispatch-requests-the-machine-envelope | story-5, story-6, story-9 | covered | Authored here. Story 5 requests the envelope on every non-REPL dispatch, Story 6 preserves the REPL exception, Story 9 relocates operator visibility onto the consumer. |
| adr | adr-2026-07-22-build-dispatch-json-usage-capture | story-1, story-8 | covered | Amended additively: its consequence that `invokeInteractive` sessions are unmetered where they occur is falsified because the path acquired unattended traffic. Its usage-capture decision and envelope keys are honored unchanged by Story 1 and Story 8. |
| adr | adr-2026-08-19-live-provider-stream-observation | story-5, story-4 | covered | Amended additively: D8 scoped the format change to autonomous `invoke()` only, on the ground that a human already watches the other path. Cross-layer check — Story 5 reverses that scope, which is why the clause is amended rather than cited as-is; D1-D7 and the observation-only boundary are honored unchanged by Story 4, whose negative path asserts the consumer has no dispatch authority. |
| adr | adr-2026-07-03-reactive-model-fallback-ladder | story-7 | covered | Amended additively: D4's two call sites become one. The decision's substance — the availability cache is consulted before dispatch — is honored by Story 7 and asserted by task 15.1. |
| adr | adr-2026-07-27-cold-start-within-step-retries | story-2 | covered | Amended additively: the branch-session fall-through into a second provider method ceases to exist. Story 2 delivers that unification; the ADR's finding about #1069's over-claim is historical and unaffected. |
| criterion | Story 1 happy: Given a step outside `AUTONOMOUS_STEPS` dispatched through the engine, when the dispatch completes successfully, then its emitted `provider_attempt` event carries a `tokenUsage` object with input, output, and cache token counts. | task-22 | covered | A streaming step's `provider_attempt` event carries a non-absent `tokenUsage` for both providers. | diff-local |
| criterion | Story 1 happy: Given a claude-routed streaming dispatch that completes, when its usage is read, then `costUsd` is populated from the provider's reported cost. | task-23.1 | covered | an envelope reporting tokens and a cost classifies fully metered with the provider's own figure | diff-local |
| criterion | Story 1 happy: Given a codex-routed streaming dispatch that completes, when its usage is read, then token counts are populated and cost follows the rate-card pricing already in force. | task-22, task-23.1 | covered | an envelope reporting tokens with no cost and a model absent from the rate card classifies cost-unmetered | diff-local |
| criterion | Story 1 happy: Given a feature whose steps are all streaming steps, when the feature finishes, then the count of dispatches recording no usage at all is zero. | task-22 | covered | The test fails if the envelope request is removed from the non-REPL branch. | diff-local |
| criterion | Story 1 negative: Given a streaming dispatch whose provider exits non-zero before emitting a terminal result, when the dispatch is classified, then the result records no `tokenUsage` and the dispatch is classified `unmetered` rather than recorded as zero-cost. | task-23 | covered | a non-zero-exit streaming dispatch yields no `tokenUsage` and classifies unmetered | diff-local |
| criterion | Story 1 negative: Given a streaming dispatch whose stdout is not parseable as the expected envelope, when the dispatch is classified, then the failure is surfaced as a provider/parse failure and no fabricated usage is attached. | task-23 | covered | an unparseable stdout produces a parse or provider failure and no usage | diff-local |
| criterion | Story 1 negative: Given a streaming dispatch that is killed by a signal mid-stream, when the dispatch is classified, then partial observations do not become a completed usage record. | task-24 | covered | A signal-killed dispatch that emitted observations returns a result with no `tokenUsage`. | diff-local |
| criterion | Story 2 happy: Given the engine source, when every provider dispatch call site is enumerated, then each one calls `invoke` and none calls a second dispatch member. | task-19 | covered | `grep -rn 'invokeInteractive' src/conductor/src` returns no match outside comments. | diff-local |
| criterion | Story 2 happy: Given an autonomous step and a streaming step, when both are dispatched, then both reach the provider through the same adapter entry point. | task-4, task-10 | covered | The spy test observes exactly one dispatch implementation for both modes. | diff-local |
| criterion | Story 2 happy: Given the recovery menu's interactive-fix path, when it dispatches, then it reaches the provider through that same entry point with the REPL option set. | task-16 | covered | The call site calls `invoke` with the REPL option set and supplies no consumer. | diff-local |
| criterion | Story 2 happy: Given each built-in adapter, when its dispatch implementation is inspected, then argument construction and completion classification are each reached by one path rather than duplicated per mode. | task-4, task-10 | covered | contains one method that spawns the provider process; the other path delegates to it and contains no spawn of its own | diff-local |
| criterion | Story 2 negative: Given a source-level check over the engine, when it finds any engine call to a dispatch member other than `invoke`, then the check fails and names the call site. | task-19 | covered | `grep -rn 'invokeInteractive' src/conductor/src` returns no match outside comments. | diff-local |
| criterion | Story 2 negative: Given a streaming dispatch, when the engine dispatches it, then no wrapper substitutes a different provider method in place of `invoke`. | task-17 | covered | The wrapper no longer defines `invoke` or `invokeInteractive` members. | diff-local |
| criterion | Story 3 happy: Given the `LLMProvider` type, when its members are inspected, then it declares no `invokeInteractive`. | task-19 | covered | Write a failing test asserting `LLMProvider` has no `invokeInteractive` member | diff-local |
| criterion | Story 3 happy: Given an `llm_provider` plugin entrypoint exporting only `invoke`, when the plugin loader validates it, then it loads successfully. | task-20 | covered | a plugin exporting only `invoke` loads | diff-local |
| criterion | Story 3 happy: Given an `llm_provider` plugin entrypoint exporting both `invoke` and an extra `invokeInteractive`, when the plugin loader validates it, then it still loads successfully and the extra member is never called. | task-20 | covered | a plugin exporting both `invoke` and an extra `invokeInteractive` loads and never has the extra member called | diff-local |
| criterion | Story 3 happy: Given the reference provider plugin in `plugins/`, when it is compiled against the current contract, then it compiles and its tests pass. | task-21 | covered | `plugins/recorder-provider` compiles against the revised contract and its existing tests pass. | diff-local |
| criterion | Story 3 negative: Given an `llm_provider` plugin entrypoint exporting no `invoke`, when the plugin loader validates it, then loading fails with an error naming the missing `invoke` member. | task-20 | covered | a plugin exporting no `invoke` fails with an error naming `invoke` | diff-local |
| criterion | Story 3 negative: Given an `llm_provider` plugin entrypoint whose `invoke` is not a function, when the plugin loader validates it, then loading fails rather than registering an unusable provider. | task-20 | covered | a plugin whose `invoke` is not a function fails | diff-local |
| criterion | Story 3 negative: Given a plugin that exports only `invokeInteractive`, when the plugin loader validates it, then loading fails naming `invoke` — the removed member does not satisfy the requirement. | task-20 | covered | a plugin exporting only `invokeInteractive` fails naming `invoke` | diff-local |
| criterion | Story 4 happy: Given a dispatch supplied with a stream consumer, when the provider emits stream observations, then the consumer receives them carrying running uncached input, cached input, and output token counts. | task-7 | covered | A non-REPL dispatch supplied a consumer receives observations with non-zero `uncachedInputTokens` and `outputTokens`. | diff-local |
| criterion | Story 4 happy: Given a dispatch supplied with no stream consumer, when it runs, then it behaves exactly as the buffered dispatch does today and emits no observations to any consumer. | task-7 | covered | A dispatch supplied no consumer emits no observation and behaves as the buffered dispatch does today. | diff-local |
| criterion | Story 4 happy: Given a claude dispatch and a codex dispatch each supplied with a consumer, when each runs, then both deliver observations through that same consumer. | task-7, task-11 | covered | A non-REPL codex dispatch supplied a consumer receives observations with non-zero token counts. | diff-local |
| criterion | Story 4 negative: Given a dispatch supplied with a consumer whose callback throws, when an observation is emitted, then the dispatch continues to completion and the thrown error does not fail or terminate it. | task-8 | covered | The dispatch returns a successful result despite every observation callback throwing. | diff-local |
| criterion | Story 4 negative: Given a dispatch supplied with a consumer, when the consumer attempts to influence the dispatch, then it has no timeout, kill, retry, or lifecycle authority and the dispatch is unaffected. | task-1, task-8 | covered | A doc comment on the field states it grants no timeout, kill, retry, or lifecycle authority. | diff-local |
| criterion | Story 4 negative: Given a REPL dispatch, when it is constructed, then it supplies no stream consumer, and a dispatch that is both a REPL and carries a consumer is either unrepresentable or documented as leaving the consumer inert. | task-2 | covered | Either the type rejects `interactive: true` together with a supplied consumer, or the field's doc comment states the combination is inert and a test proves the consumer is never invoked on a REPL dispatch. | diff-local |
| criterion | Story 5 happy: Given a non-REPL claude dispatch, when its argument list is constructed, then it requests the stream-json envelope with the verbose flag. | task-3 | covered | a non-REPL dispatch's argument list contains `--print`, `--output-format`, `stream-json`, and `--verbose` | diff-local |
| criterion | Story 5 happy: Given a non-REPL codex dispatch, when its argument list is constructed, then it requests the JSON envelope. | task-9 | covered | a non-REPL dispatch's argument list contains the JSON flag | diff-local |
| criterion | Story 5 happy: Given a non-REPL dispatch on either provider, when completion is classified, then the classifier receives the envelope rather than the branch that discards usage. | task-5, task-10 | covered | A test asserts a non-REPL dispatch never reaches the usage-discarding branch | diff-local |
| criterion | Story 5 happy: Given the installed codex CLI, when a real non-REPL dispatch runs against it in a smoke context, then its emitted envelope is parseable by the existing codex parser. | task-9.1 | covered | The captured envelope from the installed codex CLI parses to a usage record with non-zero token counts. | outside-diff |
| criterion | Story 5 negative: Given a non-REPL dispatch whose envelope is missing its terminal result record, when classification runs, then it reports a parse failure naming the missing record rather than silently returning empty output. | task-23 | covered | an envelope missing its terminal result record reports a parse failure naming the missing record rather than returning empty output | diff-local |
| criterion | Story 5 negative: Given a non-REPL dispatch whose envelope contains a terminal record with no usage fields, when classification runs, then the result carries no `tokenUsage` and classifies `unmetered`. | task-23.1 | covered | an envelope whose terminal record carries no usage fields classifies unmetered | diff-local |
| criterion | Story 5 negative: Given a prompt large enough to exceed the argv length limit, when a non-REPL dispatch is constructed, then the prompt is delivered on stdin and the dispatch is not rejected before the provider starts. | task-6 | covered | A prompt of at least 200 KiB dispatched non-REPL appears in the child's stdin and not in its argv. | diff-local |
| criterion | Story 6 happy: Given a REPL dispatch, when its argument list is constructed, then it requests no machine envelope. | task-3, task-16 | covered | a REPL dispatch's list contains none of `--output-format`, `stream-json`, `--verbose` | diff-local |
| criterion | Story 6 happy: Given a REPL dispatch, when it runs, then its standard input remains attached to the operator's terminal and its output is rendered as plain text. | task-6 | covered | The REPL path still passes its prompt positionally, asserted by a second case. | diff-local |
| criterion | Story 6 happy: Given a REPL dispatch, when it completes, then the engine does not require a usage record from it. | task-23.1 | covered | A usage-absent dispatch contributes neither tokens nor cost to the feature rollup, and is still counted as unmetered. | diff-local |
| criterion | Story 6 negative: Given a REPL dispatch, when its output is inspected, then it contains no machine-envelope records rendered to the operator. | task-16 | covered | The resulting argument list is asserted to contain no envelope flag. | diff-local |
| criterion | Story 6 negative: Given a REPL dispatch that produces no parseable usage, when the feature's totals are computed, then it is counted as unmetered and contributes no fabricated cost. | task-23.1 | covered | A usage-absent dispatch contributes neither tokens nor cost to the feature rollup, and is still counted as unmetered. | diff-local |
| criterion | Story 7 happy: Given any dispatch through the unified path, when the adapter entry is reached, then fresh-session enforcement is applied and the dispatch carries a freshly minted session id. | task-13 | covered | Each adapter applies the fresh-session enforcement helper exactly once, at its single dispatch entry. | diff-local |
| criterion | Story 7 happy: Given an unattended codex dispatch, when its argument list is constructed, then the workspace-write sandbox, network access, approval policy, and reviewer configuration are all present. | task-12 | covered | The test enumerates the five configuration entries by name and fails if any one is dropped. | diff-local |
| criterion | Story 7 happy: Given any dispatch through the unified path, when the provider is selected, then the model-availability cache is consulted before dispatch and a model already marked dead is substituted with the first live ladder model. | task-15.1 | covered | The substitution case is asserted to dispatch the first live ladder model and to emit its warning. | diff-local |
| criterion | Story 7 happy: Given an autonomous step dispatched after unification, when its behavior is compared with before, then its text output and its recorded usage are unchanged. | task-14.1 | covered | An autonomous step's text output equals the pre-unification expectation, asserted on both providers. | diff-local |
| criterion | Story 7 happy: Given a dispatch supplied with a branch session id, when it is dispatched, then it reaches the provider through the same unified entry point as every other dispatch. | task-15 | covered | A branch-session dispatch is asserted to reach the same entry point as a non-branch dispatch. | diff-local |
| criterion | Story 7 negative: Given a dispatch that attempts to reuse a prior provider session, when it reaches the adapter entry, then fresh-session enforcement overrides it and no session is resumed. | task-13 | covered | A dispatch requesting reuse is asserted to resume no session, on both providers. | diff-local |
| criterion | Story 7 negative: Given a dispatch whose resolved model is marked dead and whose ladder has no live model, when it is dispatched, then the substitution does not silently hand over a dead model. | task-15.1 | covered | The exhausted-ladder case is asserted not to dispatch the dead model. | diff-local |
| criterion | Story 7 negative: Given an interactive codex dispatch, when its argument list is constructed, then the unattended sandbox configuration is not applied to it. | task-12 | covered | An interactive dispatch is asserted to carry none of them. | diff-local |
| criterion | Story 7 negative: Given the unified path, when a self-host dispatch runs, then executable resolution and self-host auth handoff still occur and are not lost with the removed method. | task-14 | covered | A self-host non-REPL dispatch is asserted to call executable resolution before spawning. | diff-local |
| criterion | Story 8 happy: Given a dispatch whose envelope reports tokens and a cost, when it is classified, then it is fully metered and its reported cost is the provider's own figure or the committed rate-card price, never an estimate. | task-23.1 | covered | an envelope reporting tokens and a cost classifies fully metered with the provider's own figure | diff-local |
| criterion | Story 8 happy: Given a dispatch whose envelope reports tokens but no cost and whose model is absent from the rate card, when it is classified, then it is `cost-unmetered`, contributing tokens and no cost. | task-23.1 | covered | an envelope reporting tokens with no cost and a model absent from the rate card classifies cost-unmetered, contributing tokens and no cost | diff-local |
| criterion | Story 8 happy: Given a dispatch that records no usage at all, when it is classified, then it is `unmetered` and contributes neither tokens nor cost. | task-23.1 | covered | an envelope whose terminal record carries no usage fields classifies unmetered | diff-local |
| criterion | Story 8 happy: Given a completed feature, when its totals line is produced, then dispatches that recorded no usage are still counted and reported as unmetered. | task-23.1 | covered | A usage-absent dispatch contributes neither tokens nor cost to the feature rollup, and is still counted as unmetered. | diff-local |
| criterion | Story 8 negative: Given a dispatch with no usage record, when totals are computed, then it does not contribute a zero-dollar entry counted as measured. | task-23.1 | covered | No file changed by this feature writes a `costUsd` value not sourced from the provider envelope or the committed rate card. | diff-local |
| criterion | Story 8 negative: Given a dispatch whose envelope reports a non-finite or non-numeric cost, when it is classified, then it is not treated as fully metered. | task-23.1 | covered | a non-finite or non-numeric reported cost does not classify as fully metered | diff-local |
| criterion | Story 8 negative: Given a streaming dispatch whose observations reported running token counts but which never completed, when totals are computed, then those partial observations do not become a usage record. | task-24 | covered | The test fails if usage is derived from accumulated observations. | diff-local |
| criterion | Story 9 happy: Given a streaming step running under the unified dispatch, when the operator observes it in progress, then progress is visible before the step completes rather than only at its end. | task-25 | covered | A running streaming step's live status reports non-zero running token counts before the step completes. | diff-local |
| criterion | Story 9 happy: Given a streaming step in progress, when its live token burn is read from the daemon status surface, then it reports running uncached input and output token counts for that step. | task-25 | covered | A running streaming step's live status reports non-zero running token counts before the step completes. | diff-local |
| criterion | Story 9 happy: Given a streaming step on a provider that can observe child activity, when it is running, then the active child count is reported rather than reported as unknown. | task-25 | covered | A provider without child observability reports unsupported, asserted to be distinguishable from zero. | diff-local |
| criterion | Story 9 negative: Given a streaming step whose provider emits no observations for an extended period, when the operator observes it, then the step is still shown as running and is not reported as stalled solely because no observation arrived. | task-25.1 | covered | A streaming step with no observations for the configured stall interval is reported running, not stalled. | diff-local |
| criterion | Story 9 negative: Given a streaming step on a provider that cannot observe child activity, when its status is read, then child observability is reported as unsupported rather than as a count of zero. | task-25 | covered | A provider without child observability reports unsupported, asserted to be distinguishable from zero. | diff-local |
| criterion | Story 9 negative: Given a comparison of the operator-visible output of one streaming step before and after this change, when the two are reviewed, then any loss of information is recorded and accepted, or the rendering is corrected. | task-26 | covered | A written before/after comparison for one named streaming step exists in the feature's evidence. | diff-local |
