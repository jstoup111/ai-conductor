# Coherence Mapping: prd-audit-has-no-criterion-key-for-an-over-scope-f

Tier M, technical track (no PRD — fr rows omitted). Outcomes staged from jstoup111/ai-conductor#1848.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-6 | covered | An audit with a no-owner unplanned change parses first-attempt: the section parses (Story 1) and the skill-taught shape is the parsed shape (Story 6) |
| outcome | outcome-2 | story-2, story-4 | covered | Unique keys per report (duplicates rejected, Story 2) plus key+summary decision binding (Story 4) make every recorded decision match exactly one finding |
| outcome | outcome-3 | story-1, story-2 | covered | No-owner findings live in their own section under their own keys (Story 1); a borrowed criterion key would be a duplicate and both rows reject (Story 2) |
| outcome | outcome-4 | story-3 | covered | Salvage keeps correctly-parsed findings; rejected rows are named with reasons and still block |
| outcome | outcome-5 | story-6 | covered | Shape parity proven by a fixture from the skill's documented example, including the no-owner scope finding |
| story | story-1 | task-1, task-2 | covered | Section parse + distinguishable keys (task-1); section-grade and out-of-section NC rejections and the legacy fixture (task-1, task-2) |
| story | story-2 | task-3 | covered | Duplicate criterion and NC ordinal rejection, per-row, with the #1848 case-2 fixture |
| story | story-3 | task-2, task-4 | covered | Salvage diagnostics (task-2); gate blocks on rejected rows and names them (task-4) |
| story | story-4 | task-6 | covered | Key+summary matcher, mismatch re-ask, criterion-only regression, unknown-key defect |
| story | story-5 | task-5, task-7, task-8 | covered | Relations/classification (task-5), routing + never-work (task-7), end-to-end lap (task-8) |
| story | story-6 | task-9 | covered | Skill NC contract, caveat removal, parity fixture, harness validation |
| task | task-1 | story-1 | covered | Section scan, NC key form, legacy-identical fixture |
| task | task-2 | story-3 | covered | Per-row rejection collection; also carries Story 1's section-rule negatives |
| task | task-3 | story-2 | covered | Duplicate-key rejection |
| task | task-4 | story-3 | covered | Gate + preserve-path treat rejected rows as blocking, named |
| task | task-5 | story-5 | covered | overScopeRelations + classification for NC keys |
| task | task-6 | story-4 | covered | NC decision binding and harvest widening |
| task | task-7 | story-5 | covered | routePrdAuditOverScope NC uniformity, decision block, no work paths |
| task | task-8 | story-5 | covered | End-to-end decision lap integration test |
| task | task-9 | story-6 | covered | SKILL.md contract + parity tests + integrity suite |
| adr | adr-2026-08-24-over-scope-decision-block-and-durable-refusals | story-4, story-5 | covered | Amended D4 (key space, NC summary binding) implemented by Story 4; D1/D2/D6/D7 semantics over the widened keys by Story 5 |
| adr | adr-2026-08-22-prd-audit-stories-authority-and-bounded-kickback | story-1, story-3, story-5 | covered | Amended D3: no-owner findings legitimate (Story 1), per-row rejection that still blocks (Story 3), never becomes work (Story 5) |
| criterion | Story 1 happy: Given a report with a valid Verdict Table and a no-owner section whose `Finding`-headed table has one row keyed NC.1 graded OVER_SCOPE with relation outside-visible and evidence text, when the report is parsed, then the parse succeeds and the result contains the NC.1 finding with its grade, intent relation, and evidence alongside the Verdict Table findings | task-1 | covered | NC.1/NC.2 findings returned with grade | diff-local |
| criterion | Story 1 happy: Given a report whose no-owner section contains two rows keyed NC.1 and NC.2, when the report is parsed, then both findings are returned and each is distinguishable by its key | task-1 | covered | keys unique in result | diff-local |
| criterion | Story 1 negative: Given a no-owner section row graded PASS instead of OVER_SCOPE, when the report is parsed, then that row is rejected with a named diagnostic stating the section admits only OVER_SCOPE and sibling rows are still returned | task-2 | covered | non-OVER_SCOPE grade inside the no-owner section | diff-local |
| criterion | Story 1 negative: Given a Verdict Table row keyed NC.1 (an NC key outside the no-owner section), when the report is parsed, then that row is rejected with a named diagnostic and sibling rows are still returned | task-2 | covered | NC key outside its section | diff-local |
| criterion | Story 1 negative: Given a well-formed report (every row keyed by a valid, unique story criterion) with no `## Findings without an owning criterion` section, when the report is parsed, then parsing behaves exactly as before this change and returns only Verdict Table findings | task-1 | covered | identical to today's | diff-local |
| criterion | Story 2 happy: Given a report where every Verdict Table criterion and every NC ordinal appears exactly once, when the report is parsed, then no duplicate-key diagnostics are produced | task-3 | covered | yields zero duplicate diagnostics | diff-local |
| criterion | Story 2 negative: Given a Verdict Table with two rows keyed S1.3 (one PASS, one OVER_SCOPE), when the report is parsed, then both S1.3 rows are rejected with a diagnostic naming S1.3 as duplicated and all other rows are still returned | task-3 | covered | S1.3 both PASS and OVER_SCOPE | diff-local |
| criterion | Story 2 negative: Given a no-owner section with two rows keyed NC.1, when the report is parsed, then both NC.1 rows are rejected with a diagnostic naming NC.1 as duplicated and all other rows are still returned | task-3 | covered | Duplicate NC ordinals reject both carriers | diff-local |
| criterion | Story 3 happy: Given a 57-row report where two rows carry invented keys (OS.1, OS.2) as in issue #1848 case (1), when the report is parsed, then the 55 valid rows are returned as findings and the two rejected rows are returned as diagnostics carrying the row's key text and the rejection reason | task-2 | covered | each carrying the offending key text | diff-local |
| criterion | Story 3 negative: Given a parse result containing one or more rejected-row diagnostics, when the prd_audit gate evaluates the report, then the gate does not pass and the resulting halt body names each rejected row and its reason | task-4 | covered | does not satisfy the gate even when every parsed finding is PASS | diff-local |
| criterion | Story 3 negative: Given a parse result with rejected rows and all parsed findings graded PASS, when the prd_audit gate evaluates the report, then the gate still does not pass — salvage never converts a rejected row into a passing report | task-4 | covered | all-PASS-plus-one-rejected-row fixture scores not-done | diff-local |
| criterion | Story 3 negative: Given a report missing its `**PRD:**` marker or missing the Verdict Table entirely, when the report is parsed, then the whole report is still a mechanical fault exactly as today, not a per-row rejection | task-2 | covered | still return the whole-report mechanical fault | diff-local |
| criterion | Story 4 happy: Given an accepted decision recorded for NC.1 with summary "unplanned npm test change", when a later lap's report lists NC.1 with the identical summary and relation outside-visible, then the finding classifies as accepted and does not block | task-6 | covered | Apply-on-match | diff-local |
| criterion | Story 4 happy: Given a criterion-keyed decision for S4.1, when a later lap re-reports S4.1 OVER_SCOPE with different summary wording, then the decision still applies (criterion-only matching for story-criterion keys is unchanged) | task-6 | covered | keep matching on criterion alone | diff-local |
| criterion | Story 4 negative: Given an accepted decision recorded for NC.1 with summary "unplanned npm test change", when a later lap's report lists the same substance renumbered as NC.2, then the finding classifies as blocking-undecided and the halt offers a fresh decision entry for it | task-6 | covered | re-ask-on-renumber | diff-local |
| criterion | Story 4 negative: Given an accepted decision recorded for NC.1, when a later lap's report lists NC.1 with a reworded summary, then the finding classifies as blocking-undecided and the operator is re-asked — the recorded decision is never applied to the mismatched finding | task-6 | covered | re-ask-on-reword | diff-local |
| criterion | Story 4 negative: Given a `HALT.cleared` decision entry naming a key the current report does not flag, when decisions are harvested, then nothing is recorded for that entry and the defect is surfaced by name (existing D7 behavior over the widened key space) | task-6 | covered | records nothing and surfaces the defect by name | diff-local |
| criterion | Story 5 happy: Given a parsed NC.1 finding with intent relation outside-visible and no recorded decision, when the prd_audit gate evaluates the report, then the halt's over-scope-decisions block contains an entry for NC.1 with its summary and relation, pending decision | task-7 | covered | undecided set contains NC.1 with its summary and relation | diff-local |
| criterion | Story 5 happy: Given a parsed NC.1 finding with relation within or outside-harmless, when the prd_audit gate evaluates the report, then NC.1 is recorded and does not block | task-7 | covered | recorded and non-blocking | diff-local |
| criterion | Story 5 negative: Given a refused decision for NC.1, when the next lap re-reports NC.1 with matching summary, then the halt names NC.1 as refused — rework required, and does not re-offer a pending entry for it | task-7 | covered | not re-offered pending | diff-local |
| criterion | Story 5 negative: Given a parsed NC finding of any relation, when routing computes follow-up work, then no plan task is appended and no kickback names the NC finding as work — it routes only to the operator decision block | task-7 | covered | appends plan tasks or emits kickback work | diff-local |
| criterion | Story 6 happy: Given the section format exactly as documented in `skills/prd-audit/SKILL.md` (including its NC key contract added in this change), when a report following it verbatim is parsed, then it parses with zero rejected rows | task-9 | covered | parses with zero rejected rows | diff-local |
| criterion | Story 6 happy: Given the skill text after this change, when it is read, then it teaches the NC.«n» key form and no longer claims the engine cannot route no-owner findings | task-9 | covered | caveat sentence is absent | diff-local |
| criterion | Story 6 negative: Given a report following the OLD skill guidance (the section present but rows without NC keys), when the report is parsed, then those rows are rejected with diagnostics naming the missing/invalid key — not a whole-report mechanical fault | task-9 | covered | rejects per-row with a key-naming diagnostic | diff-local |
