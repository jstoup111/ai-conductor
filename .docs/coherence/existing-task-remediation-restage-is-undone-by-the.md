# Coherence: Existing-task remediation restage survives the Task-trailer union (#2196)

Plan stem: existing-task-remediation-restage-is-undone-by-the · Tier M · technical track · 2026-09-06

Row classes present: outcome (4 staged intake bullets), story (6), task (15), criterion (32). The fr class is omitted (technical track, no PRD). The adr class is omitted (no `.docs/decisions/adr-*` file is created, modified, or deleted in this change set).

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes | |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-4 | covered | Story 1 makes a restaged bound task unresolved so the round routes to build; Story 4 keeps the D1 halt only for rounds that stage nothing. |
| outcome | outcome-2 | story-2 | covered | Story 2 resolves a reopened task only by a post-restage trailered commit or a Done-when row close (fresh evidence), never by pre-restage trailers alone. |
| outcome | outcome-3 | story-3 | covered | Story 3 pins the #859 fresh-build handoff and the genuine-stall fixtures unmodified. |
| outcome | outcome-4 | story-4 | covered | Story 4 keeps derived-already-complete for a round that restages nothing and skips only the recheck on a consolidated FAIL round. |
| story | story-1 | task-3, task-7, task-8, task-9 | covered | Store corruption and root failure (task-3), corrupt-file abstention in the fold (task-7), the restage seam (task-8), and the route dispatch proof (task-9). |
| story | story-2 | task-4, task-5, task-6, task-15 | covered | Per-id counts (task-4), count-growth resolution (task-5), lowered-count and untouched-path negatives (task-6), worktree reconstruction (task-15). |
| story | story-3 | task-6, task-13, task-14 | covered | Missing-status and never-restaged paths (task-6), fresh-build count movement (task-13), unmodified #859 and stall fixtures (task-14). |
| story | story-4 | task-9 | covered | No-op halt preserved, restaged round routes, consolidated FAIL round restages with the recheck skipped (task-9). |
| story | story-5 | task-1, task-2, task-8, task-10 | covered | Store round-trip (task-1), merge and isolation (task-2), watermark write at the restage seam (task-8), kickback event field (task-10). |
| story | story-6 | task-11, task-12, task-13 | covered | Post-restage no-op baseline (task-11), refreshed lastResolvedCount (task-12), watcher and breaker agreement (task-13). |
| task | task-1 | story-5 | covered | Main-root store round-trip for Story 5. |
| task | task-2 | story-5 | covered | Merge-without-overwrite, stem isolation, engine-state untouched for Story 5. |
| task | task-3 | story-1 | covered | Corrupt-versus-absent and failed record for Story 1. |
| task | task-4 | story-2 | covered | Infrastructure: per-id trailer commit counts the fold and restage both consume (supports Story 2). |
| task | task-5 | story-2 | covered | Count-growth resolution for Story 2. |
| task | task-6 | story-2, story-3 | covered | Lowered count and untouched paths for Stories 2 and 3. |
| task | task-7 | story-1 | covered | Corrupt watermark abstention for Story 1. |
| task | task-8 | story-1, story-5 | covered | Restage records the watermark before rows for Stories 1 and 5. |
| task | task-9 | story-1, story-4 | covered | Route dispatch and preserved D1 guard for Stories 1 and 4. |
| task | task-10 | story-5 | covered | Kickback event field for Story 5. |
| task | task-11 | story-6 | covered | Post-restage no-op baseline for Story 6. |
| task | task-12 | story-6 | covered | Refreshed lastResolvedCount for Story 6. |
| task | task-13 | story-6, story-3 | covered | Watcher and breaker agreement for Stories 6 and 3. |
| task | task-14 | story-3 | covered | Verify-only: #859 and stall fixtures unmodified for Story 3. |
| task | task-15 | story-2 | covered | Watermark-aware reconstruction for Story 2. |
| criterion | Story 1 happy: **Given** a plan task with a `Task:` trailer from an earlier lap and a `completed` or `pending` row, **When** an existing-task remediation binds a finding to it and restages, **Then** the build completion predicate reports that task unresolved and the round routes to `build` with the remediation hint instead of halting. | task-9 | covered | "existing-task round on a trailered task routes to build" | diff-local |
| criterion | Story 1 happy: **Given** two bound tasks restaged in the same round, one with three prior trailered commits and one with one, **When** the completion predicate runs, **Then** both are unresolved and the completion-miss reason names both ids. | task-9 | covered | "two bound ids both unresolved after restage" | diff-local |
| criterion | Story 1 negative: **Given** a bound id that is absent from `task-status.json`, **When** the restage runs, **Then** the route halts `needs-human` naming the absent id and records no watermark for any id in that round. | task-8 | covered | "an absent bound id fails the restage and writes no watermark" | diff-local |
| criterion | Story 1 negative: **Given** a watermark file for this feature that is present but unparseable, **When** the fold resolves any task id the rows still show as `pending`, **Then** that id is treated as unresolved from trailers, a diagnostic naming the corrupt file is logged, and the corrupt file is never read as "no watermarks". | task-7 | covered | "corrupt watermark file excludes trailer resolution and warns once" | diff-local |
| criterion | Story 1 negative: **Given** a restage whose main repo root cannot be resolved (no git common dir), **When** the record helper runs, **Then** the route halts `needs-human` naming the unresolvable root and writes neither rows nor watermark. | task-8 | covered | "unresolvable main root fails the restage before any row write" | diff-local |
| criterion | Story 1 negative: **Given** a round whose bound task already sits at `pending` with no trailer at all, **When** the restage runs, **Then** the watermark records a count of zero and the task is unresolved exactly as before — the watermark never makes an unresolved task resolved. | task-8 | covered | "a bound id with no trailer is recorded at count 0 and stays unresolved" | diff-local |
| criterion | Story 2 happy: **Given** a restaged task whose watermark records two trailered commits, **When** a new commit carrying `Task: <id>` lands on the branch so the count becomes three, **Then** the fold resolves the id and the build completion predicate no longer lists it. | task-5 | covered | "one new trailered commit resolves it" | diff-local |
| criterion | Story 2 happy: **Given** a restaged task, **When** the `Done when:` close contract flips its row to `completed`, **Then** the fold resolves the id from the row regardless of its trailer count. | task-5 | covered | "a completed row resolves regardless of count" | diff-local |
| criterion | Story 2 happy: **Given** a restaged task and a canonical-alias trailer (`Task: T16` for plan id `16`), **When** a new commit carries the alias after the restage, **Then** the alias counts toward the same id and the task resolves. | task-5 | covered | "an alias trailer after the restage resolves it" | diff-local |
| criterion | Story 2 negative: **Given** a restaged task whose trailer count is unchanged since the watermark, **When** the fold runs on any later evaluation in the same worktree, **Then** the id stays unresolved no matter how many pre-restage trailers exist. | task-5 | covered | "unchanged count keeps a watermarked id unresolved" | diff-local |
| criterion | Story 2 negative: **Given** a restaged task whose branch was rewritten so its trailer count is now lower than the watermark, **When** the fold runs, **Then** the id stays unresolved until a new trailered commit raises the count above the watermark, and it is never reported resolved because the count merely changed. | task-6 | covered | "a lowered trailer count stays unresolved until it exceeds the watermark" | diff-local |
| criterion | Story 2 negative: **Given** a worktree removed and recreated from its branch after a restage, so `task-status.json` must be rebuilt from trailers, **When** the reconstruction and then the fold run in the new worktree, **Then** the reopened task is restored as `pending` rather than `completed`, the watermark is read from the main root, and the id is reported unresolved until a new trailered commit lands — the reconstruction never silently converges the reopened task. | task-15 | covered | "recreated worktree keeps the reopened task unresolved until new work lands" | diff-local |
| criterion | Story 2 negative: **Given** a task that was never restaged, **When** the fold runs, **Then** no watermark is consulted and its resolution is byte-identical to the pre-change fold. | task-6 | covered | "never-restaged ids resolve identically with and without a watermark file" | diff-local |
| criterion | Story 3 happy: **Given** a plan whose every task carries a `Task:` trailer and whose `task-status.json` rows are all `pending`, with no watermark recorded, **When** the build completion predicate runs, **Then** it reports done and the loop hands off to `build_review`. | task-14 | covered | "all tasks trailer-resolved ⇒ done" | diff-local |
| criterion | Story 3 happy: **Given** the same fresh build, **When** the stall circuit breaker samples `countResolvedTasks` before and after a dispatch that added a trailered commit, **Then** the count rises by one and no stall is declared. | task-13 | covered | "fresh build with no watermark file gains one per trailered commit" | diff-local |
| criterion | Story 3 negative: **Given** a fresh build with two tasks lacking trailers and no watermark recorded, **When** the build completion predicate runs, **Then** it reports not done naming those two ids, exactly as before this change. | task-14 | covered | "unresolved ids named in reason" | diff-local |
| criterion | Story 3 negative: **Given** a fresh build whose `task-status.json` is missing, **When** the build completion predicate runs, **Then** it reports not done with the existing "missing task-status.json" reason and consults no watermark. | task-6 | covered | "missing task-status.json returns the existing reason without consulting watermarks" | diff-local |
| criterion | Story 4 happy: **Given** a remediation round whose fixes append or upsert only task ids that are already evidence-complete and restage nothing, **When** the D1 guard recomputes build completion, **Then** it halts with the existing `derived-already-complete` kickback outcome and gap ledger. | task-9 | covered | "pre-existing `planRemediation D1` and `D3` tests" | diff-local |
| criterion | Story 4 negative: **Given** a remediation round that restaged at least one bound task, **When** the D1 guard recomputes build completion, **Then** it does not halt and the round routes to `build`. | task-9 | covered | "existing-task round on a trailered task routes to build" | diff-local |
| criterion | Story 4 negative: **Given** a consolidated manual-test FAIL round that also carries an existing-task gap, **When** the route runs, **Then** the bound ids are restaged and watermarked exactly as in any other round, and only the D1 completion recheck is skipped, because that round's dispatchable work is the FAIL itself. | task-9 | covered | "consolidated FAIL round restages and watermarks with the D1 recheck skipped" | diff-local |
| criterion | Story 5 happy: **Given** an existing-task round that restages ids 16 and 21, **When** the restage completes, **Then** exactly one `kickback` event for that round carries an additive field listing `16` and `21` with their recorded trailer counts, and `<mainRoot>/.daemon/restage-watermarks/<plan-stem>.json` holds the same ids and counts. | task-10 | covered | "one kickback event per round carries restaged ids and counts" | diff-local |
| criterion | Story 5 happy: **Given** a recorded watermark for id 16, **When** a later round restages id 19 only, **Then** the watermark file holds both ids and the earlier count for 16 is not overwritten. | task-2 | covered | "a later round adds ids without overwriting an earlier count" | diff-local |
| criterion | Story 5 happy: **Given** a recorded watermark, **When** the daemon re-kicks the feature in the same worktree, **Then** the watermark is read back unchanged and the fold applies it on the first evaluation. | task-1 | covered | "record then read round-trips id → count at the main root" | diff-local |
| criterion | Story 5 negative: **Given** no watermark file exists for the feature's stem, **When** it is read, **Then** the reader returns an empty watermark map, and `.pipeline/engine-state.json` (including `appendedRemediationTaskIds`) is never written by any watermark operation. | task-2 | covered | "engine-state.json is byte-identical across record and read" | diff-local |
| criterion | Story 5 negative: **Given** two features with different plan stems in sibling worktrees of the same main root, **When** each restages, **Then** each writes only its own stem's file and neither read returns the other's ids. | task-2 | covered | "two stems under one main root are isolated" | diff-local |
| criterion | Story 5 negative: **Given** a restage that fails before writing task-status, **When** the route halts, **Then** no `kickback` event carries a restage field and engine-state gains no watermark. | task-10 | covered | "a failed restage emits no restaged field" | diff-local |
| criterion | Story 5 negative: **Given** two consecutive restages of the same id in one round, **When** the record helper runs, **Then** it writes the id once with the count observed at the first restage and emits one event field entry, not two. | task-8 | covered | "a repeated id in one round is recorded once" | diff-local |
| criterion | Story 6 happy: **Given** a round that restages one task with three prior trailered commits, **When** the #647 no-op baseline is captured, **Then** its `resolvedCount` equals the post-restage count (the restaged id excluded) and a build that adds one trailered commit for that id is classified `did-work`. | task-11 | covered | "no-op baseline resolvedBefore equals the post-restage count" | diff-local |
| criterion | Story 6 happy: **Given** the same round, **When** the stall circuit breaker samples the count before and after a dispatch that added the trailered commit, **Then** it observes movement of exactly one and declares no stall. | task-13 | covered | "countResolvedTasks moves by exactly one per post-restage trailered commit and by zero without one" | diff-local |
| criterion | Story 6 negative: **Given** the same round, **When** a build attempt adds no commit at all, **Then** the breaker observes no movement and applies its existing stall handling — the restage itself is never counted as movement. | task-13 | covered | "countResolvedTasks moves by exactly one per post-restage trailered commit and by zero without one" | diff-local |
| criterion | Story 6 negative: **Given** a round whose baseline was taken before the restage, **When** the post-restage count is compared, **Then** the drop is not reported as `did-work` and the `build-progress-watcher` reports the restaged id as pending, in agreement with the fold. | task-13 | covered | "watcher reports a restaged id pending and a non-restaged trailered id resolved" | diff-local |
| criterion | Story 6 negative: **Given** a durable progress sample taken before the restage (`lastResolvedCount` in `task-evidence.json`, or the #280 `noEvidenceAttempts` counter's `resolvedTasksBefore`), **When** the round restages a task, **Then** that sample is refreshed to the post-restage count before the next build attempt is measured, so the deliberate drop is never recorded as zero progress, regression, or re-kick ineligibility. | task-12 | covered | "lastResolvedCount equals the post-restage count" | diff-local |

## Consistency pass

Cross-layer pairs sharing a subject were tested in both directions.

- outcome-2 ("returns to resolved/complete through new work (a new commit or fresh evidence), not through pre-restage history alone") against task-5's Done-when "a completed row resolves regardless of count": a `completed` row is written only by the Done-when close contract, which is fresh evidence, so both hold. No contradiction.
- outcome-3 (#859 preserved) against task-5 and task-15, which withhold trailer resolution for reopened ids: the narrowing applies only to ids an existing-task round explicitly reopened and only until a new trailered commit lands; task-6 and task-14 pin that every other id resolves byte-identically. Satisfying either side leaves the other true. No oscillation.
- outcome-4 (D1 guard still fires when nothing new is staged) against outcome-1 (a restaged task dispatches a build): the guard's input is the same predicate in both cases; task-9 proves both shapes from one fixture. No oscillation.
- outcome-1 against task-11 and task-12 (baselines refreshed after the restage): a refreshed baseline makes the post-restage build classify as did-work only when a trailered commit lands, which is exactly the work outcome-2 requires. No contradiction.

## Assumptions

None left open. The one plan-time discovery (reconstruction restores a trailered task as `completed`, bypassing the trailer watermark) was resolved in DECIDE by task-15 and the amended architecture review, not deferred to BUILD.
