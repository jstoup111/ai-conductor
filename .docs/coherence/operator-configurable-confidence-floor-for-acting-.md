# Coherence Mapping: Operator-configurable confidence floor for acting on build_review findings

**Date:** 2026-09-06  
**Tier:** M  
**Track:** technical — the `fr` row class is omitted because this track has no PRD.  
**Outcome source:** none required. `jstoup111/ai-conductor#2383` carries no `## Desired outcome`
section, so the staged `.pipeline/intake-outcomes.md` holds a Source-Ref and zero bullets, and the
outcome layer reads as not-required rather than as a gap.  
**ADR row pool:** one non-deleted ADR file in this spec change set — the governing adjudication ADR,
amended in this pass with decisions D4.1-D4.5. Its four citable decision ids are D1 through D4;
D4.1-D4.5 are sub-items of D4 and are not separately citable.  
**Consistency pass:** every covered row was re-read in both directions. Two cross-layer
contradictions were found and resolved during DECIDE rather than recorded here as `fail`: Story 4's
original claim that a demoted lap leaves `cumulative` unchanged contradicted the approved
convergence ADR's reset-on-PASS rule, and Story 2's unconditional demotion contradicted Story 5's
inert-floor requirement. Both were amended in the stories before this artifact was authored; the
resolutions are recorded in `.docs/conflicts/operator-configurable-confidence-floor-for-acting-.md`.
No contradiction or oscillation remains.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
| --- | --- | --- | --- | --- |
| story | story-1 | task-1, task-2, task-3, task-18 | covered | Integer retype and range validation land across the artifact reader, the durable store, and the adjudication context, with the fixture migration completing the retype. |
| story | story-2 | task-6, task-7 | covered | The floor is applied at judgement admission and constrained so it can only narrow an action into a deferral. |
| story | story-3 | task-8, task-9, task-10 | covered | The synthesized deferral body, cross-lap effect-id stability, and the failed-filing path together deliver file-exactly-once. |
| story | story-4 | task-11, task-12 | covered | The kickback gate is bypassed outright on the demotion path, and no work order or kickback event is produced. |
| story | story-5 | task-13 | covered | The floor is guarded on the presence of the deferral dependencies, recomputed each lap. |
| story | story-6 | task-14, task-15 | covered | The fully demoted lap reaches the existing pass branch while the unfinalized-deferral, infrastructure, and surviving-action blockers are all preserved. |
| story | story-7 | task-16, task-17 | covered | The demotion reason rides an additive field on an existing remediation event and is rendered into the per-lap trace. |
| story | story-8 | task-4, task-5 | covered | The config key is validated as a bounded integer and declares its production consumer so the registry totality test passes. |
| task | task-1 | story-1 | covered | Implements Story 1; type happy-path. |
| task | task-2 | story-1 | covered | Implements Story 1; type negative-path. |
| task | task-3 | story-1 | covered | Supporting purpose: carries the retyped confidence through the adjudication context so Story 1 typechecks end to end. |
| task | task-4 | story-8 | covered | Implements Story 8; type happy-path. |
| task | task-5 | story-8 | covered | Supporting purpose: satisfies the config-key consumer registry obligation created by Story 8. |
| task | task-6 | story-2 | covered | Implements Story 2; type happy-path. |
| task | task-7 | story-2 | covered | Implements Story 2; type negative-path. |
| task | task-8 | story-3 | covered | Implements Story 3; type happy-path. |
| task | task-9 | story-3 | covered | Implements Story 3; type negative-path. |
| task | task-10 | story-3 | covered | Implements Story 3; type negative-path. |
| task | task-11 | story-4 | covered | Implements Story 4; type happy-path. |
| task | task-12 | story-4 | covered | Implements Story 4; type negative-path. |
| task | task-13 | story-5 | covered | Implements Story 5; type negative-path. |
| task | task-14 | story-6 | covered | Implements Story 6; type happy-path. |
| task | task-15 | story-6 | covered | Implements Story 6; type negative-path. |
| task | task-16 | story-7 | covered | Implements Story 7; type happy-path. |
| task | task-17 | story-7 | covered | Implements Story 7; type happy-path. |
| task | task-18 | story-1 | covered | Supporting purpose: migrates the confidence fixtures the Story 1 retype invalidates. |
| adr | adr-2026-08-29-mixed-build-review-laps-preserve-content-adjudication | story-2, story-3, story-4, story-5, story-6, story-7 | covered | D4.1-D4.5 were appended to this ADR during this DECIDE pass and are implemented by these stories. Section 4f judgement on the plan's four obligation rows: D1 and D2 are correctly no-change, because the floor neither reclassifies a lap nor adds a dispatch; D3 is correctly task-dispositioned, because task-15's Done-when asserts a lap retaining an act case still routes to BUILD, which is D3's precedence clause; D4 is correctly task-dispositioned, because task-6's Done-when asserts the sub-floor admission as a deferral that publishes no work order, which is the decision itself. |
| criterion | Story 1 happy: Given an adjudication result whose case carries `"confidence": 72`, when the engine reads the remediation artifact, then the case is accepted and its confidence is retained as the integer 72. | task-1 | covered | The reader accepts an integer confidence of 0, 72, and 100 and returns each unchanged on the parsed case. | diff-local |
| criterion | Story 1 happy: Given an adjudication result whose case carries `"confidence": 0`, when the engine reads the remediation artifact, then the case is accepted, because 0 is a valid confidence and not an absent value. | task-1 | covered | The reader accepts an integer confidence of 0, 72, and 100 and returns each unchanged on the parsed case. | diff-local |
| criterion | Story 1 happy: Given an adjudication result whose case carries `"confidence": 100`, when the engine reads the remediation artifact, then the case is accepted. | task-1 | covered | The reader accepts an integer confidence of 0, 72, and 100 and returns each unchanged on the parsed case. | diff-local |
| criterion | Story 1 negative: Given a case carrying `"confidence": 101`, when the engine reads the remediation artifact, then the whole adjudication is rejected with reason `invalid-case-confidence` and no case is stamped. | task-1 | covered | The reader returns reason invalid-case-confidence for 101, for -1, for 72.5, and for a string confidence. | diff-local |
| criterion | Story 1 negative: Given a case carrying `"confidence": -1`, when the engine reads the remediation artifact, then the whole adjudication is rejected with reason `invalid-case-confidence`. | task-1 | covered | The reader returns reason invalid-case-confidence for 101, for -1, for 72.5, and for a string confidence. | diff-local |
| criterion | Story 1 negative: Given a case carrying `"confidence": 72.5`, when the engine reads the remediation artifact, then the whole adjudication is rejected with reason `invalid-case-confidence`, because confidence must be an integer. | task-1 | covered | The reader returns reason invalid-case-confidence for 101, for -1, for 72.5, and for a string confidence. | diff-local |
| criterion | Story 1 negative: Given a case carrying `"confidence": "high"`, when the engine reads the remediation artifact, then the whole adjudication is rejected with reason `invalid-case-confidence`, because the enum form is no longer accepted. | task-1 | covered | The reader returns reason invalid-case-confidence for 101, for -1, for 72.5, and for a string confidence. | diff-local |
| criterion | Story 1 negative: Given a case omitting `confidence` entirely, when the engine reads the remediation artifact, then the whole adjudication is rejected, because the key set is exact. | task-1 | covered | A case omitting confidence entirely is rejected by the existing exact-key check. | diff-local |
| criterion | Story 1 negative: Given a durable case store record whose persisted confidence is out of range, when the store is read, then the read fails closed as malformed state rather than admitting the record. | task-2 | covered | The durable case store rejects a persisted case whose confidence is out of range or non-integer as malformed state. | diff-local |
| criterion | Story 2 happy: Given a tracker repository is resolvable, `act_min_confidence` is 70, and an adjudication returns an `act` case with confidence 40, when the lap is adjudicated, then that case is recorded as a `defer` case and no BUILD work order is published for it. | task-6 | covered | With a tracker resolvable and a floor of 70, an act case of confidence 40 is admitted as a defer case carrying a deferral effect and publishes no work order. | diff-local |
| criterion | Story 2 happy: Given `act_min_confidence` is 70 and an adjudication returns an `act` case with confidence 70, when the lap is adjudicated, then the case remains an `act` case, because the floor is a minimum and not an exclusive bound. | task-6 | covered | An act case whose confidence equals the floor, and one above it, are admitted unchanged as act cases. | diff-local |
| criterion | Story 2 happy: Given `act_min_confidence` is 70 and an adjudication returns an `act` case with confidence 95, when the lap is adjudicated, then the case remains an `act` case and publishes its BUILD work order as today. | task-6 | covered | An act case whose confidence equals the floor, and one above it, are admitted unchanged as act cases. | diff-local |
| criterion | Story 2 happy: Given `act_min_confidence` is left unset and an adjudication returns an `act` case with confidence 1, when the lap is adjudicated, then the case remains an `act` case, because the default floor of 0 never demotes. | task-6 | covered | With the floor absent, an act case of confidence 1 is admitted unchanged. | diff-local |
| criterion | Story 2 happy: Given a tracker repository is resolvable and an adjudication returns both a sub-floor `act` case and an at-floor `act` case, when the lap is adjudicated, then only the sub-floor case is demoted and the other still publishes its work order. | task-6 | covered | No action effect is ever reserved for a demoted case, and a lap demoting one of two act cases still admits the other as an action. | diff-local |
| criterion | Story 2 negative: Given a tracker repository is resolvable, `act_min_confidence` is 70, and an adjudication returns a `defer` case with confidence 40, when the lap is adjudicated, then the case is unchanged, because the floor never alters a case that is already deferred. | task-7 | covered | A defer case below the floor and a reject case below the floor are admitted identical to their input. | diff-local |
| criterion | Story 2 negative: Given a tracker repository is resolvable, `act_min_confidence` is 70, and an adjudication returns a `reject` case with confidence 40, when the lap is adjudicated, then the case is unchanged, because the floor never alters a rejection. | task-7 | covered | A defer case below the floor and a reject case below the floor are admitted identical to their input. | diff-local |
| criterion | Story 2 negative: Given `act_min_confidence` is 90 and an adjudication returns a `defer` case with confidence 95, when the lap is adjudicated, then the case remains deferred, because the engine may narrow an action but may never promote a deferral into an action. | task-7 | covered | A defer case above the floor is never converted into an act case. | diff-local |
| criterion | Story 2 negative: Given a demoted case, when its stored record is read back, then its disposition is `defer` and its effect kind is `deferral`, with no residual action effect anywhere in the store. | task-6 | covered | No action effect is ever reserved for a demoted case, and a lap demoting one of two act cases still admits the other as an action. | diff-local |
| criterion | Story 2 negative: Given a tracker repository is resolvable, `act_min_confidence` is 70, and an adjudication returns an `act` case with confidence 40, when the demotion is applied, then it happens before case reconciliation, so no action effect is ever reserved and later contradicted. | task-6 | covered | No action effect is ever reserved for a demoted case, and a lap demoting one of two act cases still admits the other as an action. | diff-local |
| criterion | Story 3 happy: Given a sub-floor `act` case is demoted on a lap, when its deferral effect runs, then an intake issue is filed carrying the effect's hidden marker and a body with the existing Observed, Impact, Desired Outcomes and Hypotheses sections. | task-8 | covered | A demoted case's deferral body contains its case reference, the judgement rationale, the reported confidence, and the applied floor. | diff-local |
| criterion | Story 3 happy: Given a demoted case whose deferral body is synthesized by the engine, when the issue is filed, then the body states the case reference, the judgement's rationale, the reported confidence, and the floor that demoted it. | task-8 | covered | A demoted case's deferral body contains its case reference, the judgement rationale, the reported confidence, and the applied floor. | diff-local |
| criterion | Story 3 happy: Given a demoted case that was already filed on a previous lap, when the same case is demoted again on the next lap, then the existing issue is reused via its exact marker and no second issue is created. | task-9 | covered | The second lap reuses the existing issue by exact marker and files no duplicate, including when that issue has been closed. | diff-local |
| criterion | Story 3 negative: Given a demoted case filed on lap one, when lap two demotes the same case and the prior issue has since been closed, then the closed issue is reused and no duplicate is filed. | task-9 | covered | The second lap reuses the existing issue by exact marker and files no duplicate, including when that issue has been closed. | diff-local |
| criterion | Story 3 negative: Given a demoted case, when the same feature runs three consecutive laps that each demote it, then exactly one issue exists for it across all three laps. | task-9 | covered | Three consecutive demoting laps leave exactly one issue for the case. | diff-local |
| criterion | Story 3 negative: Given a demoted case whose issue filing fails, when the lap settles, then the effect is recorded as failed and the lap does not report a clean pass around an unfiled finding. | task-10 | covered | A demoted case whose issue filing fails has its effect recorded as failed. | diff-local |
| criterion | Story 3 negative: Given a demoted case, when its effect id is compared across two laps at the same floor and confidence, then the id is identical, because the demotion is deterministic and precedes reconciliation. | task-9 | covered | Two consecutive laps demoting the same case at the same floor produce an identical effect id. | diff-local |
| criterion | Story 3 negative: Given two distinct sub-floor cases demoted on the same lap, when their deferrals are filed, then two separate issues exist with distinct markers and neither dedups against the other. | task-9 | covered | Two distinct demoted cases on one lap file two issues with distinct markers. | diff-local |
| criterion | Story 4 happy: Given a lap whose only `act` case is demoted, when the lap settles, then the kickback ledger's `count` for `build_review` is unchanged from before the lap. | task-11 | covered | A lap whose only act case is demoted leaves count unchanged and does not invoke the kickback gate for that case. | diff-local |
| criterion | Story 4 happy: Given a lap whose only `act` case is demoted, when the lap settles, then the demotion itself does not increment the ledger's `cumulative` value; a subsequent pass may still reset it to 0, which is the existing convergence rule and not an effect of the demotion. | task-11 | covered | The demotion does not increment cumulative; any reset observed on a passing lap comes from the existing pass-convergence rule. | diff-local |
| criterion | Story 4 happy: Given a lap with one demoted case and one surviving `act` case, when the lap settles, then exactly one kickback is charged, for the surviving action only. | task-11 | covered | A lap mixing a demoted case and a surviving act case charges exactly one kickback. | diff-local |
| criterion | Story 4 negative: Given a lap whose only `act` case is demoted, when the lap settles, then the kickback gate is not invoked at all for the demoted case, rather than invoked for a zero charge. | task-11 | covered | A lap whose only act case is demoted leaves count unchanged and does not invoke the kickback gate for that case. | diff-local |
| criterion | Story 4 negative: Given a feature one kickback below its cumulative cap, when a lap demotes its only `act` case, then the demotion does not advance it toward the cap and it does not halt on budget exhaustion. | task-11 | covered | A feature one kickback below its cumulative cap is not advanced toward the cap by a demotion and does not halt. | diff-local |
| criterion | Story 4 negative: Given a lap whose only `act` case is demoted, when the lap settles, then no BUILD work order is published and BUILD is not re-dispatched. | task-12 | covered | No BUILD work order is published for a demoted case and BUILD is not re-dispatched for it. | diff-local |
| criterion | Story 4 negative: Given a demoted case, when the event stream for the lap is read, then it contains no `kickback` event attributable to that case. | task-12 | covered | The lap's event stream contains no kickback event attributable to a demoted case. | diff-local |
| criterion | Story 5 happy: Given no tracker repository can be resolved and `act_min_confidence` is 70, when an adjudication returns an `act` case with confidence 40, then the case remains an `act` case and publishes its BUILD work order as though no floor were set. | task-13 | covered | With tracker dependencies absent, a sub-floor act case keeps its act disposition and publishes its work order. | diff-local |
| criterion | Story 5 happy: Given a tracker repository is resolvable and `act_min_confidence` is 70, when an adjudication returns an `act` case with confidence 40, then the case is demoted, confirming the inert behavior is conditional and not permanent. | task-13 | covered | Inertness is recomputed each lap, so the same case is demoted on a later lap once a tracker resolves. | diff-local |
| criterion | Story 5 negative: Given no tracker repository can be resolved, when a lap adjudicates a sub-floor `act` case, then the lap does not halt on an unfinished deferral effect. | task-13 | covered | With tracker dependencies absent, the lap route is identical with the floor set and with it unset, and the lap does not halt on an unfinished deferral. | diff-local |
| criterion | Story 5 negative: Given no tracker repository can be resolved, when a lap adjudicates a sub-floor `act` case, then no deferral effect is reserved for it, so nothing is left in a reserved state across laps. | task-13 | covered | With tracker dependencies absent, no deferral effect is reserved for a sub-floor case, so nothing is left reserved across laps. | diff-local |
| criterion | Story 5 negative: Given no tracker repository can be resolved, when a lap adjudicates a sub-floor `act` case, then the lap's route is identical to the route it would take with the floor unset. | task-13 | covered | With tracker dependencies absent, the lap route is identical with the floor set and with it unset, and the lap does not halt on an unfinished deferral. | diff-local |
| criterion | Story 5 negative: Given the tracker becomes resolvable on a later lap, when the same sub-floor case is adjudicated again, then it is demoted on that lap, because inertness is evaluated per lap and not cached. | task-13 | covered | Inertness is recomputed each lap, so the same case is demoted on a later lap once a tracker resolves. | diff-local |
| criterion | Story 6 happy: Given a lap whose every `act` case is demoted and whose rubrics are otherwise healthy, when the lap settles, then build_review passes and the step is recorded done. | task-14 | covered | A lap whose every act case was demoted, whose deferrals all finalized and whose rubrics are healthy reaches a pass verdict through the conductor's build_review gate. | diff-local |
| criterion | Story 6 happy: Given a lap whose every `act` case is demoted, when the lap settles, then BUILD is not re-entered. | task-14 | covered | That lap neither re-enters BUILD nor halts. | diff-local |
| criterion | Story 6 happy: Given a lap with three `act` cases all below the floor, when the lap settles, then all three are filed as deferrals and the lap still passes. | task-14 | covered | A lap with three sub-floor act cases files three deferrals and still passes. | diff-local |
| criterion | Story 6 negative: Given a lap whose every `act` case is demoted, when the lap settles, then it does not halt with a route of `halt`, and specifically not on an unfinished-effect reason. | task-14 | covered | That lap neither re-enters BUILD nor halts. | diff-local |
| criterion | Story 6 negative: Given a lap whose every `act` case is demoted but one deferral effect has not finalized, when the lap settles, then the lap halts rather than passing, because an unfinished effect still blocks a pass. | task-15 | covered | A lap with an unfinalized deferral halts rather than passing. | diff-local |
| criterion | Story 6 negative: Given a lap whose every `act` case is demoted while an uncovered infrastructure failure remains, when the lap settles, then the lap follows the existing mechanical lane rather than passing, because content demotion does not clear an infrastructure blocker. | task-15 | covered | A lap with a remaining uncovered infrastructure failure follows the mechanical lane rather than passing. | diff-local |
| criterion | Story 6 negative: Given a lap with one demoted case and one surviving `act` case, when the lap settles, then the lap routes to BUILD rather than passing. | task-15 | covered | A lap retaining at least one act case routes to BUILD rather than passing. | diff-local |
| criterion | Story 7 happy: Given a case is demoted, when the lap's event stream is read, then it contains a remediation event carrying the demotion reason, including the reported confidence and the applied floor. | task-16 | covered | Each demotion emits its reason on the persisted event spine, carrying the reported confidence and the applied floor. | diff-local |
| criterion | Story 7 happy: Given a case is demoted, when the lap's adjudication trace is rendered, then it contains a line naming the case, its confidence, and the floor that demoted it. | task-17 | covered | The rendered adjudication trace contains one line per demoted case naming the case, its confidence, and the applied floor. | diff-local |
| criterion | Story 7 happy: Given a lap demotes two cases, when the trace is rendered, then both appear as separate lines. | task-17 | covered | A lap demoting two cases renders two distinct lines. | diff-local |
| criterion | Story 7 negative: Given a case is demoted, when the event stream is read, then the demotion is not emitted as a `kickback` event, because no kickback was charged. | task-16 | covered | The demotion is never emitted as a kickback event, and any new event member introduced instead declares its sink and its audit-trail mapping. | diff-local |
| criterion | Story 7 negative: Given a lap that demotes nothing, when its event stream is read, then no demotion reason appears on any event. | task-16 | covered | A lap that demotes nothing emits no demotion reason on any event. | diff-local |
| criterion | Story 7 negative: Given a case is demoted, when the demotion record is inspected, then it is stamped at the time of demotion rather than reconstructed later from stored case state. | task-16 | covered | The demotion reason is stamped at demotion time rather than derived afterwards from stored case state. | diff-local |
| criterion | Story 7 negative: Given a demoted case, when the lap subsequently halts for an unrelated reason, then the demotion line still appears in the halt evidence. | task-17 | covered | The demotion lines appear in the halt evidence when the lap halts for an unrelated reason. | diff-local |
| criterion | Story 7 negative: Given the demotion record rides an existing event member, when a new event member is introduced instead, then that member declares its sink and appears in the audit-trail mapping, so event completeness is preserved. | task-16 | covered | The demotion is never emitted as a kickback event, and any new event member introduced instead declares its sink and its audit-trail mapping. | diff-local |
| criterion | Story 8 happy: Given a project config setting `build_review.adjudication.act_min_confidence` to 70, when config loads, then it is accepted and the resolved value is 70. | task-4 | covered | The key is accepted at 0, 70 and 100 and resolves to the configured integer. | diff-local |
| criterion | Story 8 happy: Given a project config that omits the key, when config loads, then the resolved value is 0 and no warning is produced. | task-4 | covered | An absent key resolves to a floor of 0. | diff-local |
| criterion | Story 8 happy: Given a project config setting the key to 0, when config loads, then it is accepted and the floor never demotes. | task-4 | covered | The key is accepted at 0, 70 and 100 and resolves to the configured integer. | diff-local |
| criterion | Story 8 happy: Given a project config setting the key to 100, when config loads, then it is accepted. | task-4 | covered | The key is accepted at 0, 70 and 100 and resolves to the configured integer. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to 101, when config loads, then loading fails with a validation error naming the exact `build_review.adjudication.act_min_confidence` path and its permitted range. | task-4 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to -5, when config loads, then loading fails with a validation error naming the exact path. | task-4 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to 70.5, when config loads, then loading fails with a validation error, because the value must be an integer. | task-4 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to the string "70", when config loads, then loading fails with a validation error, because the value must be an integer and is not coerced. | task-4 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting a misspelled `act_min_confidance`, when config loads, then loading fails with the existing unknown-key error naming the `build_review.adjudication` block. | task-4 | covered | A misspelled sibling key still fails with the existing unknown-key error naming the block. | diff-local |
| criterion | Story 8 negative: Given the config-key consumer registry, when its totality test runs, then `build_review.adjudication.act_min_confidence` declares a resolvable production consumer and the test passes. | task-5 | covered | The key declares a resolvable production consumer in the config-key consumer registry. | diff-local |
