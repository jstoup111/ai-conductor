# Coherence Check: Monitor daemon HALTs through a guided resolution queue

Date: 2026-09-20
Source: jstoup111/ai-conductor#1228
Tier: L; product track; session-default model.
Verdict: PASS — all required layers covered, no waiver required.

## Inputs and Judgment

This review compares the 10 staged issue outcomes, 27 functional requirements, the 2 approved ADRs in the change set (adr-2026-09-20-halt-resolution-queue-derived-from-markers, adr-2026-09-20-operator-launched-sessions-retain-conductor-authority), 19 accepted stories, 21 plan tasks, and 121 extracted criteria. Every counterpart id was confirmed against its artifact file; every criterion quote was matched as an exact substring of a cited task's `Done when` block.

Consistency pass (§4d): no cross-layer contradiction. The one contradiction found during conflict-check — FR-9's priority ordering against FR-21's deferral rule — was resolved before this pass by amending FR-9 so deferral partitions ahead of priority, and by replacing the superseded assertions in Story 9 and Story 16. Task-7 implements that precedence and task-15 depends on it, so no task now asserts strict band order across the partition boundary.

PRD↔stories tie-out (§4e): every FR-1..FR-27 is cited by a story whose scenarios exercise it, and no story cites an undeclared FR.

## Coverage

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote |
|---|---|---|---|---|
| outcome | outcome-1 | story-1 | covered | An operator can run one dedicated monitoring command that stays active and surfaces daemon HALTs as work to resolve. |
| outcome | outcome-2 | story-2 | covered | The operator can choose to monitor every registered daemon project or one selected project. |
| outcome | outcome-3 | story-3 | covered | On startup, the monitor discovers work that is already halted and places it into the resolution queue before waiting for new HALTs. |
| outcome | outcome-4 | story-5, story-8 | covered | Each detected HALT is presented once in a durable resolution queue, including enough project and feature context to act on it. |
| outcome | outcome-5 | story-9, story-10 | covered | Higher-priority HALTs are offered before lower-priority HALTs, with stable, observable ordering when priorities are equal or absent. |
| outcome | outcome-6 | story-12, story-13 | covered | For the next queued HALT, the monitor opens a session using the configured provider, performs evidence-based debugging, and presents the applicable user-guided recovery runbook. |
| outcome | outcome-7 | story-15 | covered | Ending that provider session returns the operator to the monitor, which advances to the next queued HALT without requiring the monitor to be restarted. |
| outcome | outcome-8 | story-16 | covered | Skipping a HALT ends its current session and re-enqueues it for a later pass instead of resolving, dropping, or immediately reopening it. |
| outcome | outcome-9 | story-1, story-5 | covered | When no HALTs are pending, the monitor remains ready for newly detected HALTs without creating duplicate queue entries or provider sessions. |
| outcome | outcome-10 | story-17 | covered | A HALT that cannot be diagnosed or resolved remains visible with its state and evidence; it is not silently dropped or marked resolved. |
| fr | fr-1 | story-1 | covered | Story 1 exercises this requirement directly. |
| fr | fr-2 | story-2 | covered | Story 2 exercises this requirement directly. |
| fr | fr-3 | story-3 | covered | Story 3 exercises this requirement directly. |
| fr | fr-4 | story-4 | covered | Story 4 exercises this requirement directly. |
| fr | fr-5 | story-5 | covered | Story 5 exercises this requirement directly. |
| fr | fr-6 | story-6 | covered | Story 6 exercises this requirement directly. |
| fr | fr-7 | story-7 | covered | Story 7 exercises this requirement directly. |
| fr | fr-8 | story-8 | covered | Story 8 exercises this requirement directly. |
| fr | fr-9 | story-9 | covered | Story 9 orders by band within the deferral partition, per the 2026-09-20 amendment to FR-9. |
| fr | fr-10 | story-10 | covered | Story 10 exercises this requirement directly. |
| fr | fr-11 | story-10 | covered | Story 10 exercises this requirement directly. |
| fr | fr-12 | story-11 | covered | Story 11 exercises this requirement directly. |
| fr | fr-13 | story-12 | covered | Story 12 exercises this requirement directly. |
| fr | fr-14 | story-12 | covered | Story 12 exercises this requirement directly. |
| fr | fr-15 | story-13 | covered | Story 13 exercises this requirement directly. |
| fr | fr-16 | story-14 | covered | Story 14 exercises this requirement directly. |
| fr | fr-17 | story-13 | covered | Story 13 exercises this requirement directly. |
| fr | fr-18 | story-5 | covered | Story 5 exercises this requirement directly. |
| fr | fr-19 | story-15 | covered | Story 15 exercises this requirement directly. |
| fr | fr-20 | story-16 | covered | Story 16 exercises this requirement directly. |
| fr | fr-21 | story-16 | covered | Story 16 keeps a deferred item behind unseen work regardless of its band. |
| fr | fr-22 | story-17 | covered | Story 17 exercises this requirement directly. |
| fr | fr-23 | story-1 | covered | Story 1 exercises this requirement directly. |
| fr | fr-24 | story-1 | covered | Story 1 exercises this requirement directly. |
| fr | fr-25 | story-18 | covered | Story 18 exercises this requirement directly. |
| fr | fr-26 | story-19 | covered | Story 19 exercises this requirement directly. |
| fr | fr-27 | story-19 | covered | Story 19 exercises this requirement directly. |
| story | story-1 | task-17, task-20, task-21 | covered | Delivered by task-17, task-20, task-21. |
| story | story-2 | task-3, task-20 | covered | Delivered by task-3, task-20. |
| story | story-3 | task-1 | covered | Delivered by task-1. |
| story | story-4 | task-6 | covered | Delivered by task-6. |
| story | story-5 | task-6, task-18 | covered | Delivered by task-6, task-18. |
| story | story-6 | task-2, task-6 | covered | Delivered by task-2, task-6. |
| story | story-7 | task-2 | covered | Delivered by task-2. |
| story | story-8 | task-1 | covered | Delivered by task-1. |
| story | story-9 | task-7 | covered | Delivered by task-7. |
| story | story-10 | task-7 | covered | Delivered by task-7. |
| story | story-11 | task-8 | covered | Delivered by task-8. |
| story | story-12 | task-9, task-11 | covered | Delivered by task-9, task-11. |
| story | story-13 | task-12 | covered | Delivered by task-12. |
| story | story-14 | task-10, task-13 | covered | Delivered by task-10, task-13. |
| story | story-15 | task-14, task-18 | covered | Delivered by task-14, task-18. |
| story | story-16 | task-7, task-15, task-18 | covered | Delivered by task-7, task-15, task-18. |
| story | story-17 | task-16 | covered | Delivered by task-16. |
| story | story-18 | task-4, task-5 | covered | Delivered by task-4, task-5. |
| story | story-19 | task-19 | covered | Delivered by task-19. |
| task | task-1 | story-3, story-8 | covered | The enumerator returns one entry per halted worktree carrying slug, reason, and halt class, as asserted by the two-halt fixture test. |
| task | task-2 | story-7, story-6 | covered | A halted, operator-parked feature produces no queue entry, as asserted by the park fixture test. |
| task | task-3 | story-2 | covered | A three-project fixture produces one merged list containing halts from every selected project, each entry labelled with its project. |
| task | task-4 | story-18 | covered | The deferral key is a structured type carrying project, feature, and halt identity as separate fields, as asserted by a round-trip test that reads the fields back individually. |
| task | task-5 | story-18 | covered | A deferral whose stored halt identity does not match the feature's current halt does not suppress the item, as asserted by the changed-identity test. |
| task | task-6 | story-5, story-4, story-6 | covered | A halt created after startup appears in a subsequent pass's queue with no restart. |
| task | task-7 | story-9, story-10, story-16 | covered | A fixture of mixed-priority unseen halts orders highest band first. |
| task | task-8 | story-11 | covered | A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue. |
| task | task-9 | story-12 | covered | Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn. |
| task | task-10 | story-14 | covered | Launching with no attached interactive terminal is refused and reports why, spawning nothing. |
| task | task-11 | story-12 | covered | The session's opening input contains the halt's project, feature, reason, and classification, as asserted against a mocked launch boundary. |
| task | task-12 | story-13 | covered | Each recognized classification presents its corresponding recovery procedure, as asserted by one case per member of the existing disposition union. |
| task | task-13 | story-14 | covered | A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary. |
| task | task-14 | story-15 | covered | Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue. |
| task | task-15 | story-16 | covered | Skipping ends the current session and advances to the next item. |
| task | task-16 | story-17 | covered | A session exiting zero with the halt marker still present leaves the halt in the queue. |
| task | task-17 | story-1 | covered | Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes. |
| task | task-18 | story-5, story-15, story-16 | covered | Offering an item, opening a session, deferring an item, and ending a session each emit their own union member, as asserted by a recording emitter. |
| task | task-19 | story-19 | covered | A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub. |
| task | task-20 | story-1, story-2 | covered | The detector recognizes the verb with all-projects and single-project selectors, and returns help rather than null for a malformed-but-recognized invocation. |
| task | task-21 | story-1 | covered | The generated full help reference contains the monitor verb and its project selectors, as asserted by a help-rendering test. |
| criterion | Story 1 happy: Given at least one feature is halted in a selected project, when the operator starts the monitor, then it reports that halt as work to resolve rather than exiting. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 happy: Given the monitor is running and no halts exist anywhere in the selected projects, when a resolution pass completes, then the monitor reports an empty queue, remains active, and opens no session. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 happy: Given the monitor is running with an empty queue, when a feature halts in a selected project, then the next pass reports it without the operator restarting the monitor. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 happy: Given the monitor is running, when the operator ends it with an interrupt, then it stops and reports that it stopped. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 negative: Given the monitor is running with an empty queue, when several passes complete with no halts, then no queue entry and no provider session are created by any of those passes. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 negative: Given a guided session is open, when the operator ends the monitor, then the monitor stops without marking the open item resolved and without writing a deferral for it. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 negative: Given the operator ended the monitor while an item was open, when the monitor is started again and that feature is still halted, then the item is offered again. | task-17 | covered | "Starting with no halted features reports an empty queue, stays alive, and creates zero sessions across several passes." | diff-local |
| criterion | Story 1 negative: Given the monitor is invoked in an environment carrying the engine's daemon-session marker, when it starts, then it is refused by the existing entry guard and no queue is built. | task-20 | covered | "A test asserts the monitor verb is not in the session-sanctioned subcommand set and is refused under the daemon-session marker." | diff-local |
| criterion | Story 2 happy: Given three projects are registered and two contain halted features, when the operator runs the monitor across all registered projects, then the queue contains the halts from both projects. | task-3 | covered | "A three-project fixture produces one merged list containing halts from every selected project, each entry labelled with its project." | diff-local |
| criterion | Story 2 happy: Given three projects are registered and the operator names one of them, when the monitor runs, then the queue contains only halts from the named project. | task-3 | covered | "A three-project fixture produces one merged list containing halts from every selected project, each entry labelled with its project." | diff-local |
| criterion | Story 2 happy: Given the monitor is running across all registered projects, when a queue item is displayed, then it states which project it belongs to. | task-3 | covered | "A three-project fixture produces one merged list containing halts from every selected project, each entry labelled with its project." | diff-local |
| criterion | Story 2 negative: Given the operator names a project that is not registered, when the monitor starts, then it reports the unknown name, lists nothing, and exits non-zero without building a queue. | task-3 | covered | "An unknown project name exits non-zero with the name reported and nothing enumerated." | diff-local |
| criterion | Story 2 negative: Given one registered project's path no longer exists on disk, when the monitor runs across all projects, then that project is reported as unreadable and halts from the remaining projects are still queued. | task-3 | covered | "A three-project fixture produces one merged list containing halts from every selected project, each entry labelled with its project." | diff-local |
| criterion | Story 2 negative: Given one registered project's directory is unreadable due to permissions, when the monitor runs, then that project is reported and the pass completes with the other projects' halts. | task-3 | covered | "A three-project fixture produces one merged list containing halts from every selected project, each entry labelled with its project." | diff-local |
| criterion | Story 2 negative: Given the registry file is absent, when the monitor runs across all registered projects, then it reports that no projects are registered and exits without error rather than throwing. | task-3 | covered | "An absent registry reports that no projects are registered and exits zero; a malformed registry exits non-zero." | diff-local |
| criterion | Story 2 negative: Given the registry file contains malformed content, when the monitor runs, then it reports the registry as unreadable and exits non-zero rather than silently monitoring nothing. | task-3 | covered | "An absent registry reports that no projects are registered and exits zero; a malformed registry exits non-zero." | diff-local |
| criterion | Story 3 happy: Given two features halted before the monitor was started, when the monitor starts, then both appear in the queue on its first pass. | task-1 | covered | "The enumerator returns one entry per halted worktree carrying slug, reason, and halt class, as asserted by the two-halt fixture test." | diff-local |
| criterion | Story 3 happy: Given a feature halted before the monitor was started, when the monitor starts, then it offers that halt before waiting for any new halt to occur. | task-1 | covered | "The enumerator returns one entry per halted worktree carrying slug, reason, and halt class, as asserted by the two-halt fixture test." | diff-local |
| criterion | Story 3 negative: Given a feature halted and was then resolved before the monitor was started, when the monitor starts, then that feature does not appear in the queue. | task-1 | covered | "The enumerator returns one entry per halted worktree carrying slug, reason, and halt class, as asserted by the two-halt fixture test." | diff-local |
| criterion | Story 3 negative: Given a feature's halt marker exists but its worktree directory has been removed, when the monitor starts, then that feature is not offered as a live halt and the pass completes without throwing. | task-1 | covered | "The enumerator returns one entry per halted worktree carrying slug, reason, and halt class, as asserted by the two-halt fixture test." | diff-local |
| criterion | Story 3 negative: Given a project contains no worktrees at all, when the monitor starts, then that project contributes no entries and produces no error. | task-1 | covered | "The enumerator returns one entry per halted worktree carrying slug, reason, and halt class, as asserted by the two-halt fixture test." | diff-local |
| criterion | Story 4 happy: Given the monitor has completed a pass with an empty queue, when a feature halts and the next pass runs, then that halt appears in the queue. | task-6 | covered | "A halt created after startup appears in a subsequent pass's queue with no restart." | diff-local |
| criterion | Story 4 happy: Given the monitor is working through a queue of two items, when a third feature halts and membership is next recomputed, then the third halt is included in the ordering. | task-6 | covered | "A halt created after startup appears in a subsequent pass's queue with no restart." | diff-local |
| criterion | Story 4 negative: Given a feature halts and is resolved between two consecutive passes, when the later pass runs, then that feature is not offered. | task-6 | covered | "A halt created after startup appears in a subsequent pass's queue with no restart." | diff-local |
| criterion | Story 4 negative: Given a halt marker is written while a pass is midway through enumerating projects, when that pass completes, then the queue is internally consistent and the halt is picked up by that pass or the next one, never producing a duplicate entry across the two. | task-6 | covered | "A halt created after startup appears in a subsequent pass's queue with no restart." | diff-local |
| criterion | Story 5 happy: Given a halt is currently being worked in an open guided session, when membership is recomputed, then that halt is not offered a second time while its session is open. | task-6 | covered | "A duplicate enumeration of one feature collapses to exactly one queue entry." | diff-local |
| criterion | Story 5 happy: Given a queue of three halts, when the monitor works through them, then exactly one guided session exists at any moment. | task-6 | covered | "A duplicate enumeration of one feature collapses to exactly one queue entry." | diff-local |
| criterion | Story 5 negative: Given a halt is currently open in a guided session, when a pass recomputes membership, then the monitor does not open a second session for that same halt. | task-6 | covered | "A duplicate enumeration of one feature collapses to exactly one queue entry." | diff-local |
| criterion | Story 5 negative: Given the same feature is somehow enumerated twice within one pass, when the queue is built, then it yields a single entry for that feature. | task-6 | covered | "A duplicate enumeration of one feature collapses to exactly one queue entry." | diff-local |
| criterion | Story 5 negative: Given two monitors are run over the same project by the same operator, when both compute membership, then each offers the halt independently and neither corrupts the other's state, the redundant offer being accepted as a duplicate prompt rather than a data fault. | task-6 | covered | "A duplicate enumeration of one feature collapses to exactly one queue entry." | diff-local |
| criterion | Story 6 happy: Given a queued halt is resolved during its guided session, when membership is next recomputed, then that feature is no longer in the queue. | task-6 | covered | "A halt created and cleared between passes never reaches the operator." | diff-local |
| criterion | Story 6 happy: Given a queued halt is cleared by the daemon between passes, when the next pass runs, then that feature is not offered. | task-6 | covered | "A halt created and cleared between passes never reaches the operator." | diff-local |
| criterion | Story 6 happy: Given a halt was resolved, when the queue is displayed, then the resolved feature is absent rather than shown with a resolved status. | task-6 | covered | "A halt created and cleared between passes never reaches the operator." | diff-local |
| criterion | Story 6 negative: Given a feature's halt marker is removed while its guided session is still open, when the session ends and membership is recomputed, then the feature is absent from the queue and is not reported as unresolved. | task-6 | covered | "A halt created and cleared between passes never reaches the operator." | diff-local |
| criterion | Story 6 negative: Given a feature's halt marker is removed and a new halt marker is written for the same feature before the next pass, when that pass runs, then the feature is offered as current halted work. | task-6 | covered | "A halt created and cleared between passes never reaches the operator." | diff-local |
| criterion | Story 6 negative: Given a halt marker is present alongside the completion marker that existing state scanning treats as reclaimable, when membership is computed, then that feature is not offered as halted. | task-6 | covered | "A halt created and cleared between passes never reaches the operator." | diff-local |
| criterion | Story 7 happy: Given a feature is halted and also carries an operator park marker, when membership is computed, then that feature is not offered. | task-2 | covered | "A halted, operator-parked feature produces no queue entry, as asserted by the park fixture test." | diff-local |
| criterion | Story 7 happy: Given a parked feature is unparked while still halted, when membership is next computed, then it is offered. | task-2 | covered | "A halted, operator-parked feature produces no queue entry, as asserted by the park fixture test." | diff-local |
| criterion | Story 7 negative: Given a feature carries a park marker and the park-marker check cannot complete due to a read error, when membership is computed, then the feature is treated as parked and is not offered, the check confirming absence of a park before offering. | task-2 | covered | "A halted, operator-parked feature produces no queue entry, as asserted by the park fixture test." | diff-local |
| criterion | Story 7 negative: Given a park marker is added while that feature's guided session is open, when the session ends, then the feature is absent from the recomputed queue. | task-2 | covered | "A halted, operator-parked feature produces no queue entry, as asserted by the park fixture test." | diff-local |
| criterion | Story 8 happy: Given a queued halt, when the queue is displayed, then the item states its project, its feature, the stated halt reason, and the halt classification. | task-1 | covered | "A halt whose class sidecar is missing yields the existing `unclassified` disposition rather than an invented label or an empty string." | diff-local |
| criterion | Story 8 happy: Given a halt whose stated reason spans several lines, when the item is displayed, then the operator-facing reason is presented without the display becoming unreadable. | task-1 | covered | "A halt whose class sidecar is missing yields the existing `unclassified` disposition rather than an invented label or an empty string." | diff-local |
| criterion | Story 8 negative: Given a halt whose marker body is empty, when the item is displayed, then the reason is reported as unstated and the item is still offered. | task-1 | covered | "A halt whose marker body is empty yields a reason reported as unstated and the entry is still returned." | diff-local |
| criterion | Story 8 negative: Given a halt whose classification sidecar is missing, when the item is displayed, then the classification is reported as undetermined and the item is still offered. | task-1 | covered | "A halt whose class sidecar is missing yields the existing `unclassified` disposition rather than an invented label or an empty string." | diff-local |
| criterion | Story 8 negative: Given a halt whose marker cannot be read at display time, when the item is displayed, then the read failure is reported against that item and the remaining items still display. | task-1 | covered | "A halt marker whose worktree directory is absent yields no entry and throws nothing." | diff-local |
| criterion | Story 9 happy: Given two unseen halted features whose linked issues carry different priority labels, when the queue is ordered, then the higher-priority halt is offered before the lower-priority one. | task-7 | covered | "A fixture of mixed-priority unseen halts orders highest band first." | diff-local |
| criterion | Story 9 happy: Given unseen halts spanning several priority bands, when the queue is ordered, then they appear in descending band order among themselves. | task-7 | covered | "A fixture of mixed-priority unseen halts orders highest band first." | diff-local |
| criterion | Story 9 happy: Given one deferred critical halt and one unseen low-priority halt, when the queue is ordered, then the unseen low-priority halt is offered first, deferral partitioning the queue ahead of priority. | task-7 | covered | "A fixture pairing a deferred critical halt with an unseen low-priority halt offers the unseen one first, proving deferral partitions ahead of priority." | diff-local |
| criterion | Story 9 happy: Given several deferred halts of differing bands, when they are offered after unseen work is exhausted, then they appear in descending band order among themselves. | task-7 | covered | "A fixture pairing a deferred critical halt with an unseen low-priority halt offers the unseen one first, proving deferral partitions ahead of priority." | diff-local |
| criterion | Story 9 happy: Given the ordering reuses the existing priority resolver, when several halts share one linked reference, then that reference's priority is fetched once for the pass rather than once per item. | task-7 | covered | "A fixture of mixed-priority unseen halts orders highest band first." | diff-local |
| criterion | Story 9 negative: Given a halted feature whose linked issue does not exist, when the queue is ordered, then that item is still queued and its band is reported as unresolved rather than the pass failing. | task-7 | covered | "A fixture of mixed-priority unseen halts orders highest band first." | diff-local |
| criterion | Story 9 negative: Given a halted feature with no linked issue reference at all, when the queue is ordered, then it is placed according to the existing band ranking for unlinked work and is still offered. | task-7 | covered | "A fixture of mixed-priority unseen halts orders highest band first." | diff-local |
| criterion | Story 9 negative: Given the priority lookup fails for one reference while succeeding for others, when the queue is ordered, then every halt is still queued and the failure is reported once rather than per item. | task-7 | covered | "A fixture of mixed-priority unseen halts orders highest band first." | diff-local |
| criterion | Story 10 happy: Given several halts carrying the same priority band, when the queue is ordered twice over identical contents, then both orderings are identical. | task-7 | covered | "Ordering identical queue contents twice yields byte-identical sequences for both equal-band and no-priority fixtures, and removing one item from an equal-band group leaves the remaining items' relative order unchanged." | diff-local |
| criterion | Story 10 happy: Given several halts for which no priority is available, when the queue is ordered twice over identical contents, then both orderings are identical. | task-7 | covered | "Ordering identical queue contents twice yields byte-identical sequences for both equal-band and no-priority fixtures, and removing one item from an equal-band group leaves the remaining items' relative order unchanged." | diff-local |
| criterion | Story 10 happy: Given a queue is displayed, when the operator reads it, then each item shows the band attributed to it and the ordering basis applied to the queue. | task-7 | covered | "Ordering identical queue contents twice yields byte-identical sequences for both equal-band and no-priority fixtures, and removing one item from an equal-band group leaves the remaining items' relative order unchanged." | diff-local |
| criterion | Story 10 negative: Given two halts whose bands are equal, when one of them is deferred and the queue is recomputed, then the remaining items hold their previous relative order. | task-7 | covered | "Ordering identical queue contents twice yields byte-identical sequences for both equal-band and no-priority fixtures, and removing one item from an equal-band group leaves the remaining items' relative order unchanged." | diff-local |
| criterion | Story 10 negative: Given the queue is recomputed after an unrelated feature halts, when the ordering is applied, then previously-ordered equal-band items retain their relative order with the new item placed by its own band. | task-7 | covered | "Ordering identical queue contents twice yields byte-identical sequences for both equal-band and no-priority fixtures, and removing one item from an equal-band group leaves the remaining items' relative order unchanged." | diff-local |
| criterion | Story 10 negative: Given priority is unresolved for every item, when the queue is displayed, then the ordering basis is reported as the fallback rather than implying a priority order that was never resolved. | task-7 | covered | "Ordering identical queue contents twice yields byte-identical sequences for both equal-band and no-priority fixtures, and removing one item from an equal-band group leaves the remaining items' relative order unchanged." | diff-local |
| criterion | Story 11 happy: Given priority resolution is in an outage, when the queue is built, then every halt is still offered, ordered by the stable fallback. | task-8 | covered | "A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue." | diff-local |
| criterion | Story 11 happy: Given priority resolution is in an outage, when the queue is displayed, then the operator is told once that ordering degraded to the fallback. | task-8 | covered | "A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue." | diff-local |
| criterion | Story 11 negative: Given priority resolution fails for every item, when the queue is built, then the queue is not empty and contains every halted feature. | task-8 | covered | "A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue." | diff-local |
| criterion | Story 11 negative: Given priority resolution fails, when several passes run during the same outage, then the operator is warned once for the outage rather than once per pass per item. | task-8 | covered | "A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue." | diff-local |
| criterion | Story 11 negative: Given priority resolution fails, when the queue is built, then the monitor does not block waiting on the lookup and the pass completes. | task-8 | covered | "A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue." | diff-local |
| criterion | Story 11 negative: Given priority resolution recovers after an outage, when the next pass runs, then band ordering is applied again without restarting the monitor. | task-8 | covered | "A forced priority outage yields a queue containing every halted feature, ordered by the stable fallback, never an empty queue." | diff-local |
| criterion | Story 12 happy: Given a queue whose head item is a halt, when the monitor reaches that item, then it opens a session in the operator's configured provider without the operator starting it. | task-9 | covered | "Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn." | diff-local |
| criterion | Story 12 happy: Given a session is opened for a halt, when it begins, then it has been given that halt's project, feature, stated reason, and classification as explicit input rather than starting from a blank prompt. | task-9 | covered | "Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn." | diff-local |
| criterion | Story 12 happy: Given the operator's configured provider is the non-default one, when a session is opened, then it is opened in that configured provider. | task-9 | covered | "Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn." | diff-local |
| criterion | Story 12 happy: Given a session is opened, when it begins, then it is a fresh session rather than a resumption of any previous session. | task-9 | covered | "Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn." | diff-local |
| criterion | Story 12 negative: Given the configured provider's binary is not on the path, when the monitor tries to open a session, then the failure is reported against that item, the item remains in the queue, and the monitor stays active. | task-9 | covered | "Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn." | diff-local |
| criterion | Story 12 negative: Given the configured provider fails to start, when the monitor tries to open a session, then no deferral is written for that item and it is not marked resolved. | task-9 | covered | "Both supported providers are exercised through the launch seam via a mocked process boundary, with the production adapter proven to reach the mock before any real spawn." | diff-local |
| criterion | Story 12 negative: Given no interactive terminal is attached, when the monitor reaches an item, then it refuses to open a session and reports why rather than spawning an unattended one. | task-10 | covered | "Launching with no attached interactive terminal is refused and reports why, spawning nothing." | diff-local |
| criterion | Story 12 negative: Given the configured provider name is not a registered provider, when the monitor starts, then it reports the unknown provider and opens no session. | task-9 | covered | "An unregistered configured provider name is reported and no process is spawned." | diff-local |
| criterion | Story 13 happy: Given a halt whose classification maps to a recovery procedure, when the guided session runs, then that procedure is presented. | task-12 | covered | "Each recognized classification presents its corresponding recovery procedure, as asserted by one case per member of the existing disposition union." | diff-local |
| criterion | Story 13 happy: Given a halt whose classification differs from the previous item's, when its session runs, then the procedure presented is the one for its own classification. | task-12 | covered | "Each recognized classification presents its corresponding recovery procedure, as asserted by one case per member of the existing disposition union." | diff-local |
| criterion | Story 13 happy: Given a halt is presented, when the session begins, then its evidence gathering happens without changing the state of the halted feature. | task-12 | covered | "Each recognized classification presents its corresponding recovery procedure, as asserted by one case per member of the existing disposition union." | diff-local |
| criterion | Story 13 negative: Given a halt whose classification is not one the monitor recognizes, when the session runs, then the halt is still presented with its classification stated as undetermined and is not skipped. | task-12 | covered | "Each recognized classification presents its corresponding recovery procedure, as asserted by one case per member of the existing disposition union." | diff-local |
| criterion | Story 13 negative: Given a halt whose classification sidecar is absent, when the session runs, then the halt is presented with its classification stated as undetermined and is not skipped. | task-12 | covered | "Each recognized classification presents its corresponding recovery procedure, as asserted by one case per member of the existing disposition union." | diff-local |
| criterion | Story 13 negative: Given a halt classification is added to the underlying classification set in future, when the monitor handles classifications, then handling is exhaustive over that set with no catch-all branch that would silently absorb the new member. | task-12 | covered | "Classification handling is exhaustive with no catch-all default, pinned by a test that fails to compile or fails at runtime if a union member is unhandled." | diff-local |
| criterion | Story 14 happy: Given a guided session opened by the monitor, when it attempts a conductor operation needed for recovery, then that operation is permitted rather than refused by the entry guard. | task-13 | covered | "A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary." | diff-local |
| criterion | Story 14 happy: Given a guided session proposes a state-changing recovery action, when it is about to act, then it presents the action and its blast radius and waits for approval before acting. | task-13 | covered | "A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary." | diff-local |
| criterion | Story 14 happy: Given the operator approves one recovery action, when a further state-changing action follows, then approval is requested again for that action. | task-13 | covered | "A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary." | diff-local |
| criterion | Story 14 happy: Given a guided session is diagnosing, when it gathers evidence, then it does so without requesting approval, diagnosis being read-only. | task-13 | covered | "A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary." | diff-local |
| criterion | Story 14 happy: Given a guided session runs, when its working directory is resolved, then it runs within the halted feature's own worktree. | task-13 | covered | "A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary." | diff-local |
| criterion | Story 14 negative: Given a guided session, when the operator declines a proposed recovery action, then the action is not performed and the halt remains. | task-13 | covered | "A session launched through the seam is not stamped with the daemon-session marker, as asserted on the child environment handed to the mocked boundary." | diff-local |
| criterion | Story 14 negative: Given the launch seam, when it is invoked from the daemon, a step runner, or any non-foreground path, then the launch is refused and no unmarked session is created. | task-10 | covered | "A static reachability test fails if the launch seam becomes importable from the daemon, from a step runner, or from any dispatched execution path." | diff-local |
| criterion | Story 14 negative: Given a session dispatched by the engine rather than launched by the monitor, when it attempts a conductor state-changing operation, then it is refused exactly as it is today, the sanctioned-subcommand set being unchanged. | task-13 | covered | "A test asserts the session-sanctioned subcommand set is unchanged and an engine-dispatched session is still refused for state-changing verbs." | diff-local |
| criterion | Story 14 negative: Given a guided session runs, when it writes provider configuration or acquires permissions, then those writes land inside the feature worktree and not in the main checkout. | task-13 | covered | "The guided session's resolved working directory is the halted feature's worktree, as asserted against the mocked boundary." | diff-local |
| criterion | Story 15 happy: Given a guided session for the head item ends normally, when the monitor continues, then it recomputes membership and offers the next item. | task-14 | covered | "Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue." | diff-local |
| criterion | Story 15 happy: Given a queue of three halts, when each session ends in turn, then all three are offered in order without the monitor being restarted. | task-14 | covered | "Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue." | diff-local |
| criterion | Story 15 happy: Given the last item's session ends and no halts remain, when the monitor continues, then it reports an empty queue and stays active. | task-14 | covered | "Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue." | diff-local |
| criterion | Story 15 negative: Given a guided session exits with a non-zero status, when the monitor continues, then it still returns to the queue and offers the next item. | task-14 | covered | "Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue." | diff-local |
| criterion | Story 15 negative: Given a guided session is terminated by a signal, when the monitor continues, then it still returns to the queue rather than exiting. | task-14 | covered | "Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue." | diff-local |
| criterion | Story 15 negative: Given a guided session ends having changed nothing, when membership is recomputed, then the same halt is offered again rather than being silently consumed. | task-14 | covered | "Sessions ending with zero status, non-zero status, and by signal all return the operator to the queue." | diff-local |
| criterion | Story 16 happy: Given a guided session is open, when the operator skips the item, then that session ends and the monitor moves to the next item. | task-15 | covered | "Skipping ends the current session and advances to the next item." | diff-local |
| criterion | Story 16 happy: Given an item was skipped, when the pass continues, then the skipped item is not offered ahead of items the operator has not yet seen, regardless of its priority band. | task-15 | covered | "Skipping ends the current session and advances to the next item." | diff-local |
| criterion | Story 16 happy: Given every item in the queue has been skipped, when a later pass runs, then the skipped items are offered again, ordered among themselves by priority band. | task-15 | covered | "A skipped item is not re-offered ahead of unseen work, and is offered again once unseen work is exhausted." | diff-local |
| criterion | Story 16 negative: Given an item is skipped, when membership is recomputed, then that item is not treated as resolved and its halt is still present. | task-15 | covered | "Skipping ends the current session and advances to the next item." | diff-local |
| criterion | Story 16 negative: Given an item is skipped, when the queue is next displayed, then the item is not dropped from the system and remains discoverable. | task-15 | covered | "Skipping ends the current session and advances to the next item." | diff-local |
| criterion | Story 16 negative: Given an item is skipped, when the immediately following item is selected, then the skipped item is not re-offered immediately ahead of unseen work. | task-15 | covered | "Skipping ends the current session and advances to the next item." | diff-local |
| criterion | Story 16 negative: Given the last unseen item is skipped and only skipped items remain, when the monitor continues, then it offers a deferred item again rather than reporting an empty queue. | task-15 | covered | "A skipped item is not re-offered ahead of unseen work, and is offered again once unseen work is exhausted." | diff-local |
| criterion | Story 17 happy: Given a guided session ends without resolving the halt, when membership is recomputed, then that halt is still in the queue with its state and evidence available. | task-16 | covered | "A session exiting zero with the halt marker still present leaves the halt in the queue." | diff-local |
| criterion | Story 17 happy: Given a halt is offered a second time after an unsuccessful session, when it is displayed, then it carries the same project, feature, reason, and classification. | task-16 | covered | "A session exiting zero with the halt marker still present leaves the halt in the queue." | diff-local |
| criterion | Story 17 negative: Given a guided session exits with a success status but the halt marker is still present, when membership is recomputed, then the halt is still offered, resolution being derived from the marker rather than from the exit status. | task-16 | covered | "A session exiting zero with the halt marker still present leaves the halt in the queue." | diff-local |
| criterion | Story 17 negative: Given a guided session claims in its output that the halt is resolved but the marker remains, when membership is recomputed, then the halt is still offered. | task-16 | covered | "A session exiting zero with the halt marker still present leaves the halt in the queue." | diff-local |
| criterion | Story 17 negative: Given a guided session ends, when the monitor records anything about it, then it records no resolution verdict of its own. | task-16 | covered | "A session exiting zero with the halt marker still present leaves the halt in the queue." | diff-local |
| criterion | Story 18 happy: Given the operator skipped an item and then restarted the monitor, when the queue is built, then that item is not re-offered ahead of unseen work. | task-15 | covered | "A skip survives a monitor restart and the item is still not re-offered ahead of unseen work." | diff-local |
| criterion | Story 18 happy: Given a deferral was recorded, when the deferral record is read, then it identifies the project, the feature, and the identity of the specific halt that was deferred. | task-4 | covered | "The deferral key is a structured type carrying project, feature, and halt identity as separate fields, as asserted by a round-trip test that reads the fields back individually." | diff-local |
| criterion | Story 18 happy: Given a deferred halt is resolved and the same feature halts again, when the queue is built, then the new halt is offered despite the earlier deferral. | task-4 | covered | "The deferral key is a structured type carrying project, feature, and halt identity as separate fields, as asserted by a round-trip test that reads the fields back individually." | diff-local |
| criterion | Story 18 negative: Given a stored deferral whose halt identity does not match the feature's current halt, when the queue is built, then the deferral does not apply and the item is offered. | task-5 | covered | "A deferral whose stored halt identity does not match the feature's current halt does not suppress the item, as asserted by the changed-identity test." | diff-local |
| criterion | Story 18 negative: Given a stored deferral whose halt identity cannot be established, when the queue is built, then the item is offered rather than suppressed. | task-5 | covered | "An unestablishable halt identity results in the item being offered, proving the fail direction is toward offering." | diff-local |
| criterion | Story 18 negative: Given the deferral record contains unparseable content, when the monitor runs, then the record is preserved by copy rather than rename, the monitor continues with no deferrals in effect, and every halt is offered. | task-5 | covered | "An unparseable deferral record is preserved by copy rather than rename, and the pass continues with every halt offered." | diff-local |
| criterion | Story 18 negative: Given the deferral record's directory is unwritable, when the operator skips an item, then the failure is reported, the item is not lost from the queue, and the monitor stays active. | task-5 | covered | "An unwritable deferral directory reports the failure, leaves the item in the queue, and keeps the monitor alive." | diff-local |
| criterion | Story 18 negative: Given the deferral record is absent, when the monitor runs, then it proceeds with no deferrals rather than treating absence as corruption. | task-5 | covered | "An absent deferral record yields no deferrals and no error, distinct from the unparseable case." | diff-local |
| criterion | Story 18 negative: Given a deferral is written from within a feature worktree, when its location is resolved, then it is written under the main checkout's daemon state directory rather than inside the worktree. | task-4 | covered | "A deferral written from inside a feature worktree resolves to the main checkout's daemon state directory, as asserted by a worktree-rooted write test." | diff-local |
| criterion | Story 19 happy: Given the monitor is running, when a monitoring cycle completes, then the existing halt-issue reconciliation is invoked. | task-19 | covered | "A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub." | diff-local |
| criterion | Story 19 happy: Given the reconciliation runs, when it completes, then its behavior is unchanged from invoking it directly. | task-19 | covered | "A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub." | diff-local |
| criterion | Story 19 negative: Given the reconciliation exits non-zero, when the cycle continues, then the failure is reported and the operator's queue continues to be offered. | task-19 | covered | "A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub." | diff-local |
| criterion | Story 19 negative: Given the reconciliation cannot reach the network, when the cycle continues, then the failure is reported once and the queue is unaffected. | task-19 | covered | "A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub." | diff-local |
| criterion | Story 19 negative: Given the reconciliation throws unexpectedly, when the cycle continues, then the monitor stays active and the next pass still builds a queue. | task-19 | covered | "A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub." | diff-local |
| criterion | Story 19 negative: Given the reconciliation is slow, when it is running, then the operator's queue work is not blocked waiting for it. | task-19 | covered | "A monitoring cycle invokes the existing reconciliation with its behavior unchanged, as asserted against an injected reconciliation stub." | diff-local |
| adr | adr-2026-09-20-halt-resolution-queue-derived-from-markers | story-5, story-6, story-9, story-16, story-18 | covered | D1 derived membership is delivered by task-6; D2 marker-as-source by task-1; D3 and D4 the deferral record and its identity key by task-4 and task-5; D5 ordering by task-7 and task-8; D6 spine transitions by task-18; D7 records no artifact, so nothing is built for it. Dispositions judged individually below. |
| adr | adr-2026-09-20-operator-launched-sessions-retain-conductor-authority | story-12, story-13, story-14, story-17 | covered | D1 and D2 are delivered by task-13 (no marker stamped, sanctioned set unchanged); D3 the provider-agnostic seam by task-9; D4 the foreground-only confinement by task-10; D5 the worktree working directory by task-13; D6 cold start and exit-is-not-resolution by task-11 and task-16; D7 the approval contract is inherited unchanged by task-13. Dispositions judged individually below. |
