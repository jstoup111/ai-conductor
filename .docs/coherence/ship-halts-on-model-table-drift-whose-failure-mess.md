# Coherence: Bounded mechanical remediation in the self-host release gate

Date: 2026-09-06
Tier: M
Track: technical — no PRD/FR row class.
Source: jstoup111/ai-conductor#658; three verbatim intake outcomes in .pipeline/intake-outcomes.md.
Verdict: covered, with one waived gap (outcome-2)

One table maps the three intake outcomes, the one amended ADR in the change set, all four stories, all thirteen tasks, and all thirty-three criteria. Criterion rows use six cells: row class, exact criterion, task ids, verdict, exact Done-when quote, diff locality. Other rows use the five-column header.

Consistency pass (§4d): every covered row was re-read against its counterpart in both directions. The one cross-layer pair that could oscillate — outcome-1 ("does not terminally halt") against story-3 ("still halts fail-closed") — does not: story-3 halts only the complement of the cases story-2 heals, and the amended ADR D1–D3 partitions the two exhaustively. No `fail` rows.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-2, story-3, story-4 | covered | The gate self-heals a declared, allowlisted mechanical failure (story-2), halts fail-closed on everything else (story-3), and reports the outcome on the spine (story-4); no terminal halt for regenerable drift. |
| outcome | outcome-2 |  | gap | outcome-2 — the issue's stated alternative (build-time prevention) is excluded by the operator-confirmed scope boundary; waived in .docs/coherence-waivers/ship-halts-on-model-table-drift-whose-failure-mess.md. |
| outcome | outcome-3 | story-2 | covered | The named 2026-07 feature has long since shipped; the outcome's durable content — a feature whose only integrity failure is model-table drift ships without an operator hand-running the generator — is story-2's first happy-path criterion. |
| adr | adr-2026-06-30-halt-based-release-gates | story-2, story-3 | covered | Amended 2026-09-06 (decisions 1–3). D1 → task-6/task-7: Done-when asserts one command run, one engine commit, exactly two exec calls, and a bound of one re-run — the decision's one-attempt semantics. D2 → task-3: Done-when asserts the exact two-entry readonly allowlist and rejects suffixed/prefixed variants — the decision's engine-owned exact-match rule. D3 → task-9: Done-when asserts the class passed to writeHaltMarker is needs-human on the declined paths — the decision's halt-class rule. Each mapping semantically satisfies its decision. |
| story | story-1 | task-1, task-2 | covered | Record emission and the two declared remediations; shell test asserts records, truncation, and unchanged two-argument behavior. |
| story | story-2 | task-5, task-6, task-7, task-8, task-12, task-13 | covered | Record channel, happy-path self-heal, the three bounded-lane negatives, failed command/commit halts, migration ordering, and the non-self-host guard. |
| story | story-3 | task-3, task-9 | covered | Pure allowlist decision and every declined path halting needs-human naming its cause. |
| story | story-4 | task-4, task-10, task-11 | covered | Event variants with persisted sinks, per-branch emission sequences, and one-line rendering. |
| task | task-1 | story-1 | covered | Infrastructure: assert third argument and record write; serves story-1's negative paths directly. |
| task | task-2 | story-1 | covered | Declares the two remediations; story-1 happy paths. |
| task | task-3 | story-3 | covered | Infrastructure: pure decision module; serves story-3's allowlist criteria and ADR D2. |
| task | task-4 | story-4 | covered | Infrastructure: event union and sinks; serves story-4 and the compile-time exhaustiveness negative. |
| task | task-5 | story-2 | covered | Infrastructure: record channel into the gate; prerequisite of every story-2 lane criterion. |
| task | task-6 | story-2 | covered | Happy-path self-heal through runReleaseArtifactGate — the integration-owning task for the SHIP-tail boundary. |
| task | task-7 | story-2 | covered | Bounded-lane negatives: failing re-run, no-op remediation, re-run timeout. |
| task | task-8 | story-2 | covered | Failed command or commit halts before any re-run. |
| task | task-9 | story-3 | covered | Declined lanes halt needs-human naming the cause; ADR D3. |
| task | task-10 | story-4 | covered | Per-branch event sequences; optional emitter. |
| task | task-11 | story-4 | covered | Renderer lines per variant. |
| task | task-12 | story-2 | covered | Migration sub-gate ordering after the remediation commit. |
| task | task-13 | story-2 | covered | Verify-only: existing self-host guard keeps non-self-host builds out of the lane. |
| criterion | Story 1 happy: Given HARNESS.md's generated model table drifts from `bin/generate-model-table` output, when `test/test_harness_integrity.sh` runs, then it exits non-zero and emits exactly one remediation record for check 5a naming the command `bin/generate-model-table` and marking it deterministic | task-2 | covered | `test/test_integrity_remediation_records.sh` asserts exactly one deterministic record naming `bin/generate-model-table` for the drifted-table fixture and exactly one naming `bin/generate-docs-guard-hook` for the drifted-hook fixture | diff-local |
| criterion | Story 1 happy: Given `hooks/claude/docs-guard.sh` drifts from `bin/generate-docs-guard-hook` output, when the suite runs, then it emits exactly one remediation record for that check naming `bin/generate-docs-guard-hook` and marking it deterministic | task-2 | covered | `test/test_integrity_remediation_records.sh` asserts exactly one deterministic record naming `bin/generate-model-table` for the drifted-table fixture and exactly one naming `bin/generate-docs-guard-hook` for the drifted-hook fixture | diff-local |
| criterion | Story 1 happy: Given every check passes, when the suite runs, then it exits 0 and emits zero remediation records | task-2 | covered | The clean fixture yields an empty record file and exit 0, and the fixture without `src/conductor/node_modules` yields no record for the model-table check | diff-local |
| criterion | Story 1 happy: Given an existing `assert` call that passes only a description and a result, when the suite runs, then that call's pass/fail behavior and human-readable output are byte-for-byte unchanged | task-1 | covered | `assert` accepts an optional third argument and every existing two-argument call site is unmodified in the diff | diff-local |
| criterion | Story 1 negative: Given a check with no declared remediation fails, when the suite runs, then it exits non-zero and emits a remediation record for that check that names no command and is marked not deterministic | task-1 | covered | `test/test_integrity_remediation_records.sh` passes and asserts the two-record file content, the stale-line truncation, the unwritable-path tolerance, and the unset-variable byte-identical output | diff-local |
| criterion | Story 1 negative: Given `src/conductor/node_modules` is absent so check 5a warn-skips, when the suite runs, then no remediation record is emitted for check 5a | task-2 | covered | The clean fixture yields an empty record file and exit 0, and the fixture without `src/conductor/node_modules` yields no record for the model-table check | diff-local |
| criterion | Story 1 negative: Given the remediation record stream is unwritable, when the suite runs, then the suite still reports its FAIL line and exits non-zero rather than aborting before the summary | task-1 | covered | `test/test_integrity_remediation_records.sh` passes and asserts the two-record file content, the stale-line truncation, the unwritable-path tolerance, and the unset-variable byte-identical output | diff-local |
| criterion | Story 1 negative: Given a remediation record from a previous run is present, when the suite runs, then only records from the current run are present afterward | task-1 | covered | `test/test_integrity_remediation_records.sh` passes and asserts the two-record file content, the stale-line truncation, the unwritable-path tolerance, and the unset-variable byte-identical output | diff-local |
| criterion | Story 2 happy: Given the suite fails with one deterministic record naming `bin/generate-model-table`, when the release gate runs, then it runs that command from the worktree root, commits the regenerated HARNESS.md as an engine commit, re-runs the suite exactly once, and returns ok when the re-run exits 0 | task-6 | covered | `release-gate.test.ts` asserts that for one allowlisted record `runReleaseArtifactGate` runs the command once from `harnessRoot`, commits once with `CONDUCT_ENGINE_COMMIT` set and a message naming the command and check, invokes `exec` exactly twice, and returns `ok: true` | diff-local |
| criterion | Story 2 happy: Given the suite fails with two deterministic records naming `bin/generate-model-table` and `bin/generate-docs-guard-hook`, when the gate runs, then both commands run, one commit captures both outputs, the suite re-runs once, and the gate returns ok on a passing re-run | task-6 | covered | The two-record case asserts two command runs, exactly one commit, exactly two `exec` invocations, and `ok: true` | diff-local |
| criterion | Story 2 happy: Given the gate self-healed, when the SHIP tail continues, then the migration sub-gate is evaluated against the changed-file set that includes the remediation commit | task-12 | covered | `release-gate.test.ts` asserts `changedFiles` is invoked after the remediation commit and that a remediation touching `hooks/claude/docs-guard.sh` without a migration block returns the existing hook-wiring migration HALT reason | diff-local |
| criterion | Story 2 happy: Given the gate self-healed, when the operator inspects the branch, then the remediation commit's message names the commands run and the checks they remediated | task-6 | covered | `release-gate.test.ts` asserts that for one allowlisted record `runReleaseArtifactGate` runs the command once from `harnessRoot`, commits once with `CONDUCT_ENGINE_COMMIT` set and a message naming the command and check, invokes `exec` exactly twice, and returns `ok: true` | diff-local |
| criterion | Story 2 negative: Given the suite fails with a deterministic record and the re-run still fails, when the gate runs, then it does not run any command a second time and halts naming the check that still fails after remediation | task-7 | covered | Case (a) asserts exactly one `runCommand` and exactly two `exec` calls and a halt reason naming the still-failing check | diff-local |
| criterion | Story 2 negative: Given the remediation command exits non-zero, when the gate runs, then the suite is not re-run and the gate halts naming the command and its exit code | task-8 | covered | Case (a) asserts exactly one `exec` call, zero `git` calls, and a reason containing both the command and its exit code | diff-local |
| criterion | Story 2 negative: Given the remediation command succeeds but the engine commit fails, when the gate runs, then the suite is not re-run and the gate halts naming the commit failure | task-8 | covered | Case (b) asserts exactly one `exec` call and a reason containing the git failure message | diff-local |
| criterion | Story 2 negative: Given the remediation command succeeds but produces no working-tree change, when the gate runs, then no commit is made, the suite re-runs exactly once, and a still-failing re-run halts naming the check as unremediated | task-7 | covered | Case (b) asserts no `git commit` argv was issued and the suite still re-ran exactly once | diff-local |
| criterion | Story 2 negative: Given the suite times out on the re-run, when the gate runs, then the gate halts with the existing timeout reason and does not attempt a further remediation | task-7 | covered | Case (c) asserts the timeout reason is returned verbatim from the existing `runIntegritySuite` text and no third `exec` call occurs | diff-local |
| criterion | Story 2 negative: Given the gate is invoked for a non-self-host build, when the release gate would run, then no remediation record is read and no command runs | task-13 | covered | `wiring.test.ts` asserts that with `isSelfHost: false` the release gate's `exec`, `runCommand`, and `git` fakes are never invoked and no remediation record is read | diff-local |
| criterion | Story 3 happy: Given the suite fails and at least one failed check has no deterministic remediation record, when the gate runs, then no command runs and the gate halts naming every undeclared failing check | task-9 | covered | Cases (a)–(c) assert zero `runCommand` calls, exactly one `exec` call, and a halt reason naming the undeclared check, the rejected command verbatim, or the malformed line respectively | diff-local |
| criterion | Story 3 happy: Given the suite fails and a deterministic record names a command not on the allowlist, when the gate runs, then no command runs and the gate halts naming the rejected command verbatim | task-9 | covered | Cases (a)–(c) assert zero `runCommand` calls, exactly one `exec` call, and a halt reason naming the undeclared check, the rejected command verbatim, or the malformed line respectively | diff-local |
| criterion | Story 3 happy: Given the gate halts on any self-heal path, when the halt marker is written, then its class is `needs-human` | task-9 | covered | Case (e) asserts the class argument passed to `writeHaltMarker` is `needs-human` | diff-local |
| criterion | Story 3 happy: Given a remediation record cannot be fully parsed, when the gate runs, then the record is treated as undeclared and the gate halts naming the malformed record | task-9 | covered | Cases (a)–(c) assert zero `runCommand` calls, exactly one `exec` call, and a halt reason naming the undeclared check, the rejected command verbatim, or the malformed line respectively | diff-local |
| criterion | Story 3 negative: Given a deterministic record names `bin/generate-model-table --force`, when the gate runs, then the command is rejected as not allowlisted because matching is exact-string, not prefix | task-3 | covered | `integrity-remediation.test.ts` passes and asserts declined verdicts for an undeclared check, for `bin/generate-model-table --force`, for `../bin/generate-model-table`, and for a malformed record, each naming the offending check, command, or line | diff-local |
| criterion | Story 3 negative: Given a deterministic record names a command whose path is `../bin/generate-model-table`, when the gate runs, then the command is rejected as not allowlisted | task-3 | covered | `integrity-remediation.test.ts` passes and asserts declined verdicts for an undeclared check, for `bin/generate-model-table --force`, for `../bin/generate-model-table`, and for a malformed record, each naming the offending check, command, or line | diff-local |
| criterion | Story 3 negative: Given the halt marker write fails after a declined self-heal, when the gate runs, then the returned verdict is not ok and its reason includes the marker-write failure, matching the existing gate behavior | task-9 | covered | Case (d) asserts the returned reason contains `HALT marker write failed:` followed by the writer's reason | diff-local |
| criterion | Story 3 negative: Given the gate halted after a failed self-heal, when its halt marker is read, then the marker class is `needs-human`, the class the daemon's re-kick sweep retains rather than clears | task-9 | covered | Case (e) asserts the class argument passed to `writeHaltMarker` is `needs-human` | diff-local |
| criterion | Story 4 happy: Given the gate self-heals successfully, when the gate runs, then the spine carries an attempted event naming the commands and checks followed by a succeeded event, both persisted to `.pipeline/events.jsonl` | task-10 | covered | `release-gate.test.ts` asserts the exact event type sequence for the success, failed-re-run, undeclared, and not-allowlisted branches | diff-local |
| criterion | Story 4 happy: Given the gate declines because a check declared no remediation, when the gate runs, then the spine carries a declined event whose reason names the undeclared checks | task-10 | covered | `release-gate.test.ts` asserts the exact event type sequence for the success, failed-re-run, undeclared, and not-allowlisted branches | diff-local |
| criterion | Story 4 happy: Given the gate declines because a command is not allowlisted, when the gate runs, then the spine carries a declined event whose reason names the rejected command | task-10 | covered | `release-gate.test.ts` asserts the exact event type sequence for the success, failed-re-run, undeclared, and not-allowlisted branches | diff-local |
| criterion | Story 4 happy: Given the remediation ran but the re-run failed, when the gate runs, then the spine carries an attempted event followed by a failed event naming the still-failing check | task-10 | covered | `release-gate.test.ts` asserts the exact event type sequence for the success, failed-re-run, undeclared, and not-allowlisted branches | diff-local |
| criterion | Story 4 negative: Given the event emitter is absent from the gate options, when the gate self-heals, then the gate still completes its verdict and no error is thrown | task-10 | covered | The omitted-emitter case asserts `ok: true` with no thrown error | diff-local |
| criterion | Story 4 negative: Given a self-heal event type is added without an `EVENT_SINKS` declaration, when the conductor package compiles, then compilation fails | task-4 | covered | The package typecheck passes, and removing any one of the four `EVENT_SINKS` entries makes it fail (the registry type is `Record<ConductorEvent['type'], SinkDeclaration>`) | diff-local |
| criterion | Story 4 negative: Given the terminal renderer receives a self-heal event, when it renders, then the line identifies the gate and outcome without printing the full remediation record body | task-11 | covered | The renderer test asserts one line per variant containing `release gate` and the outcome word | diff-local |
