# Retro: Phase 9.3 — Supervisor / Brain Mode
**Date:** 2026-06-25 | **Stats:** 34 tasks, 3 rework cycles (batches 8, 9 + 1 wiring fix), 0 user interventions, 301/301 brain-suite tests passing (22/22 acceptance = all 7 PRD scenarios)

## Part A: Harness

- **H-1:** Untested negative-path correctness gap escaped every intermediate gate — the no-remote authoring path skipped `recordAuthoredKey`, leaving a brain-planned feature invisible to the FR-12 flywheel trend (ADR-006). `engine/brain/loop.ts` no-remote else-branch. Severity: IMPORTANT (caught only by the final Opus evaluator; resolved f1d839d). This is the *same* class of escape flagged in 3/3 prior Phase-9 features. Gate that should have caught it: `/stories` + `/writing-system-tests` — no acceptance scenario asserted "no-remote authoring → ledger contains the key" (Scenario 4.4 checks only branch + message; Scenario 7 seeds the ledger directly).
- **H-2:** Parallel-commit race recurred across batches 6–9 — a commit carries task-N's message but task-M's file content (task-24→task-19 `3888717`; task-33/task-20 `a56a89a`/`6ae2b7d`). Content stayed isolated/correct; messages are cosmetically wrong. Where: pipeline parallel-dispatch within a batch sharing one working tree. Severity: MINOR (cosmetic, but pollutes `git blame`/audit trail). Recurs every multi-task batch.
- **H-3:** A stub-backed sibling test silently flipped to exercising the real implementation the moment its module landed — task-32's `cli-brain` wiring test hung on `process.stdin` (5s timeout) once `loop.ts` provided the real `runBrainMode`. Where: `test/engine/brain-cli.test.ts`. Severity: MINOR (caught same-session, fixed d948fab via io-injectable `dispatchBrain`). The breakage was in a *previously-green sibling test*, not the task under change.

**Proposed changes:**
- [ ] H-1: Add a `stories`/`writing-system-tests` rule — when a happy path delegates a side effect to a helper (ledger write, cleanup, metric), every *alternate* branch that bypasses the helper needs its own falsifiable negative-path scenario asserting the side effect, not just the happy output. Add to `skills/stories/SKILL.md` negative-path categories as "Invariant side-effect on alternate branches." (Persisted: `.memory/gotchas/no-remote-path-must-record-ledger.md`.)
- [ ] H-2: `skills/pipeline/SKILL.md` — when dispatching parallel tasks in a shared tree, have each subagent `git add` only its explicit paths AND commit before the next agent's COMMIT phase races it; or serialize the COMMIT step within a batch while keeping RED/GREEN parallel. (Persisted as a pattern below.)
- [ ] H-3: `skills/pipeline/SKILL.md` batch-boundary checklist — after any new entry-point/module lands, re-run sibling tests that previously stubbed that module; a passing stub test can flip to the real impl without any edit to itself.

## Part B: Application

- **A-1:** `engine/brain/loop.ts` `createFn` ignores the injected `name` param and re-derives it (cosmetic; injected value and derivation always agree today, but the dead param invites future drift). Severity: MINOR. Fix: use the injected `name` or drop the param.
- **A-2:** `engine/brain/loop.ts` uses a non-null assertion `deps.gh!` on the remote path. Contractually safe (remote path only reached when `gh` is present) but the assertion hides the invariant from the type. Severity: MINOR. Fix: narrow via an explicit guard or make the remote branch take a `gh`-present sub-type.
- **A-3:** `engine/brain/rates.ts` double-casts `(kb as any).count` for readability shortcut. Severity: MINOR. Fix: type the kickback record's `count` field instead of casting at the call site.

**Proposed changes:**
- [ ] A-1/A-2/A-3: Bundle the three MINORs into one cleanup story `.docs/stories/phase-9.3-brain-cleanup.md` (loop.ts dead param + gh non-null + rates cast). Non-blocking follow-up; run `conflict-check` when picked up.

## Part C: Context Efficiency

- **C-1:** All four intermediate-batch evaluators ran on Sonnet and produced correct verdicts (batch 8 REQUEST_CHANGES with 2 real IMPORTANTs; others APPROVE) — consistent with the prior Medium-run finding that intermediates need not be Opus. The final Opus evaluator earned its keep: it alone caught H-1. Impact: model scaling was correct for this run; no downgrade available without losing the H-1 catch. Proposed change: none — record as confirmation that "Sonnet intermediate / Opus final" is calibrated for L-tier.
- **C-2:** The final batch required reconciling `governor`/`flywheel-trend`/`lesson-store`/`rates` *to* the acceptance contract — the source had drifted from the interface the acceptance suite assumed (wrapped-vs-bare routing JSON; provider stub writing no files). Impact: rework concentrated in the final batch instead of being surfaced when those modules were first built. Proposed change: `skills/writing-system-tests/SKILL.md` — when acceptance specs encode an adapter seam (provider/IO shape), generate a thin contract test at the seam in the *building* batch, not only the integration batch, so drift fails early.
- **C-3:** Authoring artifacts for the acceptance flow are materialized by the loop's adapter because the provider stub writes no files — correct, but the discovery cost a debugging cycle. Proposed change: document the "stub provider writes nothing → caller materializes" contract in `src/conductor/README.md` brain section (done) and in the loop's authoringProvider doc-comment so the next author doesn't re-derive it.

**Proposed changes:**
- [ ] C-2: Add seam-contract-test guidance to `skills/writing-system-tests/SKILL.md`.
- [ ] C-3: Add a doc-comment at `engine/brain/loop.ts` authoringProvider adapter noting the stub-writes-nothing contract.

## Trends
- Negative-path correctness escape (H-1) recurs for the **4th consecutive Phase-9 feature** — the final evaluator keeps catching what `/stories` should specify. Calibration target: the fix must move upstream into `/stories`, or the pattern will repeat in 9.4.
- Parallel-commit message/content race (H-2) recurs every multi-task batch since Phase 9.1 — cosmetic but persistent; warrants the serialize-COMMIT fix rather than per-run tolerance.
- User interventions: 0 (down from prior phases) — autonomy holding; no skill-gap interventions this run.
