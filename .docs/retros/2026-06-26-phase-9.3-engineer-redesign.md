# Retro: Phase 9.3 Engineer Redesign (agent-hosted, human-gated DECIDE loop)

**Date:** 2026-06-26 | **Stats:** 39 tasks, 11 batches, 1 final-batch rework cycle, 0 user interventions during build, 1599/1599 tests passing | **Diff:** 133 files, +19,979/−1,057 vs `main`

## Part A: Harness

- **H-1 (NEW class — orphaned primitives):** The C1/C2 safety primitives (`runAuthoring` gated DECIDE seam + `AuthoringGuard`) were fully implemented with thorough, *passing* acceptance tests — but the live path (`runEngineerMode → processIdea`) still called the OLD ungated `authorSpec`. The new functions had **zero production callers**; the FR-6/C2 acceptance specs invoked `runAuthoring` *directly* (unit-style), so they were green against an orphaned function while the shipping engineer violated C1/C2. Caught only by the final-batch Opus evaluator (rework-0 BLOCKING). Where: `engine/engineer/loop.ts` `processIdea` vs `engine/engineer/authoring.ts`. **Severity: High.** Gate that should have caught it: `/writing-system-tests` — no acceptance spec drove the *real* entry point (`runEngineerMode`) end-to-end; all C2 coverage went through the new unit seam. **Fix:** (a) `skills/writing-system-tests/SKILL.md` — when a task *replaces* an existing behavior, require ≥1 acceptance test that drives the production entry point (not the new unit under test) and asserts the observable artifact; (b) `skills/pipeline/SKILL.md` batch gate — when a task's plan says "replace/supersede X", grep that the superseded symbol has zero non-test callers in `src/` before marking the task complete.

- **H-2 (RECURRING — negative-path/boundary escape, now ~5 consecutive Phase-9 features):** `AuthoringGuard` constructor did not strip a trailing slash despite its docstring claiming it did → a trailing-slash `canonicalPath` would fail-closed and reject **all** legitimate writes. No direct boundary-value unit test existed until the fix (`c3f4943`); caught by Opus, not the suite. Where: `engine/engineer/authoring-guard.ts` constructor. **Severity: Medium.** Same escape class flagged in 9.0/9.1/9.2/9.3-supervisor retros (correctness derivation caught only by the fresh-context final evaluator). **Fix:** the H-1(a) entry-point rule plus the standing negative-path-bullet→spec gate already proposed in prior retros — escalate: add an explicit boundary-value row (trailing slash, root, empty) to the path-guard test checklist in `skills/writing-system-tests/SKILL.md`.

- **H-3 (tooling — rebase corruption recurred mid-build, then root-fixed):** A 2nd git-rebase corruption hit during Batch 10 and was root-fixed by daemon-gating `runRebaseStep` (`b08f534`) so it no-ops when `!daemon`. Where: `engine/daemon` rebase step running against the real worktree. **Severity: Medium (resolved).** Already persisted to `.memory` ([[project_rebase_step_daemon_gated]], [[feedback_no_adhoc_rebase]]). **Fix:** none outstanding — recorded as confirmation the daemon-gate fix holds; rebase tests must set `daemon:true` + isolated repo.

**Proposed changes:**
- [ ] H-1: `skills/writing-system-tests/SKILL.md` — add "replacement tasks need a real-entry-point acceptance test" rule; `skills/pipeline/SKILL.md` — add zero-superseded-caller grep to the batch completion gate.
- [ ] H-2: `skills/writing-system-tests/SKILL.md` — add boundary-value checklist row for path/prefix guards (trailing slash, root, empty, sibling-prefix).
- [ ] H-3: No change — verify rebase tests carry `daemon:true`.

## Part B: Application

- **A-1:** `test/engine/engineer/routing.test.ts` (~lines 516/643/666) — three assertions are truthy-only / precondition-light (`expect(combined).toMatch(/create|no.*match|no.*project/i)` asserts an offer was printed but not that the proposed repo was left untouched). **Severity: MINOR.** Open (logged in `summary.json` openMinorFindings). Fix → new story: tighten each to assert the specific repo-untouched / registry-unchanged invariant.
- **A-2:** `engine/engineer/loop.ts` is 465 LOC and `processIdea` orchestrates the full route→gate→author→PR→ensure-running chain inline — the largest hand-written engineer module and a god-function risk as 9.3b adds intake adapters. **Severity: MINOR (watch).** Fix → new story: extract the post-authoring handoff (PR open + ensure-running + record) into a named step function before 9.3b lands more branches.
- **A-3:** `engine/engineer/loop.ts:421` retains a `gh!` non-null assertion on the remote path (carry-over from prior 9.3 retro A-2). Contractually safe but hides the invariant from the type. **Severity: MINOR.** Fix → fold into the A-1 cleanup story: narrow via explicit guard or a `gh`-present sub-type.

**Proposed changes:**
- [ ] A-1/A-3: New story — "Engineer test+type hardening: routing repo-untouched assertions + remove `gh!` assertion" → `.docs/stories/`, run `/conflict-check`.
- [ ] A-2: New story — "Extract engineer post-authoring handoff step from `processIdea`" (pre-req for 9.3b intake adapters).

## Part C: Context Efficiency

- **C-1 (confirmation — model scaling correct):** Sonnet intermediate / Opus final remained calibrated: the single Opus final-batch pass alone caught both H-1 (BLOCKING) and H-2 (IMPORTANT); every Sonnet intermediate produced correct verdicts. No safe downgrade available without losing the H-1/H-2 catch. **Impact: none — record as calibrated for L-tier.**
- **C-2 (cheap mechanical pre-check):** H-1's orphaned-primitive class is detectable by a **static grep** (zero non-test callers of the superseded symbol) — far cheaper than an Opus reasoning pass spending a full BLOCKING rework cycle. **Proposed change:** add the zero-superseded-caller grep from H-1 to `skills/pipeline/SKILL.md` as a pre-evaluator mechanical gate so this class fails the batch *before* the (expensive) evaluator dispatch.
- **C-3 (recovery overhead):** 2 git-rebase-corruption recovery cycles in Batch 10 burned re-dispatches before the root daemon-gate fix landed. **Proposed change:** none new — the root fix (`b08f534`) plus the existing `.memory` gotchas prevent recurrence; noted so the dispatch count isn't read as plan churn.

**Proposed changes:**
- [ ] C-2: `skills/pipeline/SKILL.md` — mechanical "superseded symbol has zero `src/` callers" grep as a pre-evaluator batch gate for replacement tasks.

## Trends

- **Negative-path / correctness-derivation escape caught only by the final fresh-context evaluator: recurs every Phase-9 feature (9.0→9.3-redesign).** H-1 (orphaned wiring) and H-2 (boundary fail-closed) are this run's instances. The standing fix (negative-path-bullet→spec + final Opus evaluator) holds the line but does not *prevent* the escape; H-1 adds a new mechanical guard (real-entry-point test + zero-caller grep) that could catch it one layer earlier.
- **Intervention count: 0 during build** (down/flat vs prior Phase-9 runs) — the human gate was correctly reserved for VERSION + push approval only.
- **Rebase corruption: contained.** Recurred once more this run but root-fixed (`b08f534`); no longer expected to recur.
