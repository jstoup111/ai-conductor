# Architecture: Phase 9.1 — Retro-Signal Emission Path

**Last updated:** 2026-06-25
**Scope:** The daemon's feature-completion emission path into the brain store. Additive to the
existing daemon; no new loop instrumentation. Consumed by `/architecture-review`.
**Source:** PRD/stories `2026-06-25-phase-9.1-retro-signal-brain-memory.md`

---

## Component view — emission on daemon completion

```mermaid
graph TD
    subgraph daemon["daemon (existing)"]
        runFeature["makeRunFeature()<br/>daemon-runner.ts"]
        outcome["readOutcome → FeatureOutcome<br/>(done | halted | error)"]
        teardown["teardownWorktree(keep?)"]
    end

    subgraph sources["signal sources (existing, from 9.0)"]
        events["feature events.jsonl<br/>(kickback, loop_halt, step_completed+tokens,<br/>retries, rebase_*)"]
        report["report-renderer.ts<br/>(durations / retries / token spend)"]
    end

    subgraph emit["EMISSION (new, FR-1..FR-11)"]
        assemble["assemble signal record<br/>(FR-3/FR-4)"]
        narrate["narrative producer<br/>done: full retro · halted: short halt (FR-5/6)"]
        store["brain-store writer<br/>atomic append + narratives (FR-2/8/10/11)"]
    end

    subgraph brain["brain store (new) — ~/.ai-conductor/brain/ (FR-2)"]
        jsonl["signals.jsonl<br/>(append-only, one line/feature-run)"]
        narr["narratives/&lt;project&gt;/&lt;feature&gt;-&lt;runId&gt;.md"]
    end

    runFeature --> outcome
    outcome -->|"daemon only (FR-1)"| assemble
    events --> assemble
    report --> assemble
    outcome --> narrate
    assemble --> store
    narrate --> store
    store -->|append one JSON line| jsonl
    store -->|write narrative| narr
    store -.->|narrativeRef| jsonl
    store -->|then| teardown
    store -.->|"write fail → log + swallow (FR-10)"| teardown
```

---

## Sequence — done / halted / retro-skipped

```mermaid
sequenceDiagram
    participant R as makeRunFeature
    participant O as FeatureOutcome
    participant A as signal assembler
    participant N as narrative producer
    participant S as brain store
    participant T as teardown

    R->>O: readOutcome (after build/PR or HALT)
    Note over R,O: manual /conduct run → SKIP emission entirely (FR-1)
    R->>A: assemble from events.jsonl + report + outcome (FR-4)
    alt outcome = done, retro ran
        R->>N: full retro narrative
        N->>S: write narratives/<proj>/<feat>-<runId>.md
    else outcome = done, tier skipped retro (ST-005)
        Note over N,S: no narrative — narrativeRef absent (FR-3/5)
    else outcome = halted
        R->>N: SHORT halt narrative (why it halted) (FR-6)
        N->>S: write halt narrative
    end
    A->>S: append one signal line (atomic) (FR-3/11)
    S-->>R: ok (or log+swallow on failure, FR-10)
    R->>T: teardownWorktree (keep for halted)
```

> Emission runs **after `readOutcome`, before `teardownWorktree`**, so the worktree context
> (diff, events, stories) is still available to the narrative producer.

---

## Decision surface for architecture-review (the retro-redirect mechanism)

The structured signal is emission-owned regardless. The open decision is how the **narrative** is
produced for daemon runs without landing in the repo:

- **Option A — skip the in-loop retro step for daemon runs; emission produces the narrative.**
  The gate loop omits `retro` under the daemon; the emission step generates done/halted narratives
  straight into the store.
- **Option B — keep the in-loop retro step; redirect its output to the store.** The `retro` step,
  when under the daemon, writes to the brain store instead of `.docs/retros/`.

```mermaid
graph LR
    A["A: skip in-loop retro, emit narrative"] -->|pros| A1["one place owns narrative;<br/>no repo write to undo;<br/>tier-skip & halted uniform"]
    A -->|cons| A2["narrative generation moves to<br/>emission (a Claude call there)"]
    B["B: redirect in-loop retro output"] -->|pros| B1["reuses existing retro step"]
    B -->|cons| B2["couples retro step to brain-store +<br/>daemon-awareness; signal still<br/>assembled separately → split"]
```

## Legend
- **Solid** = data/control flow; **dotted** = reference / best-effort / failure path.
- **Existing** components unchanged; **EMISSION** + **brain store** are the new 9.1 surface.

## Change Log
| Date | Change | Reason |
|------|--------|--------|
| 2026-06-25 | Initial emission-path component + sequence diagrams | Phase 9.1 architecture input |
