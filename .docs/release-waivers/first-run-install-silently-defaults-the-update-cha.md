Waives: skill symlink targets

Rationale: The release classifier flags every bin/install change as a skill-symlink surface, but this additive first-run channel option changes no skill symlink target.
