# Track: Operator park — a human-placed park must survive the base-advance re-kick sweep

Track: product

Operator-facing capability (park/unpark verbs, PARKED dashboard state, "nothing re-dispatches a
park" contract) with requirements worth enumerating as FRs, matching the ADR-013 re-kick lineage.
