# Track: bin/conduct unknown-subcommand guard

Track: technical

Internal CLI arg-parsing guard (fail loudly instead of silently launching the pipeline); no user-facing product requirements — acceptance criteria live in stories. Source: jstoup111/ai-conductor#178.
