# Track: Daemon log feature tags

Track: technical

This is an internal observability change: every line emitted while a daemon feature is active gains a bounded feature tag, without changing user-facing product behavior.
