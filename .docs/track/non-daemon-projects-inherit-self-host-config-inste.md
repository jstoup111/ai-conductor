# Track: Non-daemon projects inherit self-host config instead of sane defaults (#683)

Track: technical

Internal onboarding tooling and documentation correction: add a deterministic project-scoped
config scaffolder, retire the manual "copy the template out of the harness checkout" step, and
reconcile the docs/ADR claims with the code. No user-facing product requirements to enumerate —
acceptance criteria live directly in the stories.
