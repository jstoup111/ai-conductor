# Track: prd_audit passes on a partial report

Track: technical

Internal engine gate correctness — the `prd_audit` completion predicate records a pass on an
audit report that never covered every functional requirement. No user-facing capability or
product requirement is involved, so acceptance criteria live directly in the stories rather
than in a PRD.
