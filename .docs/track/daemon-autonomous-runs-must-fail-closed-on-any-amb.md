# Track: Daemon autonomous runs fail closed on ambiguous DECIDE entry

Track: technical

Engine/daemon safety policy for the conductor's step-navigation seams (#550). No user-facing
product capability — the deliverable is a fail-closed routing invariant plus a HALT payload
contract, so acceptance criteria live directly in stories and no PRD is authored.
