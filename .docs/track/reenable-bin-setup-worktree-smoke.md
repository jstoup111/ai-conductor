# Track: re-enable bin/setup worktree smoke via worktree-local invocation (#334)

Track: technical

Build/test infrastructure fix — re-enables a skipped smoke test by invoking the
worktree's own `bin/setup`; no user-facing behavior, no product requirements.
