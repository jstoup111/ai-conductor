# Track: BUILD post-task tail telemetry

Track: technical

Internal instrumentation of the `build` step's interior: tick provenance on
`build_progress`, deterministic closeout-artifact observation, a tail rollup, and a
committed baseline. No user-facing product capability and no functional requirements
worth a PRD — acceptance criteria live directly in the stories.
