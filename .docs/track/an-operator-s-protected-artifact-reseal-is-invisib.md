# Track: Operator reseal visible to build_review's Scope rubric

Track: technical

Internal harness machinery only — engine input assembly (`build-review-inputs.ts`) and
grader prompt assembly (`build-review-prompt.ts`). No user-facing product surface and no
new operator command; acceptance criteria live directly in the stories.
