# Track: Runnable example scripts for every conduct-ts flow

Track: technical

Internal developer tooling — a set of runnable `examples/` scripts that demonstrate each
`conduct-ts` execution flow at S/M/L prompt tiers. No user-facing product behavior and no
product requirements to spec; acceptance criteria live directly in the stories. The eval /
regression-runner layer is split out to intake #807 and is out of scope here.
