# Track: daemon issue-priority scheduling

Track: product

Operator-facing scheduling behavior (which merged spec the daemon builds next), driven by
priority labels on the linked GitHub issue — enumerable requirements worth a PRD.
