# Track: Acceptance specs hide missing RED evidence and completion waits (#1246)

Track: technical

Engine-internal observability and gate evidence — the `acceptance_specs` RED lifecycle on the
event spine, the RED marker's own recorded fields, and the `daemon status` progress rendering.
No user-facing product surface, so acceptance criteria live directly in stories and no PRD is
authored.
