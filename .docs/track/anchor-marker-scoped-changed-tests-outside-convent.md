# Track: Anchor marker-scoped changed tests outside conventional test paths

Track: technical

Scope boundary: Small fix for #2165, approved by the operator on 2026-09-06 (delegated). Make the changed-test title snapshot cover every selector the marker-derived test-quality scope already admits, so a finding on an in-scope file is representable no matter where the file lives. The Covers-marker resolution rules, the coordinator's in-scope filter, the anchor grammar, the tautology preflight, and the rubric's own judgement are all unchanged and outside this slice.

This is an internal correction to the grader's input assembly. There is no new flag, configuration key, step, gate, hook, or CLI surface, and no reader-visible behavior to document; acceptance criteria therefore live in technical stories rather than a PRD.

The operator-delegated decision on 2026-09-06 was to widen the title-extraction candidate set to the union of the path-classified changed tests and the marker-derived in-scope selectors, rather than replacing the path classification outright. The union keeps every existing path-classified extraction intact while making marker-scoped files anchorable, and the coordinator's existing in-scope filter still decides what the grader actually sees, so review scope cannot widen.

Scope check: A — consumer-facing (this is shipped engine behavior; a repository with no daemon and no self-host history still runs build_review through it), and no repo-only signal fires. The change is code only: it narrows or replaces no shared convention, so no rule text belongs in either instruction file. B — not applicable, no new skill. C — provider-agnostic: the change is in input assembly, touches no provider adapter, prompt, or model routing.

Verified foundation: `src/conductor/src/engine/build-review-inputs.ts` line 501 builds the changed-test title selector set from `classifyTautologyPaths(changedPathsFromDiff(diff)).tests`, a path-pattern classifier defined at `src/conductor/src/engine/build-review-test-quality-preflight.ts` line 236. Test-quality scope membership is computed separately by `snapshotTestQualityScope` at `build-review-inputs.ts` line 279, which deliberately does not path-filter and admits any changed file carrying a resolvable `Covers:` marker. `src/conductor/src/engine/build-review-coordinator.ts` line 337 then intersects the snapshot titles with that marker-derived set, and `buildReviewFindingReferenceContext` in `src/conductor/src/engine/build-review-domain.ts` line 63 turns the surviving titles into `changedTestRegions`. An empty region array is truthy, so `parseBuildReviewFindingAnchor` on the next line rejects every anchor when no title survived. The existing real-git fixture in `src/conductor/test/engine/build-review-inputs.test.ts` (the `projects only changed tests whose Covers markers bind` case) already resolves `src/technical-coverage.ts` into `inScopeTests` while the path classifier excludes it, so the defect is demonstrable in tree today. The skill contract in `skills/build-review-test-quality/SKILL.md` lines 20-21 already promises that in-scope changed tests are "represented as immutable content-region references", so no skill text changes.
