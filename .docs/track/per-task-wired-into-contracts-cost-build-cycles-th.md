# Track: remove the BUILD wiring_check gate

Track: technical

Gate teardown and internal tooling removal — no user-facing product requirements
to spec; acceptance criteria live directly in the stories. Consumer-facing in
surface (engine step + settings.json step config), so it carries a migration
block, but it has no PRD.
