# Track: Off-tag checkout reports up to date forever (#1437)

Track: technical

Self-update tooling fix in `bin/update` / `bin/conduct` / `bin/install` — no product
requirements worth a PRD; acceptance criteria live directly in the stories. Consistent
with sibling issue #1400's landed plan, which covers the same files on the same track.
