# Track: Tautology FAILs are unfixable when planned behavior already exists (#1579)

Track: technical

Scope boundary: Comprehensive — a fourth closed Tautology exception ("verify-only maintenance")
anchored to engine-parsed `**Verify-only:**` / `**Type:** verification` plan-task markers, PLUS the
maker-side authoring reform absorbed from #1529 (tdd/writing-system-tests boundary: when legitimate
RED cannot be established because the behavior already exists, mark the task verify-only or delete
the redundant test — never invent unrelated assertions). Excludes: engine-run pre-diff-pass evidence
(Approach B) — deferred to a follow-up intake issue filed after this spec's handoff.

Harness-internal review/authoring machinery with no user-facing product surface — same track as the
#1521 removal-maintenance sibling.
