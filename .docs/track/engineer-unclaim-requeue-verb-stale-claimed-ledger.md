# Track: engineer unclaim/requeue verb — stale `claimed` ledger entries starve intake

Track: product

Operator-facing engineer/intake capability with enumerable functional requirements
(claim-time auto-heal reap rule, staleness threshold, manual `unclaim`/`requeue` verb
contract, bulk liveness/forget rule, FIFO preservation). Mirrors repo precedent for
engineer/intake features (ledger FR-33/34; build-auth spec #785 is product track despite
being harness-internal daemon tooling). Operator-confirmed 2026-07-22.
