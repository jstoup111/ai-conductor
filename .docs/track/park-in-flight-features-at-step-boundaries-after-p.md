# Track: Park in-flight features at step boundaries

Track: product

This changes operator-visible daemon park semantics; the approved direction is a daemon-only conductor boundary guard that preserves the active step's natural persisted outcome before stopping progression.
