# Track: Surface owner-gated (un-owned) specs in the daemon dashboard and status output

Track: product

Operator-facing visibility feature (dashboard bucket, `daemon status` section, warn-once
GitHub write-back) with enumerable functional requirements — warrants a PRD.
