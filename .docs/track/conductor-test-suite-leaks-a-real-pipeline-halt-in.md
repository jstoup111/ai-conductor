# Track: conductor test suite leaks a real .pipeline/HALT into the process cwd

Track: technical

Test isolation / engine hardening (un-injected cwd default in the Conductor constructor); no user-facing behavior — acceptance criteria live in stories, no PRD.
