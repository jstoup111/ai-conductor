# Track: CI test-suite workflow

Track: technical

Infra/CI tooling — a PR-triggered GitHub Actions workflow that runs the existing
harness-integrity script and the conductor vitest suite. No user-facing behavior
and no product requirements, so acceptance criteria live directly in stories (no PRD).
