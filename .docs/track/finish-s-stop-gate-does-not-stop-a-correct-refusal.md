# Track: finish's STOP gate does not stop — a correct refusal is retried as an ordinary step failure

Track: technical

Engine machinery (`finish-publication.ts` disposition schema + halt-reason rendering) and the
`skills/finish/SKILL.md` provider verdict contract. The change is operator-diagnostic and internal:
no product-facing capability, so no PRD — acceptance criteria live directly in the stories.
