# Track: Report a failing integrity check instead of aborting the suite silently

Track: technical

Scope boundary: Small fix for #2160, approved by the operator on 2026-09-06 (delegated). Make the harness integrity suite report a failing check and run to its summary, and make any abort it cannot guard name itself. Raising, removing, or reordering checks, changing what any existing check asserts, and reworking the suite's file enumeration are outside this slice.

This is one of this repository's own validation gates, so acceptance criteria live in technical stories rather than a PRD.

The operator approved, on 2026-09-06 (delegated), guarding every status-passing call site plus a named abort diagnostic, over the two alternatives weighed: dropping `set -e` from the suite (rejected — it would silence every genuinely fatal error the option currently catches, and the suite has 21 regions that already toggle errexit deliberately), and reporting only through a trap without guarding the call sites (rejected — a trap names a line number, but the check itself still never reports PASS or FAIL and every later check is still skipped, so the summary stays wrong). The filer's hypothesis — guard each checked command so the reporter always runs — is one half of the accepted approach; the named abort diagnostic is the alternative contribution, because guarding known sites cannot cover the roughly one hundred other commands in the file whose failure would still exit it without a word.

Scope check: A — harness-repo-only (Step 1 signal 2: this repository's own validation gate, `test/test_harness_integrity.sh`); B — n/a (no new skill); C — provider-agnostic (a bash gate, no provider surface). No catalog registration is required. No rule in the shared harness contract is narrowed or replaced, so no consumer-convention sentence is owed. Event spine: no event, metric, span, log line, or report is added or changed; the suite keeps reporting through its own existing PASS/FAIL/summary output.

Verified foundation: `test/test_harness_integrity.sh:2` sets `-euo pipefail`; `assert()` at `:27` and `warn_check()` at `:52` take an already-computed status as their second argument; 17 call sites (`:76`, `:85`, `:93`, `:101`, `:1120`, `:1123`, `:1132`, `:1135`, `:1142`, `:1150`, `:1154`, `:1159`, `:1180`, `:1385`, `:1388`, `:1392`, `:1395`) run a bare command and then pass `$?`, so under errexit the shell exits before the reporter is reached. The suite already uses `set +e` / `rc=$?` / `set -e` at 21 places to capture a status deliberately, so status capture is established house style rather than a new idea. The summary and exit live at `:1752-1765`. The only existing trap is an `EXIT` trap set and cleared inside check 5c (`:592`, `:602`), so an `ERR` trap adds no interference. `test/test_harness_integrity_update_flow.sh` is the local precedent for a focused fixture spec over a gate, and check 22 (`:1518-1557`) is the precedent for wiring one.
