# Halt record

Status: halted
Slug: operator-configurable-confidence-floor-for-acting-
Class: needs-human
Halting step: prd_audit
Phase: SHIP
Branch: feat/daemon-operator-configurable-confidence-floor-for-acting-
Head SHA: 6ce59171169fa1e3d855a0c1788c96f3cfb0120c
Halted at: 2026-09-07T12:04:27.212Z

Push status: this record may be ahead of the remote; push is not guaranteed.

## HALT

```text
Validation group "prd_audit" halted: needs human DECIDE — S3.7 (existing-task: The authoritative completion gate at src/conductor/src/engine/artifacts.ts:3644-3646 calls (ctx.buildReviewEffectiveResolver ?? resolveEffectiveBuildReviewVerdict)(dir, aggregate) with no minConfidence, so build-review-aggregate.ts:323-328 defaults the floor map to {} and a fully suppressed lap scores FAIL at artifacts.ts:3654. Plan task 8's Done when — 'A lap whose every finding is suppressed passes the build_review gate with no remediate dispatch and no kickback charged' — admits exactly this repair, and its step 3 names the threading; task 8 is still pending in .pipeline/task-status.json, so no new task is needed. Sibling sweep: the same unthreaded-floor shape recurs at src/conductor/src/engine/conductor.ts:10787-10790 (S3.8/AB-3), which task 9's Done when admits and which is bound separately below; no other resolveEffectiveBuildReviewVerdict call site lacks the floor (the runner at step-runners.ts:2134-2137 supplies it).); S3.8 (existing-task: src/conductor/src/engine/conductor.ts:10787-10790 re-resolves the aggregate with only { emit }, so the suppressedFindingIds wired at conductor.ts:10843 into build-review-adjudication-coordinator.ts:453,467 is always empty and a mixed lap still sends sub-floor findings to remediate. The coordinator side of task 9 is shipped and correct; the missing half is the floor threading that task 8 step 3 declares and that task 9's Done when 'Suppressed ids reach the coordinator as an input distinct from operator-resolved ids' requires end to end. Both tasks are pending, so the remedy is admitted without plan growth. Matched-pair note: this call site and artifacts.ts:3644-3646 must both receive the resolved floor or the gate and the router will disagree; they are bound together across S3.7 and this row.); S4.5 (existing-task: No suppression path emits a kickback directly, but because the completion gate at src/conductor/src/engine/artifacts.ts:3644-3646 fails a suppressed-only lap, routing still reaches the charge at src/conductor/src/engine/conductor.ts:10892-10895, leaving a kickback attributable to a suppressed finding. This is the same root cause as S3.7 and is admitted by task 8's Done when clause 'no kickback charged'; task 8 is pending, so it is re-staged rather than duplicated.); S5.1 (existing-task: src/conductor/src/engine/remediation-case-store.ts is absent from git diff main...HEAD --name-only and carries no 'suppress' match, so no durable entry keyed by suppressed finding id exists. Task 12's Done when — 'The case store persists one suppression entry per suppressed finding id, updated in place on recurrence and never pruned' — admits the work verbatim, and .pipeline/task-status.json still records task 12 as pending despite commit 21ace421b naming it (git show --stat is empty).); S5.2 (existing-task: src/conductor/src/engine/build-review-adjudication-context.ts:55-67 has no suppression-history field; the branch adds only excludedSourceIds, which subtracts from unresolvedSources rather than assembling a non-blocking history section. Task 13's Done when 'The adjudication context carries suppression entries in a history section distinct from current sources' admits it, and task 13 is pending.); S5.3 (existing-task: No store entry exists at all (src/conductor/src/engine/remediation-case-store.ts:58-62 defines only version, feature and cases, and its parser at :217 rejects any further key), so there is no last-seen lap to update in place on recurrence. Task 12's Done when covers in-place update on recurrence; task 12 is pending.); S5.4 (existing-task: src/conductor/src/engine/remediation-case-validator.ts is unchanged on this branch and matches no 'suppress', so the source-complete exemption that lets a case-v1 judgement assign no outcome to a suppression entry does not exist. Task 13's Done when 'A judgement that assigns no outcome to a suppression entry still validates' admits it; task 13 is pending.); S5.5 (existing-task: Never-pruned retention is untestable because no suppression entry is persisted at all (remediation-case-store.ts:58-62). Task 12's Done when names 'never pruned' and 'Resolved cases and suppression entries remain in the store across laps'; task 12 is pending, so the retention obligation lands with the entry itself rather than as separate work.); S5.6 (existing-task: Resolved cases persist as before, but the suppression entries this criterion pairs them with are never written (remediation-case-store.ts:217 accepts only version, feature, cases). Task 12's Done when 'Resolved cases and suppression entries remain in the store across laps' admits the pairing; task 12 is pending. No existing resolved-case persistence coverage is displaced — the entry list is additive and parses as empty when absent, which the same Done when clause pins.); S5.7 (existing-task: The suppression entry whose absence from the operator disposition store this criterion asserts does not exist in any store, so the invariant is untestable until task 12 lands. Task 12's Done when 'A suppression entry never appears in the operator disposition store' admits it; task 12 is pending.); S7.2 (existing-task: src/conductor/src/engine/event-sinks.ts:42 keeps remediation_adjudication_completed at render: false and src/conductor/src/daemon-cli.ts has no case for it, so a skipped dispatch produces no daemon-log adjudication line even though the event is emitted correctly at build-review-adjudication-coordinator.ts:338-346. Task 16's Done when 'the daemon log shows a completed adjudication line' admits the repair; only its step 3 (emit) was performed and task 16 is pending. Matched-pair note bound into the task: flipping the sink entry requires the corresponding daemon-cli switch case and its DAEMON_SWITCH_HANDLED_EVENT_TYPES registry row to agree, exactly as build_review_outer_verdict does at event-sinks.ts:37 / daemon-cli.ts:2522.); S7.5 (existing-task: src/conductor/src/engine/conductor.ts:10887-10890 takes the pass route with only saveConductorStepStatus(... 'done') and continue, discarding adjudication.trace, which is read only on the halt and build routes at conductor.ts:10884,10894 — so the trace finalize builds via renderBuildReviewAdjudicationTrace is never surfaced on the skipped-dispatch path. Task 16's Done when 'The rendered trace lists each settled case with its finalized outcome' admits it; task 16 is pending.); AB-1 (existing-task: REMEDIABLE, governing clause Task 3: skills/build-review-test-quality/SKILL.md:93-98 states optional grader confidence, but the production dispatch at src/conductor/src/engine/step-runners.ts:2189-2195 requires the exact result shape from src/conductor/src/engine/build-review-domain.ts:70, which omits confidence — so no conforming runtime producer can emit the field. Task 3's Done when 'A result matching the skill text's JSON example parses with its confidence retained' admits including confidence in the live exact shape; task 3 is pending. Matched-pair: the skill contract text and the runtime shape constant are the pair that must agree, and the task binds both sides; the approved architecture is unchanged, so this is conforming implementation drift and routes to build work, not architecture_review.); AB-2 (existing-task: REMEDIABLE, governing clause Task 8: authoritative completion at src/conductor/src/engine/artifacts.ts:3644-3646 omits configured floors, so the empty default at build-review-aggregate.ts:323-328 leaves every scored finding unresolved and D4.3/D4.4's fully-suppressed PASS is unreachable. The approved ADR set already specifies this outcome (the as-built Plan-Gap Check records no PLAN_GAP), so this is implementation drift under an approved design, and task 8's Done when admits it. Task 8 is pending.); AB-3 (existing-task: REMEDIABLE, governing clause Task 9: raw-FAIL routing at src/conductor/src/engine/conductor.ts:10787-10791 omits configured floors before forwarding suppressedFindingIds at conductor.ts:10843, so D4.4's mixed-lap exclusion is unreachable in the production controller even though the coordinator consumption at build-review-adjudication-coordinator.ts:451-467 is correct. Tasks 9 and 8 are both pending and jointly admit the threading; they are bound together because this call site and artifacts.ts:3644-3646 are the matched pair that must receive the same resolved floor.); AB-4 (existing-task: REMEDIABLE, governing clause Task 12: D4.6 requires one durable entry per suppressed finding, but src/conductor/src/engine/remediation-case-store.ts:58-62 defines no such state and its parser at :217 rejects any state key beyond version, feature and cases. Task 12's Done when admits the additive suppression list, its coordinator write under the existing lease, and STORE_VERSION staying v1; task 12 is pending. The v1-parse clause preserves the existing store-compatibility coverage rather than replacing it.); AB-5 (existing-task: REMEDIABLE, governing clause Task 13: D4.6 requires a distinct non-blocking judge-history section, but src/conductor/src/engine/build-review-adjudication-context.ts:55-67 has no such field and build-review-adjudication-coordinator.ts:465-468 merely filters current suppressed ids. Task 13's Done when admits the history section plus the validator exemption; task 13 is pending and depends on task 12.); AB-6 (existing-task: REMEDIABLE, governing clause Task 14: D5.1 treats an applied defer or effect-free reject as finalized, but new cases are created resolution: 'open' at src/conductor/src/engine/remediation-case-reconciler.ts:162-175, applying a deferral changes only the effect at remediation-case-effects.ts:248-251, and the predicate at build-review-adjudication-coordinator.ts:149-156 requires resolution === 'resolved', so exact recurrence still dispatches the judge. Task 14's Done when 'A finding whose exact id links to a finalized deferred, rejected, or merged case is removed from the live source set before dispatch' admits realigning the predicate; task 14 is pending. Coverage note: the existing coordinator cases at test/engine/build-review-adjudication-coordinator.test.ts:829-891 already pin resolved-case settlement and must keep passing — the predicate is widened to finalized state, not relaxed away.); AB-7 (existing-task: REMEDIABLE, governing clause Task 15: D5.2 excludes reserved or failed effects, but the predicate at src/conductor/src/engine/build-review-adjudication-coordinator.ts:149-156 checks resolution, disposition and source outcome and never reads record.effect.status — it currently relies on remediation-case-reconciler.ts:212-238 withholding 'resolved', an indirect guard that AB-6's widening to finalized state would remove. Task 15's Done when 'A drifted id, a reserved or failed effect, and an open action case each leave the finding live' admits the explicit effect-status check; task 15 is pending and depends on 14. Bound with AB-6 in that order so the widening never ships without its guard.); AB-8 (existing-task: REMEDIABLE, governing clause Task 16: D5.3 requires settled case ids, no new effect ids, and daemon-log visibility, but src/conductor/src/engine/build-review-adjudication-coordinator.ts:338-346 emits every historical non-none effect id and src/conductor/src/engine/event-sinks.ts:42 declares the completion event render: false with no daemon-cli.ts case. Task 16's Done when carries all four clauses (settled ids, empty effect list, unchanged kickback ledger, rendered trace and daemon line); task 16 is pending. This subsumes S7.2 and S7.5, which cite the same task.) — remediation produced no dispatchable build work; the implicated task(s) are already evidence-complete — human needed
```
