# Halt record

Status: resolved
Resolution cause: rekick
Resolved at: 2026-09-06T11:59:49.837Z
Slug: build-review-rubrics-need-a-post-join-adjudicator-
Class: needs-human
Halting step: prd_audit
Phase: SHIP
Branch: feat/daemon-build-review-rubrics-need-a-post-join-adjudicator-
Head SHA: 7f0378a15011d1410f984c6ac83354236830055b
Halted at: 2026-09-06T10:51:47.004Z

Push status: this record may be ahead of the remote; push is not guaranteed.

## HALT

```text
Validation group "prd_audit" halted: needs human DECIDE — AB-1 (existing-task: REMEDIABLE implementation violation against admitted work: all three all-operator-resolved shortcuts in src/conductor/src/engine/build-review-adjudication-coordinator.ts (:272-273 before the store read and attempt-evidence read even happen, :299-300 after context assembly, and :322-323 after the judgement) call finalize directly, bypassing readBuildReviewWorkOrderAttemptedCaseIds at :282-286 and reconcileRemediationCases at :333-337; finalize (:170-218) retires only cases whose complete source set is accepted in the current lap, so a prior open applied action case with no current source survives, reduceBuildReviewAdjudication returns PASS at build-review-adjudication.ts:74, and BUILD recovery at conductor.ts:4604-4629 still treats it as live. Plan task 5 Done-when 3 ('A case absent after recorded BUILD attempt evidence becomes resolved; history remains present') admits exactly this remedy, and the as-built Plan-Gap check records no PLAN_GAP, so no task is appended. Class sweep: all three shortcut exits are the same shape and are named together; the :272 exit additionally has no attempt evidence in scope yet, so the repair must read it before finalizing rather than only threading it through the later two. Matched-pair counterpart: the absence-resolution predicate must stay derived from the shared effect-status test that reconciler and settlement already share (commit 735cd44b4), never a second open-coded status check. No coverage is removed; the existing all-resolved route assertions in build-review-adjudication-coordinator.test.ts remain and gain durable-state assertions.); AB-2 (existing-task: REMEDIABLE implementation violation against admitted work: the judgement await at src/conductor/src/engine/build-review-adjudication-coordinator.ts:315 returns through the non-authority-aware fail helper at :135-137 on rejection, and the next operatorResolvedFindingIds read at :318 exists only on the successful-judgement path, so an exact operator acceptance landing while the provider call is in flight yields an obsolete content-specific needs-human HALT at conductor.ts:10370-10376. Plan task 16 Done-when 1 ('Each exact late-acceptance fixture suppresses obsolete work/effect/route/HALT for only its source') and Done-when 2 admit this remedy directly. Class sweep: every content-specific fail(...) exit that follows awaited work shares this shape and is named in the same repair — the context-assembly fail at :293 and :311, the graph-validation fail at :329, and the reconciliation fail at :337; the infrastructure fail paths ('operator disposition state is unavailable', 'case store ...') must keep failing closed regardless of authority and are deliberately excluded. Matched-pair counterpart: live source ids must be computed with the existing liveSourceIdsFor helper (:139-141, reassigned at :304), never a second open-coded acceptance predicate. No coverage is removed: the effect-boundary re-reads already delivered at :432 and :492 and their tests stay unchanged as the counterpart proving suppression at the later windows.); AB-3 (existing-task: REMEDIABLE implementation violation against admitted work: failPending durably flips every settling reserved action effect to failed under its own lease at src/conductor/src/engine/remediation-case-effects.ts:135-165 and RemediationCaseStore.mutate returns only after atomic replacement (remediation-case-store.ts:326-343), but the executor does not return the persisted transition set; the coordinator instead re-reads the store at build-review-adjudication-coordinator.ts:442 and treats a read failure as an empty set at :443-445, so :448 suppresses the remediation_effect_failed occurrence for a case accepted during the action boundary, and finalize cannot recover it because retiredEffect is created only for a still-reserved effect at :203-215, leaving :248-252 with nothing to emit. Plan task 21 Done-when 2 admits emission of remediation_effect_failed at the point the coordinator performs it. Class sweep: the deferral failure path at :495-510 is the correct counterpart — it emits from the in-scope reconciled record with no second store read and needs no change — and finalize's retirement emission at :248-252 already emits unconditionally; the action path at :438-452 is the only lossy site of this class. No coverage is removed: the existing injected-sink assertions for reserve/apply/fail and the Task 15 start/completion/failure assertions stay green, and no second event file, writer, or channel is introduced.); AB-4 (existing-task: REMEDIABLE implementation violation against admitted work, and the same defect the prd_audit FIXABLE row S10.1 raises: in settleRemediationCasesOnCleanBuildReview the store read and the isBuildReviewSettlementObligationCase guard are nested inside if (missingAttemptEvidence) at src/conductor/src/engine/conductor.ts:4679-4694, so once any earlier lap has published .pipeline/build-review-work-order.json (never deleted — build-review-work-order.ts has only the temp-file cleanup at :202) attemptEvidence.ok is true, the obligation is never evaluated, resolveAbsentOpenNonActionCases is omitted at :4699-4704, an open reserved deferral stays open, the function returns settled at :4722 and conductor.ts:11446-11470 stamps build_review done — a terminal PASS with an unfiled intake effect. Plan task 19 Done-when 1 ('never returns PASS from a reserved/failed effect') admits this remedy and names conductor.ts in its Files list. Class sweep: the sibling site is remediation-case-reconciler.ts:210-220, whose absent open act branch resolves from attemptedIds membership alone without the effect-status test its non-action sibling applies at :226-229; both are repaired in this one task. Matched-pair counterpart: both branches and the settlement guard must derive from the single shared hasReservedOrFailedRemediationEffect / isBuildReviewSettlementObligationCase test (remediation-case-effects.ts:64, remediation-case-reconciler.ts:230) so the two vocabularies cannot drift again. No coverage is removed: the benign-absence clean-PASS assertions and the resolvedAbsentCaseIds reconciliation emissions in conductor-build-review-adjudication.test.ts stay green as the counterpart.); S10.1 (existing-task: FIXABLE with an existing active-plan owner (the audit records Plan task 19 for this row and no PLAN_GAP): a resumed process cannot derive the next legal transition because clean-PASS settlement skips its obligation test whenever a stale work order remains on disk — src/conductor/src/engine/conductor.ts:4679 nests the store read and isBuildReviewSettlementObligationCase guard (:4684-4694) inside if (missingAttemptEvidence), readBuildReviewWorkOrderAttemptedCaseIds (build-review-work-order.ts:283-291) returns ok for any parsable order and nothing deletes it, so a deferral whose intake create failed keeps effect.status reserved (remediation-case-effects.ts:232-234) while settlement returns settled at :4722 and conductor.ts:11446-11470 stamps build_review done. This is the same defect as as-built finding AB-1 through AB-4's fourth violation and is bound to the same plan task so the two gates cannot produce divergent repairs. Class sweep: the second unguarded branch is remediation-case-reconciler.ts:210-220, which resolves an absent open act case from attemptedIds membership without consulting hasReservedOrFailedRemediationEffect the way :226-229 does; both sites are named in this one binding. Matched-pair counterpart: the settlement guard and both reconciler branches derive from the one shared effect-status test at remediation-case-effects.ts:64 and remediation-case-reconciler.ts:230. No coverage is removed; the existing absent-order terminal-PASS and resolvedAbsentCaseIds assertions remain as the benign-absence counterpart.) — remediation produced no dispatchable build work; the implicated task(s) are already evidence-complete — human needed
```
