# Halt record

Status: halted
Slug: testquality-admits-724-test-titles-for-eight-chang
Class: needs-human
Halting step: prd_audit
Phase: SHIP
Branch: feat/daemon-testquality-admits-724-test-titles-for-eight-chang
Head SHA: f0fe02b1be8d22821487b4d3f31da7f9d110604a
Halted at: 2026-09-06T20:59:51.976Z

Push status: this record may be ahead of the remote; push is not guaranteed.

## HALT

```text
Validation group "prd_audit" halted: needs human DECIDE — AB-1 (existing-task: Conforming implementation drift inside the approved architecture (the as-built report records plan gap: none): build-review-inputs.ts:770,795-797 reduces established targets to a file set and :781-790 serializes every changed declaration from those files, build-review-coordinator.ts:427-435 filters declarations by that file set alone, and build-review-domain.ts:161-173 turns every retained title region into authorized finding content, so one established target authorizes unrelated changed siblings; the repair is to carry target-level identity through projection and authority instead of file membership, which Task 10's Done when (input v3 with unchanged directly changed target count and no extra unrelated sibling titles) and Task 11's Done when (fresh reference validation rejects unrelated sibling findings even under a coarse fallback anchor) already admit. Class sweep: the four sites above are one class and must be changed together; the changedTestTitles compatibility projection at build-review-inputs.ts:781-790 and the region construction at build-review-domain.ts:161-173 are a matched pair (the hash of a title feeds the authority region), so narrowing one without the other silently drops or widens authority. No coverage is removed: Task 10 and Task 11 own the tests that assert both the target count and the sibling rejection.); AB-2 (existing-task: Conforming implementation drift, not a design gap: UncertainBuildReviewTestScopeCandidate carries no source path (build-review-test-scope.ts:69-78), mergeTestScopes flattens per-file candidates (build-review-inputs.ts:578-586), and buildReviewCandidateScopeResolutionContext matches evidence only by HEAD side plus byte offsets (build-review-coordinator.ts:250-304), so two candidates in different files with equal spans can take each other's evidence and candidate id, after which build-review-domain.ts:167-169 anchors on the matched region hash instead of declared-title/content-region/occurrence identity; the repair is to bind source identity onto the candidate before the merge and to match on source plus region, which Task 11's Done when (preserving result-v3 title/occurrence identities for legitimate targets) and Task 12's Done when (persists a source-grounded unsupported-candidate resolution with engine-stamped result-v3 identity) already admit. Class sweep: the candidate type, the merge, the evidence match, and the canonical-reference construction are one matched set keyed by the same identity, and changing only the type leaves the offset-only match in place; the evidence-pinning side of the pair (pinScopeEvidence at build-review-inputs.ts:769) must emit the same source key the matcher reads. No existing assertion is removed or relaxed.); AB-3 (existing-task: The provider contract Task 12 owns was not delivered in an executable form: step-runners.ts:2213 requires a top-level payload of findings, scopeResolutions and optional counterfactualSensitivity while step-runners.ts:2215 requires the final JSON to match renderBuildReviewJudgedResultShape, whose rendered envelope at build-review-domain.ts:192 has no scopeResolutions field, so a candidate-bearing reviewer cannot satisfy both clauses; the repair is one satisfiable schema that names scopeResolutions and leaves engine-owned envelope identity to the engine, which Task 12's Done when (one provider call accepts and persists a source-grounded candidate resolution with engine-stamped result-v3 identity) admits directly. Class sweep, matched pair: the rendered shape (build-review-domain.ts:192), the two prompt clauses (step-runners.ts:2213,2215) and the skill contract text in skills/build-review-test-quality/SKILL.md state the same schema in three places and must be brought into agreement in one change — preferably by rendering the single engine-owned shape into both the prompt and the contract test rather than restating it — since the contract test at build-review-skill-contract.test.ts is what makes the drift visible. Nothing is removed: the existing findings and counterfactualSensitivity fields and their assertions stay.); AB-4 (existing-task: Diagnosis and acceptance read different inputs: validateBuildReviewDispatchedResult parses candidate resolutions and builds candidate-aware authority at build-review-coordinator.ts:318-324, while describeBuildReviewDispatchedResultRejection builds authority from the projection alone at build-review-coordinator.ts:339-349, so a rejection caused by another field can be reported as an unauthorized candidate-region finding; the repair is to build the rejection description from the same parsed resolutions and reference context the accepting predicate used, which Task 11's Done when (rejects the named problem) and Task 12's Done when (the dispatch path's bounded repair turn) admit. Class sweep, same class at a second site: describeBuildReviewJudgedResultRejection (build-review-domain.ts:200-236) enumerates no scopeResolutions problem at all, so a resolutions-caused rejection reaches the repair turn as the generic 'no enumerated check explains why' message — fix both in one change, since sharing the context without enumerating the cause still yields a false or empty cause. No assertion is dropped; the existing rejection-message fixtures keep their expectations.); AB-5 (existing-task: Task 15 step 4 and the governing decision require scope summaries with established/candidate counts and unresolved reasons on the existing ConductorEvent path, but only prompt-size telemetry and the terminal build_review_scope_incomplete event ship (types/events.ts:258-277, build-review-coordinator.ts:148-166), so the normal-path summary is absent; the repair is one additional summary event on the existing emitter/persister spine — no sidecar, no second channel — which Task 15's Done when (candidate/binding/missing-evidence detail on the existing result/event path) and its named event-spine step admit. Class sweep, matched pair: the ConductorEvent union (types/events.ts), the render registry (event-sinks.ts:29) and the daemon renderer switch (daemon-cli.ts, default: break at :2948) must agree because event-sinks.test.ts:167,729 binds the rendered set to that switch — add the daemon case if and only if the new event is rendered, and note that the equivalent operator-visible line for the terminal event was already audited as within intent, so a rendered summary must be justified the same way or emitted unrendered. The coordinator's existing emitScopeIncomplete path and its assertions are preserved, not replaced.) — remediation produced no dispatchable build work; the implicated task(s) are already evidence-complete — human needed
```
