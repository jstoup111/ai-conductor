# Halt record

Status: halted
Slug: testquality-admits-724-test-titles-for-eight-chang
Class: needs-human
Halting step: prd_audit
Phase: SHIP
Branch: feat/daemon-testquality-admits-724-test-titles-for-eight-chang
Head SHA: ad99b77edf8a4855b558566ca51aba84c588a98b
Halted at: 2026-09-07T01:24:00.638Z

Push status: this record may be ahead of the remote; push is not guaranteed.

## HALT

```text
Validation group "prd_audit" halted: needs human DECIDE — AB-1 (existing-task: Conforming implementation drift inside the approved architecture (the as-built report records plan gap: none): snapshotTestQualityScope was materially rewritten at src/conductor/src/engine/build-review-inputs.ts:327-383 to consume BuildReviewScopeSource, structured changes and pinned stories, but a repository-wide search over src, test and scripts finds only its definition (verified 100%), while live assembly uses snapshotTypedTestScope at build-review-inputs.ts:926-933. Task 8 step 2 already governs this exact repair — it requires REPLACING the existing independent file-wide scope collection in assembleBuildReviewInputs with the typed analyzer — so removing the superseded helper is admitted by Task 8's Done when (assembly returns precise changed declarations rather than admitting every title in a marked file) and needs no new plan task. Class sweep: AB-2 is the second site of the same class (the sibling title helper at :541) and is dispositioned alongside this one; both must be removed in the same change together with anything their removal orphans (their private helpers and any fixtures that reach them only through them). No coverage is removed: nothing calls either helper, so no assertion loses its subject; if a test does reach one, Task 8's assembly integration proof is the surviving coverage and the test must be re-pointed at snapshotTypedTestScope in the same task rather than deleted.); AB-2 (existing-task: Same class as AB-1 and same governing task: snapshotChangedTestTitles was materially rewritten at src/conductor/src/engine/build-review-inputs.ts:541-550 to read through the new source reader and structured change inventory, but has no caller in src, test or scripts (verified 100% by symbol search); the live typed path derives changedTestTitles inside snapshotTypedTestScope at build-review-inputs.ts:781-802. Task 8 step 2's replace-the-file-wide-title-collection mandate and its Done when admit the removal, so no plan growth is required. Class sweep: this and AB-1 are the only two unreachable snapshot rungs in build-review-inputs.ts (searched the whole module's exports and local functions); the aggregate re-export in AB-3 is a third unreachable rung of a different shape and is dispositioned separately. No assertion is dropped — the changedTestTitles behavior remains covered by Task 8's assembly integration proof and Task 10's projection fixtures.); AB-3 (existing-task: src/conductor/src/engine/build-review-aggregate.ts:158 re-exports deriveBuildReviewScopeIncompleteFault, but every production consumer imports the defining domain module directly (build-review-coordinator.ts:8, build-review-cli.ts:8, step-runners.ts:95; verified 100% by import search) and only tests reach the alias, so the newly shipped public seam is unreachable. Task 15 owns build-review-aggregate.ts and the scope-fault derivation it wraps, and its Done when (routing fixtures charge only the existing fault allowance and retain valid findings) is unaffected by which module name the tests import, so the removal is admitted with no new task. Class sweep: build-review-aggregate.ts:155 keeps the real aggregate-local wrapper that production does use — that stays; only the bare re-export at :158 goes. Matched pair: the aggregate tests that import through the alias must be re-pointed at build-review-domain.js in the SAME change, never deleted — Task 15's aggregate coverage is the behavior they assert and it must survive intact.); AB-4 (existing-task: A failed read of a plan-selected stories artifact silently becomes empty scope authority: src/conductor/src/engine/build-review-inputs.ts:922-925 does readOptional(...) ?? '', and BuildReviewScopeSource.readAtOptional returns undefined for ANY failed git show at build-review-scope-source.ts:99-126, not only for an intentionally absent artifact, so an evidence-read failure is indistinguishable from absence and quietly changes binding authority. This is conforming implementation drift against approved decision 1, and Task 1's Done when already states the required behavior verbatim — the source-reader negative fixtures must reject required missing blobs, never substitute live content or an empty file, and expose a bounded evidence-read failure — with Task 8 owning the assembly call site in build-review-inputs.ts. Class sweep: the fix must separate the two outcomes at the reader (a required read failing vs. an artifact legitimately absent) and then apply it to EVERY readOptional(...) ?? '' / ?? undefined coalescing site in assembleBuildReviewInputs, not only the selected-stories line the auditor cited; sites found and left alone must be named. Matched pair: readAtOptional's return contract and its callers' coalescing are one contract — widen the return to distinguish absence from failure and update all callers in the same change, or the next lap re-raises this at a sibling line. No coverage is removed: the existing absent-artifact fixtures keep asserting the absence path; the failure path is added beside them.); AB-5 (existing-task: Established-target authority widens to unrelated changed siblings by file membership: src/conductor/src/engine/build-review-inputs.ts:781-802 serializes every changed declaration in any file that contains an established target, build-review-coordinator.ts:427-435 filters those records by file selector alone, and build-review-domain.ts:174-184 turns every surviving record into finding authority, so one bound target authorizes an unrelated unbound sibling contrary to approved decision 8. The repair is to carry target-level identity (declared title, content region, occurrence) through projection and authority instead of file membership, which Task 10's Done when (input v3 with an unchanged directly-changed target count and no extra unrelated sibling titles as the fixture grows) and Task 11's Done when (fresh reference validation rejects unrelated sibling findings even under a coarse fallback anchor) already admit — no new plan task. Class sweep: those three sites are one class and must change together; the changedTestTitles compatibility projection and the region construction at build-review-domain.ts:174-184 are a matched pair because the title hash feeds the authority region, so narrowing one alone silently drops or widens authority. No coverage is removed — Task 10 and Task 11 own the target-count and sibling-rejection assertions and both must still pass.); AB-6 (existing-task: Candidate evidence is not source-bound: UncertainBuildReviewTestScopeCandidate carries declaration spans but no source path at src/conductor/src/engine/build-review-test-scope.ts:69-78, per-file scopes are flattened at build-review-inputs.ts:578-586, and build-review-coordinator.ts:250-304 then matches evidence by HEAD side plus equal character offsets without comparing source.fileName, so equal spans in two different files bind to the same first evidence record and produce wrong-file authority or duplicate candidate ids (approved decision 2). Task 5 owns the candidate representation in build-review-test-scope.ts, Task 8 owns the per-file merge in assembly, and Task 12's Done when (one provider call accepts and persists a SOURCE-GROUNDED unsupported-candidate resolution with engine-stamped result-v3 identity) already requires the source-bound outcome — so the remedy is admitted without plan growth. Class sweep, matched pair: the candidate type, the merge, and the offset-only matcher are keyed by the same identity — adding a source field to the type without changing the matcher leaves the collision in place, and the evidence-pinning side (pinScopeEvidence at build-review-inputs.ts:769) must emit the same source key the matcher reads. Found and named, not silently fixed: build-review-domain.ts's region anchoring consumes the matched record and is repaired under AB-5's target-identity change, so it is not re-derived here. No existing assertion is removed or relaxed.); AB-7 (existing-task: Duplicate sibling suites borrow each other's Covers markers because ancestry is title-prefix-only: src/conductor/src/engine/build-review-test-bindings.ts:147-149 defines suite ancestry as a matching title chain prefix, so :210-215 supplies markers from every same-titled suite satisfying that prefix without lexical/body containment or suite occurrence identity — with two sibling describe("dup") suites and a marker on only the first, both suites' children bind to that marker, violating approved decision 4's no-sibling-borrowing rule. Task 4's Done when states this behavior directly (the sibling-marker fixture must produce no borrowed binding and suite inheritance is allowed only within descendant tests), so the repair is admitted by an existing task and appends nothing. Class sweep: title-chain equality is used for ancestry AND for the descendant test lookup in the same module — both must move to source-region containment plus suite occurrence identity in one change, or a marker still leaks in the other direction; the declaration comparison's occurrence ordinal (build-review-test-declarations.ts) is the existing identity to reuse rather than inventing a second one. No coverage is dropped: Task 4's existing sibling-marker, absent-feature-id and unmarked-test fixtures keep their expectations and gain the duplicate-suite case beside them.); AB-8 (existing-task: The provider contract is not executable: src/conductor/src/engine/step-runners.ts:2213 mandates a payload with top-level findings, scopeResolutions and optional counterfactualSensitivity, while :2215 simultaneously mandates that the final JSON match the rendered shape at build-review-domain.ts:205, which is the engine-stamped envelope and contains no scopeResolutions — so a candidate-bearing reviewer cannot satisfy both mandatory clauses. Task 12 owns exactly this boundary (its files include step-runners.ts, build-review-cli.ts and skills/build-review-test-quality/SKILL.md) and its Done when requires one provider call to accept and persist a source-grounded candidate resolution with engine-stamped result-v3 identity, so the remedy is admitted with no plan growth. Class sweep, matched triple: the rendered shape (build-review-domain.ts:205), the two prompt clauses (step-runners.ts:2213,2215) and the skill contract text in skills/build-review-test-quality/SKILL.md state the same schema in three places — bring them into agreement in one change by rendering the single engine-owned provider-payload shape into both the prompt and the contract assertion rather than restating it, since build-review-skill-contract.test.ts is what makes the drift visible. Nothing is removed: findings and counterfactualSensitivity and their existing assertions stay, and envelope identity remains engine-stamped.); AB-9 (existing-task: Rejection diagnosis is weaker than acceptance: validateBuildReviewDispatchedResult parses candidate resolutions and builds candidate-aware authority at src/conductor/src/engine/build-review-coordinator.ts:313-331, while describeBuildReviewDispatchedResultRejection passes authority with no resolutions at :339-349, and build-review-domain.ts:213-246 enumerates no invalid, missing, duplicate or unknown scopeResolutions cause — so a result rejected for another field can be diagnosed as an unauthorized anchor, and a resolution-caused rejection reaches the bounded repair turn as an unexplained cause (approved envelope decision 6). Task 11's Done when (validation rejects missing/duplicate candidate answers and foreign obligations WITH THE NAMED PROBLEM) and Task 12's bounded-repair dispatch path admit the repair; no new task is needed. Class sweep, same class at a second site: describeBuildReviewJudgedResultRejection at build-review-domain.ts:213-246 must gain the enumerated resolution causes in the same change, because sharing the parsed context without naming the cause still yields an empty diagnosis. No fixture expectations are relaxed — the existing rejection-message assertions must continue to hold for their current causes.); AB-10 (existing-task: Approved decision 9 requires normal-path scope counts and unresolved reasons on the existing event spine, but only prompt-size telemetry and the terminal build_review_scope_incomplete event ship (src/conductor/src/types/events.ts:258-277, build-review-coordinator.ts:159-166) — no summary event with established/candidate counts and unresolved reasons exists in the union or at any emit site. Task 15 step 4 names this obligation explicitly (emit scope summaries through ConductorEvent and existing sinks; do not introduce a sidecar) and its Done when requires candidate/binding/missing-evidence detail on the existing result/event path, so the remedy is admitted and nothing is appended. Per the repository event-spine rule this MUST extend ConductorEvent — no sidecar file, no second channel. Class sweep, matched set: the ConductorEvent union (types/events.ts), the sink render registry (event-sinks.ts:20-30) and the daemon renderer switch (daemon-cli.ts) must agree because event-sinks.test.ts binds the rendered set to that switch — add the daemon case if and only if the summary is meant to be operator-visible, otherwise emit it unrendered and say so. The drift note that .pipeline/events.jsonl contains no build_review_scope_incomplete record is the same gap's other face: the terminal emit path exists in source but is unexercised, so the added coverage must exercise emit through persist for both events. The existing emitScopeIncomplete path and its assertions are preserved, not replaced.); AB-11 (existing-task: The two safe-path validators disagree: safeRepoRelativePath at src/conductor/src/engine/build-review-scope-source.ts:29-43 admits a space in any segment, while isCanonicalPath at build-review-domain.ts:76-82 rejects any space-bearing value whose first segment contains a space, so 'my tests/widget.test.ts' can enter frozen scope but cannot survive candidate/finding authority validation (verified 100% by reading both validators). Task 1 step 3 (permit safe spaces, reject absolute/traversal/NUL) and Task 11 step 4 (permit safe repository-relative space-containing paths CONSISTENTLY WITH SOURCE ACCESS) both admit the repair, so it appends no task. Binding constraint from the operator's 2026-09-07 NC.1 refusal recorded in .pipeline/HALT.cleared: the shared PATH validator must NOT be re-widened to admit prose, and the restored refused-fixture guard at src/conductor/test/engine/build-review-finding-identity.test.ts:85 must keep rejecting 'The affected test is test/widget.test.ts.' — reconcile by deriving BOTH validators from one shared path-shape predicate (or by narrowing the source side to the same shape), never by relaxing the finding-side guard. Class sweep, matched pair: these two validators are the same vocabulary stated twice and must be derived from a single source or they drift again; parseBuildReviewCanonicalPathReference (build-review-domain.ts:73) and region() (:79) both read the finding-side predicate and inherit whatever it becomes, so both are covered by the single-source change. No coverage is removed: the prose-refusal fixtures and the space-bearing rename fixtures must both pass after the change — the fix is invalid if either set is weakened.) — remediation produced no dispatchable build work; the implicated task(s) are already evidence-complete — human needed
```
