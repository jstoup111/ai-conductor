# Halt record

Status: resolved
Resolution cause: operator
Resolved at: 2026-09-06T21:49:23.483Z
Slug: remediation-halts-when-the-owning-plan-task-is-alr
Class: needs-human
Halting step: prd_audit
Phase: SHIP
Branch: feat/daemon-remediation-halts-when-the-owning-plan-task-is-alr
Head SHA: 52bd1b07e6997647f35a55ff7151abbcf57dbcb4
Halted at: 2026-09-06T16:46:54.130Z

Push status: this record may be ahead of the remote; push is not guaranteed.

## HALT

```text
Validation group "prd_audit" halted: needs human DECIDE — S1.4 (existing-task: src/conductor/src/engine/conductor.ts:4163 renders only the bound task ids in the existing-task refusal detail while the owning gap.id is in scope; Task 8's Done-when 2 ('refuses unknown ownership, unauthorized findings, persistence failure ... without dispatching unauthorized or unrecorded repair') already admits this refusal-diagnostic obligation, so no new plan work is needed. Sibling sweep: the same missing-finding-id shape appears at conductor.ts:4142 and :4153 in the same refusal block and must be repaired together; no other refusal site was found. No existing assertion is removed — the added acceptance case extends test/acceptance/plan-growth-existing-task-restage.acceptance.test.ts alongside its current cases.); S2.3 (existing-task: src/conductor/src/engine/task-cli.ts:247 calls completeTaskDoneWhen on every missing-stamp invocation instead of only when an open repair obligation exists, so a never-reopened task now rewrites task-status.json or exits 1 where task-cli.ts:230 documents idempotent exit 0; Task 6 Step 3 authorizes narrowing that idempotence only so it 'must not silently skip an open repair', and Task 6 Done-when 3 ('Task-close tests retain untouched legacy behavior') already admits the repair, so it is existing plan work. The guard change preserves, not relaxes, the coverage Task 6 Done-when 1/2 delivered for evidenced and refused closes; the legacy no-op case at test/engine/task-cli.test.ts:699 is strengthened rather than removed by giving its fixture an engine-state.json with no obligation. Sweep: the missing-stamp branch is the only unconditional call site; task-cli.ts:123 and :268 are already obligation-scoped.); AB-1 (existing-task: src/conductor/src/engine/task-progress.ts:257-266 parses .pipeline/engine-state.json directly and its catch treats every read or parse failure as absence of an active plan, returning 'legacy' before the typed repair-state reader at task-progress.ts:292-299 can report malformed present state, and runTaskDone removes the current-task stamp at task-cli.ts:268-279 for every non-'refused' result — so malformed present control state fails open during task close. The as-built Plan-Gap check found no PLAN_GAP: Story 4 and Task 6 Step 2 ('Do not clear the current-task marker or report success on failed persistence') and Task 6 Done-when 2 already require the named refusal, so this binds to existing plan work. Sibling sweep of the same fail-open shape: this direct read is the only remaining tolerant engine-state parse on the close path — engine-state-store.ts:105 already distinguishes ENOENT from malformed and must be the seam this call adopts, so the pair cannot drift again. No assertion is removed; the ENOENT/legacy path Task 6 Done-when 3 delivered is preserved and its coverage kept.); S5.3 (existing-task: src/conductor/src/engine/conductor.ts:3868 returns {kind:'none'} for the entire remediation round as soon as routePrdAuditOverScope (conductor.ts:919-951) records, even though that sub-route inspects only grade === 'OVER_SCOPE' rows, so coexisting FIXABLE/PLAN_GAP findings drop to the deterministic classifyPrdAuditGaps fallback at conductor.ts:10868 and, on validation-group rounds (conductor.ts:7462, :7700, :7865), the as-built finding loses its remediation route and falls through to the group halt at conductor.ts:7572. Task 7 Step 2 ('Accepted-only work advances without entering repair admission; mixed findings retain independent blockers') and Task 7 Done-when 2 already admit the repair and its mixed-scope fixture, so this is existing plan work, not a planning omission. Sweep: all three validation-group call sites share the one guard, so scoping the short-circuit at conductor.ts:3868 closes the class rather than one site; the fixture belongs in the existing test/acceptance/prd-audit-no-owner-over-scope.acceptance.test.ts, whose current accepted-only assertions are retained, not replaced.); S7.2 (existing-task: src/conductor/src/engine/conductor.ts:4753, :4765 and :4790 return their admission-persistence, settlement and re-stage refusals without calling reportRefusal (conductor.ts:3846-3859) and without rendering admission.obligation.source.findingId or resolvedExistingTaskIdsByGapId, which are in scope, so these three new causes reach the operator HALT but never the gate_blocked event path that conductor.ts:3904 and :3913 already use. Task 11 Done-when 1 and 2 ('Existing event persistence receives the same refusal context as the route/HALT result') already admit this. Sweep: those three are the complete set of refusal returns this feature introduced that bypass reportRefusal; the two legacy causes are already wired, and unavailable-current-evidence is surfaced only in artifacts.ts:2805's build-predicate reason and must be carried into the same diagnostic. No second telemetry channel is added and test/engine/remediation-disposition-rejection.test.ts:58's existing per-gap rejection assertions are retained.); AB-2 (existing-task: Recovery at src/conductor/src/engine/conductor.ts:6130-6154 iterates every persisted repair record and checks currentByPlan[obligation.planIdentity] without ever comparing that identity to the engine state's activePlanPath, while recordActivePlanPath at conductor.ts:13369-13376 leaves historical per-plan entries in place — so an open settled obligation from a superseded plan can populate pendingNoOpBaselines and the first BUILD retry hint after another plan becomes active. The as-built Plan-Gap check found no PLAN_GAP: Task 9 Step 3 ('Include a later distinct repair and a changed active-plan path') and Task 9 Done-when 2 ('isolates reused task ids in a different active plan') already admit this. Sweep: repair-obligations.ts:93 already keys identity to the normalized active-plan path, so the restart loop is the single remaining unscoped consumer; no other reader of the obligation store omits the active-plan comparison. Nothing existing is removed — the changed-active-plan fixture is added to test/acceptance/plan-growth-existing-task-restage.acceptance.test.ts beside its current restart cases.); AB-3 (existing-task: Admission at src/conductor/src/engine/conductor.ts:4735-4739 persists only sorted finding ids, an authority label and the literal instruction 'existing-task remediation', while the actionable BUILD hint is built separately from the routed gaps at conductor.ts:4832-4837, so restart reconstruction at conductor.ts:6150-6153 can only emit the generic message and loses the rationale and repair instruction that told BUILD what defect to fix. The as-built Plan-Gap check found no PLAN_GAP: Task 3 Step 2 requires the durable record to persist 'source finding/authority and instruction', and Task 9 Done-when 1 requires the finding hint to survive restart. Bound to both owners because the persisted content is written at Task 8's admission seam and consumed at Task 9's restart seam — the write and the read are a matched pair and must move together rather than one side being changed alone. Sweep found no third consumer of the persisted instruction field; the initial-dispatch hint construction at conductor.ts:4832-4837 stays the single source the persisted value is derived from, so the two cannot drift.) — remediation produced no dispatchable build work; the implicated task(s) are already evidence-complete — human needed
```
