# Complexity: Publish the in-repo docs tree as a browsable GitHub Pages site

Tier: M

The change has no authentication, state machine, or application data model, but it adds a hosted-site dependency, site-wide navigation behavior, repository publishing constraints, and several observable acceptance paths across the full documentation tree.
