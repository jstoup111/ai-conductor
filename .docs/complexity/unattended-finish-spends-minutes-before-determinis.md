# Complexity: Unattended FINISH publication

Tier: L

The change introduces resumable publication state, crosses git and GitHub integration boundaries, changes lifecycle recovery routing, and must preserve distinct interactive and unattended safety behavior. The plan stem must remain `unattended-finish-spends-minutes-before-determinis`.
