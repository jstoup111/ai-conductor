# Complexity: every-as-built-blocked-verdict-halts-needs-human-i

Tier: L

Rationale: Supersedes an APPROVED ADR decision and amends another (one-owner appender clause);
adds a schema-constrained per-finding LLM classification to the as-built report contract; wires
a new gate key through the bounded-kickback/growth-ledger machinery and both halt writers
(serial + validation-group join); touches skill contract, parser, completion predicate,
remediation admission, caps/config, events, and docs. Issue is labelled size: L.
