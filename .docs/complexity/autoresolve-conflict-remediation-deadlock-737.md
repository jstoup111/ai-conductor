# Complexity: Autoresolve conflict-remediation deadlock

Tier: M

The change spans autoresolve eligibility, remediation-lane arbitration, repository configuration, an existing ADR amendment, and concurrency/negative-path tests. It introduces no model, authentication boundary, schema migration, external integration, or new persisted state machine.
