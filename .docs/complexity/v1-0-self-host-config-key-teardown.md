# Complexity: v1.0 self-host config-key teardown

Tier: S

The change is bounded to one repository-local configuration cleanup and the existing
owner-gate notice that becomes visible when its historical cutover is absent. It adds no
model, integration, authentication behavior, state machine, or new public configuration.
