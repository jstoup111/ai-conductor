# Complexity: Feature-scoped step artifact completion

Tier: M

The change revises a shared engine contract used by completion, interactive review, and dashboard consumers; it must preserve legacy artifact recognition and intentional repository/run-wide artifacts. It requires coordinated unit and integration coverage plus canonical gate documentation, but adds no authentication, external integration, model workflow, or new state machine.
