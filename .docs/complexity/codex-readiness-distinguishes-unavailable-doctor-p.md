# Complexity: Codex readiness probe failure separation

Tier: M

The change spans provider execution, the shared readiness contract, daemon recovery and event reporting, configuration, documentation, and multi-layer regression coverage. It adds a readiness-state distinction but no new external service, authentication mechanism, or large state machine.
