# Complexity: User-level config precedence

Tier: M

The change is bounded to the central configuration load/validation boundary but affects eight independently normalized keys, requires immutable input handling, a precedence matrix, and canonical documentation updates. It introduces no new integration, authentication boundary, or state machine.
