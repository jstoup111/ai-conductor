# Complexity: Interactive skill model contract

Tier: S

The change is limited to generated model-table metadata, focused contract validation, and technical documentation. It introduces no new model integration, authentication boundary, state machine, runtime dispatch path, or product-facing workflow, and should require at most two tightly related stories.
