# Conflict Check: Phase 9.3 Engineer Redesign

**Date:** 2026-06-26
**Stories checked:** all `.docs/stories/*.md` (9.0, 9.1, 9.2, 9.3-redesign, wave-c, runmode) +
active specs in `.docs/specs/`.
**Result:** 1 blocking conflict found and resolved. Re-check clean. 0 blocking remaining.

## Conflict (RESOLVED): Two competing Status:Accepted story sets for Phase 9.3

- **Stories involved:** `phase-9.3-engineer-redesign.md` (new) vs
  `phase-9.3-supervisor-engineer.md` (old, `Status: Accepted`, sourcing the now-SUPERSEDED PRD
  `2026-06-25-phase-9.3-supervisor-engineer.md`).
- **Type:** contradiction / state-conflict — same feature, two accepted-but-divergent story sets.
  The old set encoded the pre-redesign framing (FR-8 "may launch a daemon detached" with no
  pidfile-lock / ensure-running / liveness; no intake port). The daemon build-ready predicate keys
  on `.docs/plans/*.md → stories`, so two accepted 9.3 story sets would produce ambiguous build input.
- **Severity:** blocking.
- **Resolution (operator-selected):** `git rm` the old stories file (it was **not** on `main` —
  verified against origin/main and local main; PR #85 is unmerged). The "supersede anything on main"
  safeguard did not trigger (no old 9.3 artifact is on main). The new redesign file carries all 12
  old FRs forward (FR-1…12) plus the new port/liveness/handoff FRs, so nothing was lost. The old PRD
  remains as `SUPERSEDED-…` (handoff-mandated; referenced by the new PRD header).

## Areas checked clean

- **FR-24 build-ready predicate vs `daemon-backlog.ts`** — matches: stories `Status: Accepted`
  (no DRAFT) + plan has a dependency tree (`## Task Dependency Graph` / `**Dependencies:**`) + slug
  not in `.daemon/processed/`. Tightened FR-6 wording so plan-approval is signalled by the dependency
  tree (not a Status line on the plan), matching the predicate exactly.
- **O_EXCL pidfile lock (FR-17…23) vs 9.0 daemon loop** — no lock exists today, so the lock is purely
  additive; no contradiction with how the 9.0 daemon expects to run.
- **ensure-running (FR-21) vs daemon-independence / no-ad-hoc-rebase** — ensure-running is
  fire-and-forget spawn-iff-not-alive with no lifecycle ownership; it adds no rebase and does not
  manage the daemon. Consistent with the 9.0 finish-time-only rebase constraint.
- **Regression guard** — no new story assumes the superseded spawned-`claude`/stub-story behavior;
  the only mentions are explicit negative-path guards.

## Re-check

Single Phase 9.3 story set remains. No blocking or degrading conflicts. Cleared for `/plan`
(after architecture).
