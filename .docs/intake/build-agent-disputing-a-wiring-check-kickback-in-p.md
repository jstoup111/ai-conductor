# Intake origin: build-agent-disputing-a-wiring-check-kickback-in-p

Source-Ref: jstoup111/ai-conductor#1336
Owner: jstoup111

## Desired outcome

- A build step that ends without changing the tree is distinguishable, from the daemon
- When a build agent concludes the gate's finding is wrong or that the work belongs to
- A feature does not repeat an identical no-movement kickback cycle at full dispatch
- The halt reason for this case names what the operator must decide, not only that no
- Negative path: a build that genuinely fails to fix a real wiring gap still halts and
