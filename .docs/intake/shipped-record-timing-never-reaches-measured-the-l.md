# Intake origin: shipped-record-timing-never-reaches-measured-the-l

Source-Ref: jstoup111/ai-conductor#1260
Owner: jstoup111

## Desired outcome

- A feature that ships through a normal daemon run produces a shipped record whose
- When timing genuinely cannot be measured, the committed record states which
- The aggregate KPI reports how many ships contributed to its averages, so a figure
- Negative path: a run with genuinely incomplete evidence (interrupted, parked
- Observable check: ship one feature end to end, then read its
