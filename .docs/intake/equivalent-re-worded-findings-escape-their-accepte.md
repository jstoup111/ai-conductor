# Intake origin: equivalent-re-worded-findings-escape-their-accepte

Source-Ref: jstoup111/ai-conductor#1611
Owner: jstoup111

## Desired outcome

- A finding whose substance (same rubric, same offending paths/subject) was accepted stays accepted across laps regardless of how the grader re-words its concernKind or anchor labels.
- Genuinely different findings at the same paths still surface (no blanket path-level immunity).
- When an acceptance binds via tolerant matching, the lap output says which accepted disposition matched.
- Routing-time disposition re-resolution covers every remediate outcome, halts included — a scope→plan routing composed pre-acceptance must not halt the feature when the store already says effective PASS.
