# Intake origin: tautology-rubric-grades-diff-required-fixture-relo

Source-Ref: jstoup111/ai-conductor#1545
Owner: jstoup111

## Desired outcome

- A changed test whose only change is relocating a fixture, where the relocation is
- A fixture relocation that the diff does NOT require is still graded normally and
- An operator can see the evidence the grader used to distinguish the two, from the
- A feature blocked only by relocation findings reaches a PASS without an operator
