# Intake origin: one-build-review-pass-clears-the-convergence-cap-s

Source-Ref: jstoup111/ai-conductor#1694
Owner: jstoup111

## Desired outcome

- A feature that accumulates many `build_review` kickbacks reaches a bounded terminal state without
- A feature whose `build_review` verdict is invalidated by a rebase and legitimately re-enters is
- When a convergence bound is cleared or not cleared, that decision is legible afterwards from
