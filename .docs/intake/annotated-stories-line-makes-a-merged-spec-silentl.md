# Intake origin: annotated-stories-line-makes-a-merged-spec-silentl

Source-Ref: jstoup111/ai-conductor#1330
Owner: jstoup111

## Desired outcome

- A plan whose `**Stories:**` line names a valid path followed by trailing prose resolves to
- No spec is ever excluded from the backlog without an operator-visible reason: any discovery
- A genuinely unresolvable stories reference (missing file, path traversal, non-Markdown
- An operator can determine, from `daemon status` alone, why a merged and accepted spec is not
