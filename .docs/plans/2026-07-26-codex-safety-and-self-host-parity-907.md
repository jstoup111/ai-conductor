# Implementation Plan: Codex Safety and Self-Host Parity (#907)

**Date:** 2026-07-26
**Design:** `.docs/specs/2026-07-25-codex-safety-and-self-host-parity-907.md`
**Stories:** `.docs/stories/codex-safety-and-self-host-parity-907.md`
**Architecture:** `.docs/decisions/architecture-review-2026-07-25-codex-safety-self-host-parity-907.md`
**Conflict check:** Clean as of 2026-07-26; four blocking conflicts resolved

## Summary

Build one engine-owned safety boundary for Claude and Codex, replace workspace-global task
stamping with concurrent task-local telemetry, seal approved DECIDE artifacts through BUILD/SHIP,
and provision symmetric isolated self-host environments. The plan contains 30 narrow TDD tasks
(approximately 1–2.5 hours at the required 2–5 minute task size); this is above the 20-task
advisory threshold because 15 stories each require explicit happy and negative-path coverage.

## Technical Approach

- Add `task-attribution.ts` as the provider-neutral validator/active-set telemetry surface. Feed
  validated task ids into provider dispatch context, keep concurrent task rows independent, and
  make telemetry failures diagnostic-only. Update generated git hooks so valid explicit trailers
  win and `.pipeline/current-task` is no longer a mutation or replacement authority.
- Add `protected-artifact-seal.ts` and `safety-boundary.ts`. The seal is anchored once to approved,
  committed DECIDE artifacts at first BUILD entry, consumes the existing `phase-marker.ts`
  bounded allowlist, and is checked before and after every BUILD/SHIP attempt. The boundary
  classifies required versus diagnostic capabilities. The conductor supplies one per-candidate
  attempt wrapper to `executeProviderCandidates`; the approved callback is
  `withCandidateSafety(candidate, invoke)`. It enters only after actual-provider resolution,
  wraps raw invocation, and verifies the result before candidate fallback or acceptance.
- Refactor `sandbox-build-env.ts` behind a provider-aware isolated-home contract. Claude receives
  only selected auth, engine controls, and worktree skills; Codex consumes #905's selected auth,
  adds a restricted opaque cached-credential handoff where necessary, and gets a child-only HOME
  whose `.agents/skills` points only to #904-compatible worktree content. No live settings, trust,
  hooks, sessions, catalog, or global relink are inherited.
- Add a live-boundary fingerprint and bounded teardown contract around self-host dispatch. Native
  hooks/fences remain early rejection layers; deterministic pre/post inspection is the accepting
  authority. Diagnostics pass through one redactor before logs, HALTs, attempt metadata, or
  persisted safety artifacts.
- Consume merged #905's auth/readiness and invocation policy after rebasing; do not duplicate it.
  Add a narrow optional provider-owned self-host-auth capability so the engine never rediscovers
  Codex credential layout. Preserve #904 ordinary skill installation/discovery and adjust only the
  self-host child.

## Prerequisites

- Rebase or otherwise reconcile merged #905 (`origin/main` inspected at `96adec8d`) before
  implementing Codex auth/home tasks so its selected-auth contract is the source of truth.
- Re-inspect #904 at its landing revision before Task 25; only fixture/file-layout adjustments are
  expected because the ordinary user-catalog outcome is fixed by the approved conflict resolution.
- Use the acceptance specs generated by `/writing-system-tests` as the outer RED suite; the unit
  paths below provide the narrow RED/GREEN loop for each task.

## Tasks

### Task 1: Validate exact task-local identities
**Story:** FR-1 HP-1; FR-4 HP-1 and NP-1
**Type:** happy-path

**Steps:**
1. Write failing table tests for exact, empty, malformed, unknown, stale, and mismatched plan ids.
2. Run the focused test and verify RED.
3. Implement a typed task-attribution validator that preserves only exact seeded ids and emits sanitized diagnostics without guessing.
4. Run the focused test and verify GREEN.
5. Commit with message: `feat: validate task-local attribution ids`.

**Files:**
- `src/conductor/src/engine/task-attribution.ts`
- `src/conductor/test/engine/task-attribution.test.ts`

**Wired-into:** `src/conductor/src/engine/step-runners.ts#DefaultStepRunner.runProviderAwareNormal`
**Dependencies:** none

### Task 2: Carry validated ids through both provider dispatches
**Story:** FR-1 HP-1 and NP-1; FR-15 HP-1
**Type:** happy-path

**Steps:**
1. Write failing provider-execution tests proving Claude and Codex receive the same exact validated task id and invalid ids stop before invocation.
2. Run the focused tests and verify RED.
3. Extend provider execution options/attempt context and the step runner to validate and carry task attribution before candidate invocation.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: carry provider-neutral task attribution`.

**Files:**
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/src/engine/step-runners.ts`
- `src/conductor/test/engine/provider-execution.test.ts`
- `src/conductor/test/engine/step-runners.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-execution.ts#executeProviderCandidates`
**Dependencies:** Task 1

### Task 3: Keep concurrent task telemetry independent
**Story:** FR-1 HP-2; FR-2 HP-2; FR-4 HP-2
**Type:** happy-path

**Steps:**
1. Write failing lifecycle tests that activate two independent tasks, repeat one activation, and assert both rows/contexts remain intact.
2. Run the focused tests and verify RED.
3. Implement idempotent task-local active-set transitions without clearing or serializing sibling task rows.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: track concurrent active task telemetry`.

**Files:**
- `src/conductor/src/engine/task-attribution.ts`
- `src/conductor/src/engine/session-hook-assets.ts`
- `src/conductor/test/engine/task-attribution.test.ts`
- `src/conductor/test/integration/session-hooks-attribution.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-execution.ts#executeProviderCandidates`
**Dependencies:** Task 2

### Task 4: Preserve explicit commit trailers under concurrency
**Story:** FR-1 NP-2; FR-4 NP-2
**Type:** negative-path

**Steps:**
1. Write a failing real-git hook test with two active tasks and an explicit valid trailer different from any workspace-global stamp.
2. Run the focused tests and verify RED.
3. Change prepare/commit hooks to validate and preserve the explicit trailer, never replacing it from `.pipeline/current-task`.
4. Run hook syntax, unit, and integration tests and verify GREEN.
5. Commit with message: `fix: preserve explicit task trailers concurrently`.

**Files:**
- `src/conductor/src/engine/git-hook-assets.ts`
- `src/conductor/test/engine/git-hook-assets.test.ts`
- `src/conductor/test/integration/git-hooks-attribution.test.ts`

**Wired-into:** `src/conductor/src/engine/worktree-prepare.ts#prepareWorktree`
**Dependencies:** Task 1

### Task 5: Retire only the matching terminal task
**Story:** FR-2 HP-1 and HP-2
**Type:** happy-path

**Steps:**
1. Write failing parameterized tests for completion, failure, cancellation, and interruption with a sibling task still active.
2. Run the focused tests and verify RED.
3. Make terminal telemetry remove only the supplied validated id while preserving unrelated active rows/context.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: retire matching task telemetry only`.

**Files:**
- `src/conductor/src/engine/task-attribution.ts`
- `src/conductor/src/engine/session-hook-assets.ts`
- `src/conductor/test/engine/task-attribution.test.ts`
- `src/conductor/test/integration/session-hooks-attribution.test.ts`

**Wired-into:** same as Task 3
**Dependencies:** Task 3

### Task 6: Make telemetry cleanup and replacement fail safely
**Story:** FR-2 NP-1 and NP-2
**Type:** negative-path

**Steps:**
1. Write failing tests for telemetry-write failure and missing/unknown replacement ids with unrelated rows present.
2. Run the focused tests and verify RED.
3. Report the telemetry error without changing mutation/completion verdicts and reject invalid replacement without relabeling siblings.
4. Run the focused tests and verify GREEN.
5. Commit with message: `fix: isolate task telemetry failures`.

**Files:**
- `src/conductor/src/engine/task-attribution.ts`
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/test/engine/task-attribution.test.ts`
- `src/conductor/test/engine/provider-execution.test.ts`

**Wired-into:** same as Task 2
**Dependencies:** Task 5

### Task 7: Define the provider-neutral safety verdict
**Story:** FR-10 HP-1; FR-11 NP-1
**Type:** infrastructure

**Steps:**
1. Write failing unit tests for required protection states, diagnostic capabilities, applicability, and aggregate pass/fail verdicts.
2. Run the focused tests and verify RED.
3. Implement typed safety inputs/verdicts where any required missing or unknown state fails and diagnostic gaps cannot weaken required protections.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: define provider-neutral safety verdicts`.

**Files:**
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-execution.ts#executeProviderCandidates`
**Dependencies:** none

### Task 8: Decouple attribution from mutation and completion authority
**Story:** FR-3 HP-1, HP-2, NP-1, and NP-2
**Type:** negative-path

**Steps:**
1. Write failing matrix tests for present, absent, concurrent, stale, and mismatched attribution against allowed and forbidden mutations plus build-review completeness.
2. Run the focused tests and verify RED.
3. Feed attribution into the safety boundary as advisory telemetry only and keep artifact/workspace and judgment verdicts independent.
4. Run safety, artifact, and completeness tests and verify GREEN.
5. Commit with message: `fix: keep attribution advisory to safety gates`.

**Files:**
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/src/engine/artifacts.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`
- `src/conductor/test/engine/build-review-completeness.test.ts`

**Wired-into:** same as Task 7
**Dependencies:** Task 1, Task 7

### Task 9: Seal approved DECIDE artifacts at first BUILD entry
**Story:** FR-5 HP-1; FR-12 HP-2
**Type:** infrastructure

**Steps:**
1. Write failing tests that create a committed PRD/architecture/story/plan baseline and require a durable immutable manifest at first BUILD entry.
2. Run the focused tests and verify RED.
3. Implement canonical protected-path enumeration and content fingerprints anchored to the approved commit/baseline identity.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: seal approved decide artifacts`.

**Files:**
- `src/conductor/src/engine/protected-artifact-seal.ts`
- `src/conductor/test/engine/protected-artifact-seal.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.run`
**Dependencies:** none

### Task 10: Enforce exact active-step artifact exceptions
**Story:** FR-5 HP-2 and NP-2; FR-6 HP-2
**Type:** happy-path

**Steps:**
1. Write failing tests for exact active-step prefixes, sibling paths, and permission reuse by a later step.
2. Run the focused tests and verify RED.
3. Bind seal checks to `resolveDocsAllowlist` plus current phase/step identity without broadening sibling paths or future steps.
4. Run phase-marker and seal tests and verify GREEN.
5. Commit with message: `feat: scope protected artifact exceptions`.

**Files:**
- `src/conductor/src/engine/protected-artifact-seal.ts`
- `src/conductor/src/engine/phase-marker.ts`
- `src/conductor/test/engine/protected-artifact-seal.test.ts`
- `src/conductor/test/engine/phase-marker.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.run`
**Dependencies:** Task 9

### Task 11: Reject protected drift without resealing it
**Story:** FR-5 NP-1; FR-12 NP-2
**Type:** negative-path

**Steps:**
1. Write failing tests for changed, deleted, recreated, and newly added protected artifacts plus resume after drift.
2. Run the focused tests and verify RED.
3. Compare against the durable original seal before dispatch/resume and refuse to refresh the baseline from a dirty workspace.
4. Run the focused tests and verify GREEN.
5. Commit with message: `fix: reject protected drift across resume`.

**Files:**
- `src/conductor/src/engine/protected-artifact-seal.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/protected-artifact-seal.test.ts`
- `src/conductor/test/engine/conductor.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.run`
**Dependencies:** Task 10

### Task 12: Classify canonical mutation targets
**Story:** FR-6 HP-1 and HP-2
**Type:** happy-path

**Steps:**
1. Write failing table tests for known-unprotected, exact-allowed, protected, and canonical in-workspace targets.
2. Run the focused tests and verify RED.
3. Implement a canonical target classifier shared by artifact hooks and the terminal seal audit.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: classify canonical mutation targets`.

**Files:**
- `src/conductor/src/engine/protected-artifact-seal.ts`
- `src/conductor/src/tools/generate-docs-guard-hook.ts`
- `src/conductor/test/engine/protected-artifact-seal.test.ts`
- `src/conductor/test/acceptance/generate-docs-guard-hook.acceptance.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.run`
**Dependencies:** Task 10

### Task 13: Fail closed on indeterminate and escaping targets
**Story:** FR-6 NP-1 and NP-2
**Type:** negative-path

**Steps:**
1. Write failing tests for missing/malformed/dynamic targets, outside-workspace paths, traversal, symlink indirection, and replacement races.
2. Run the focused tests and verify RED.
3. Return an indeterminate/denied result for every unresolved target and re-check canonical containment at acceptance time.
4. Run hook and seal tests and verify GREEN.
5. Commit with message: `fix: fail closed on indeterminate mutation targets`.

**Files:**
- `src/conductor/src/engine/protected-artifact-seal.ts`
- `src/conductor/src/tools/generate-docs-guard-hook.ts`
- `src/conductor/test/engine/protected-artifact-seal.test.ts`
- `src/conductor/test/acceptance/generate-docs-guard-hook.acceptance.test.ts`

**Wired-into:** same as Task 12
**Dependencies:** Task 12

### Task 14: Wrap every BUILD and SHIP provider attempt
**Story:** FR-10 HP-1; FR-12 HP-1 and NP-1
**Type:** infrastructure

**Steps:**
1. Write failing wiring tests that inventory initial BUILD/SHIP raw provider calls and require safety preflight plus terminal verification around each.
2. Run the focused tests and verify RED.
3. Construct the safety boundary in the conductor and pass the approved `withCandidateSafety(candidate, invoke)` wrapper into provider execution so preflight runs after actual-provider resolution and terminal verification runs before fallback or acceptance.
4. Run provider and conductor wiring tests and verify GREEN.
5. Commit with message: `feat: wrap build and ship provider attempts`.

**Files:**
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/src/engine/step-runners.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`
- `src/conductor/test/engine/wiring-live-probe.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-execution.ts#executeProviderCandidates, src/conductor/src/engine/conductor.ts#Conductor.runSelfBuildDispatch`
**Dependencies:** Task 7, Task 8, Task 11, Task 13

### Task 15: Distinguish inapplicable from unavailable protections
**Story:** FR-10 HP-2, NP-1, and NP-2
**Type:** negative-path

**Steps:**
1. Write failing provider/self-host matrix tests for legitimate non-self-host inapplicability and missing, corrupt, stale, disabled, or falsely inapplicable required protections.
2. Run the focused tests and verify RED.
3. Validate applicability from actual provider/run context and stop pre-dispatch or mid-run whenever required state is not verifiably passing.
4. Run the focused tests and verify GREEN.
5. Commit with message: `fix: fail closed on unavailable protections`.

**Files:**
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`
- `src/conductor/test/engine/conductor.test.ts`

**Wired-into:** same as Task 14
**Dependencies:** Task 14

### Task 16: Classify diagnostic-only provider gaps
**Story:** FR-11 HP-1, HP-2, NP-1, and NP-2
**Type:** negative-path

**Steps:**
1. Write failing tests for explicit provider-labelled non-critical warnings, retry/resume stability, required gaps, and unknown/contradictory classifications.
2. Run the focused tests and verify RED.
3. Add a durable capability classifier that permits only declared diagnostic gaps after every required protection passes.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: report diagnostic provider capability gaps`.

**Files:**
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`
- `src/conductor/test/engine/provider-execution.test.ts`

**Wired-into:** same as Task 7
**Dependencies:** Task 15

### Task 17: Re-enter safety on retry, resume, groups, and replacement
**Story:** FR-12 HP-1 and NP-1
**Type:** happy-path

**Steps:**
1. Write a failing matrix test for initial, retry, resume, grouped, auxiliary BUILD/SHIP, and provider-replacement paths under Claude and Codex, including bypass injection.
2. Run the focused tests and verify RED.
3. Route every listed attempt through the same boundary instance/contract and reject results from injected bypasses.
4. Run the matrix and wiring probe and verify GREEN.
5. Commit with message: `feat: preserve safety across recovery paths`.

**Files:**
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/src/engine/step-runners.ts`
- `src/conductor/src/engine/group-core.ts`
- `src/conductor/test/engine/conductor.test.ts`
- `src/conductor/test/engine/wiring-live-probe.test.ts`

**Wired-into:** same as Task 14
**Dependencies:** Task 14

### Task 18: Reject stale reusable protection state
**Story:** FR-12 HP-2 and NP-2
**Type:** negative-path

**Steps:**
1. Write failing tests for reuse across wrong task, provider, phase, workspace, baseline, and terminal run plus one valid same-attempt retry.
2. Run the focused tests and verify RED.
3. Key reusable safety state to all six identities and invalidate it on terminal cleanup or mismatch.
4. Run the focused tests and verify GREEN.
5. Commit with message: `fix: reject stale protection state reuse`.

**Files:**
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`
- `src/conductor/test/engine/conductor.test.ts`

**Wired-into:** same as Task 14
**Dependencies:** Task 17

### Task 19: Emit actionable structured protection failures
**Story:** FR-13 HP-1, HP-2, NP-1, and NP-2
**Type:** negative-path

**Steps:**
1. Write failing contract tests for provider, protection, sanitized reason, stopped scope, recovery class/action, unknown metadata, and non-recoverable guidance.
2. Run the focused tests and verify RED.
3. Implement structured safety diagnostics that never guess missing metadata or recommend bypass/broad permissions/unbounded retry.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: explain protection failures actionably`.

**Files:**
- `src/conductor/src/engine/safety-diagnostics.ts`
- `src/conductor/src/engine/safety-boundary.ts`
- `src/conductor/test/engine/safety-diagnostics.test.ts`
- `src/conductor/test/engine/safety-boundary.test.ts`

**Wired-into:** `src/conductor/src/engine/safety-boundary.ts#evaluateSafetyAttempt`
**Dependencies:** Task 15, Task 16

### Task 20: Redact safety output before persistence
**Story:** FR-14 HP-1, HP-2, NP-1, and NP-2
**Type:** negative-path

**Steps:**
1. Write failing canary-secret tests across logs, attempt metadata, HALT files, audit artifacts, provider errors, repository status, and partial cleanup diagnostics.
2. Run the focused tests and verify RED.
3. Add one boundary redactor for credential/config values and raw bodies before any visible or persisted output.
4. Run canary scans and focused tests and verify GREEN.
5. Commit with message: `fix: redact safety diagnostics before persistence`.

**Files:**
- `src/conductor/src/engine/safety-diagnostics.ts`
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/src/engine/self-host/gate-halt.ts`
- `src/conductor/test/engine/safety-diagnostics.test.ts`
- `src/conductor/test/engine/provider-execution.test.ts`
- `src/conductor/test/engine/self-host/gate-halt.test.ts`

**Wired-into:** same as Task 19
**Dependencies:** Task 19

### Task 21: Introduce provider-aware isolated self-host homes
**Story:** FR-8 HP-1; FR-9 HP-2
**Type:** infrastructure

**Steps:**
1. Write failing contract tests for Claude/Codex provision, child environment, selected auth, engine controls, worktree assets, and idempotent teardown.
2. Run the focused tests and verify RED.
3. Generalize the sandbox contract into a provider-aware isolated-home provisioner that accepts the resolved candidate/provider, allocates its isolated destination, and invokes its optional provider-owned auth capability there while preserving path-bounded teardown.
4. Run the focused tests and verify GREEN.
5. Commit with message: `refactor: provision provider-aware self-host homes`.

**Files:**
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/src/engine/self-host/sandbox-build-env.ts`
- `src/conductor/src/engine/self-host/wiring.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`
- `src/conductor/test/engine/self-host/sandbox-build-env.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.runSelfBuildDispatch`
**Dependencies:** Task 20

### Task 22: Make Claude self-host configuration minimal
**Story:** FR-8 HP-1, HP-2, NP-2, and NP-3; FR-15 NP-1
**Type:** negative-path

**Steps:**
1. Replace legacy expectations with failing tests proving no operator settings, personal hooks, trust/state, extensions, history, sessions, or global relink enter/change the run.
2. Run the focused tests and verify RED.
3. Generate only engine-owned settings/hooks plus worktree skills in the throwaway `CLAUDE_CONFIG_DIR` and remove the self-host relink call.
4. Run sandbox, wiring, and sentinel tests and verify GREEN.
5. Commit with message: `feat: isolate claude self-host configuration`.

**Files:**
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/src/engine/self-host/sandbox-build-env.ts`
- `src/conductor/src/engine/self-host/wiring.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`
- `src/conductor/test/engine/self-host/sandbox-build-env.test.ts`
- `src/conductor/test/engine/self-host/wiring.test.ts`

**Wired-into:** same as Task 21
**Dependencies:** Task 21

### Task 23: Launch Codex from a minimal isolated home
**Story:** FR-8 HP-1 and NP-1; FR-15 HP-2 and NP-2
**Type:** happy-path

**Steps:**
1. Write failing tests for an optional provider-owned self-host-auth capability, API-key and cached-login selection, isolated `CODEX_HOME`, bounded args, resolved executable, and Claude-only operation with all Codex state absent.
2. Run the focused tests and verify RED.
3. Extend the provider contract/runtime with a narrow optional self-host-auth preparer, implement it from #905's private Codex selection, and pass its isolated env/args only to the resolved Codex candidate.
4. Run Codex provider, self-host, and Claude-only tests and verify GREEN.
5. Commit with message: `feat: launch codex in isolated self-host home`.

**Files:**
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/src/engine/provider-runtime.ts`
- `src/conductor/src/execution/llm-provider.ts`
- `src/conductor/src/execution/codex-provider.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`
- `src/conductor/test/execution/codex-provider.test.ts`
- `src/conductor/test/execution/claude-provider.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-execution.ts#executeProviderCandidates, src/conductor/src/engine/conductor.ts#Conductor.runSelfBuildDispatch`
**Dependencies:** Task 14, Task 21

### Task 24: Hand cached Codex credentials opaquely
**Story:** FR-8 NP-4; FR-14 HP-2 and NP-2
**Type:** negative-path

**Steps:**
1. Write failing tests for one selected native credential artifact, restrictive mode, no parse/log/hash/symlink, unchanged source, and cleanup after success/failure.
2. Run the focused tests and verify RED.
3. Implement the provider-owned self-host capability with an opaque byte copy into isolated `CODEX_HOME` and a private byte-for-byte source comparison; do not hash, parse, persist derived metadata, or expose credential layout to the engine.
4. Run permission, canary, source-sentinel, and cleanup tests and verify GREEN.
5. Commit with message: `feat: hand off cached codex credentials opaquely`.

**Files:**
- `src/conductor/src/execution/codex-self-host-auth.ts`
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/src/execution/codex-provider.ts`
- `src/conductor/test/execution/codex-self-host-auth.test.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-runtime.ts#ProviderRuntimeSet.get, src/conductor/src/engine/self-host/provider-home.ts#provisionProviderHome`
**Dependencies:** Task 23

### Task 25: Isolate Codex skill discovery from the #904 user catalog
**Story:** FR-8 NP-5
**Type:** negative-path

**Steps:**
1. Write failing integration tests with conflicting live/worktree skills, a live `$HOME/.agents/skills` sentinel, and an ordinary-session control.
2. Run the focused tests and verify RED.
3. Resolve the Codex executable before HOME override and create a child-only `.agents/skills` view of worktree skills/HARNESS without touching the live catalog.
4. Run self-host `$skill` and ordinary #904 regression tests and verify GREEN.
5. Commit with message: `feat: isolate codex self-host skill discovery`.

**Files:**
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`
- `src/conductor/test/acceptance/codex-safety-self-host-parity-907.acceptance.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.runSelfBuildDispatch`
**Dependencies:** Task 23

### Task 26: Fingerprint the live self-host boundary
**Story:** FR-7 HP-1 and HP-2; FR-8 HP-2
**Type:** infrastructure

**Steps:**
1. Write failing tests that fingerprint the live checkout and unrelated provider state before launch and verify byte identity after a successful feature-worktree mutation.
2. Run the focused tests and verify RED.
3. Implement bounded live-surface manifests that exclude only the selected auth provider-owned behavior and verify every terminal result.
4. Run the focused tests and verify GREEN.
5. Commit with message: `feat: verify live self-host boundary integrity`.

**Files:**
- `src/conductor/src/engine/self-host/live-boundary.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/self-host/live-boundary.test.ts`
- `src/conductor/test/engine/conductor.test.ts`

**Wired-into:** `src/conductor/src/engine/conductor.ts#Conductor.runSelfBuildDispatch`
**Dependencies:** Task 22, Task 23

### Task 27: Reject live-checkout and unrelated-state write attempts
**Story:** FR-7 NP-1 and NP-2; FR-8 NP-2
**Type:** negative-path

**Steps:**
1. Write failing real-script tests for absolute paths, traversal, symlink/indirection, shell mutation, unreadable fingerprints, and unexpected drift under both providers.
2. Run the focused tests and verify RED.
3. Harden the early fence to fail closed on ambiguous targets and make terminal fingerprint failure override any provider success.
4. Run fence, live-boundary, and acceptance tests and verify GREEN.
5. Commit with message: `fix: reject self-host boundary escapes`.

**Files:**
- `src/conductor/src/engine/self-host/write-fence.ts`
- `src/conductor/src/engine/self-host/live-boundary.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/self-host/write-fence.test.ts`
- `src/conductor/test/engine/self-host/live-boundary.test.ts`
- `src/conductor/test/acceptance/codex-safety-self-host-parity-907.acceptance.test.ts`

**Wired-into:** same as Task 26
**Dependencies:** Task 26

### Task 28: Tear down isolation on every terminal path
**Story:** FR-9 HP-1
**Type:** happy-path

**Steps:**
1. Write failing parameterized tests for success, failure, cancellation, timeout, interruption, retry exhaustion, and provider replacement.
2. Run the focused tests and verify RED.
3. Centralize provider-home, discovery-home, credential-copy, and child-env teardown in the per-candidate attempt wrapper's `finally` path so each failed fallback candidate is cleaned before the next is provisioned.
4. Run the terminal-path matrix and verify GREEN with no residue.
5. Commit with message: `feat: clean self-host isolation on every exit`.

**Files:**
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/src/engine/conductor.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`
- `src/conductor/test/engine/conductor.test.ts`

**Wired-into:** `src/conductor/src/engine/provider-execution.ts#executeProviderCandidates`
**Dependencies:** Task 24, Task 25, Task 27

### Task 29: Bound partial provisioning and repeated cleanup
**Story:** FR-9 HP-2, NP-1, and NP-2; FR-8 NP-1
**Type:** negative-path

**Steps:**
1. Write failing injected-fs tests for permission/disk failures at each provisioning stage, absent resources, repeated cleanup, and broadened deletion targets.
2. Run the focused tests and verify RED.
3. Track only successfully created temporary paths, remove partial state without launching model work, and make repeated cleanup path-bounded/idempotent.
4. Run failure-injection and canary tests and verify GREEN.
5. Commit with message: `fix: bound partial self-host cleanup`.

**Files:**
- `src/conductor/src/engine/self-host/provider-home.ts`
- `src/conductor/src/engine/self-host/codex-credential-handoff.ts`
- `src/conductor/test/engine/self-host/provider-home.test.ts`
- `src/conductor/test/engine/self-host/codex-credential-handoff.test.ts`

**Wired-into:** same as Task 28
**Dependencies:** Task 28

### Task 30: Lock Claude compatibility outside intentional isolation
**Story:** FR-15 HP-1, HP-2, NP-1, and NP-2
**Type:** refactor

**Steps:**
1. Run existing Claude non-self-host/auth/recovery/output tests, then add failing regression cases for concurrent telemetry and Codex-absent Claude-only execution.
2. Verify the new regression cases are RED without the complete boundary wiring.
3. Adjust only shared provider contracts needed for #907 while retaining Claude auth/recovery/output behavior and the intentional minimal self-host change.
4. Run Claude, provider-routing, self-host, and #907 acceptance suites and verify GREEN.
5. Commit with message: `test: lock claude safety parity compatibility`.

**Files:**
- `src/conductor/src/execution/claude-provider.ts`
- `src/conductor/src/engine/provider-execution.ts`
- `src/conductor/test/execution/claude-provider.test.ts`
- `src/conductor/test/engine/provider-execution.test.ts`
- `src/conductor/test/acceptance/codex-safety-self-host-parity-907.acceptance.test.ts`

**Wired-into:** none (no new production surface)
**Dependencies:** Task 4, Task 6, Task 16, Task 22, Task 29

## Task Dependency Graph

```text
1 -> 2 -> 3 -> 5 -> 6
1 -> 4
1,7 -> 8
9 -> 10 -> 12 -> 13
10 -> 11
7,8,11,13 -> 14 -> 15 -> 16 -> 19 -> 20 -> 21
14 -> 17 -> 18
21 -> 22
14,21 -> 23 -> 24
23 -> 25
22,23 -> 26 -> 27 -> 28 -> 29
4,6,16,22,29 -> 30
```

## Integration Points

- After Task 6: concurrent provider-neutral task attribution and explicit commit trailers work end to end without workspace-global authority.
- After Task 14: every initial BUILD/SHIP provider attempt is enclosed by artifact/workspace safety preflight and terminal verification.
- After Task 18: retry, resume, grouped, auxiliary, and provider-replacement paths share the same safety contract without stale-state reuse.
- After Task 25: Codex self-host consumes #905 auth and #904-compatible worktree skills without reading the live user catalog.
- After Task 29: both provider homes, credential handoff, live-boundary checks, and cleanup are fail-closed across every terminal/provisioning path.
- After Task 30: the complete #907 acceptance suite proves provider parity and Claude non-self-host compatibility.

## Acceptance-Criteria Coverage

| Requirement | Happy paths | Negative paths | Tasks |
|---|---|---|---|
| FR-1 | HP-1, HP-2 | NP-1, NP-2 | 1, 2, 3, 4 |
| FR-2 | HP-1, HP-2 | NP-1, NP-2 | 3, 5, 6 |
| FR-3 | HP-1, HP-2 | NP-1, NP-2 | 8 |
| FR-4 | HP-1, HP-2 | NP-1, NP-2 | 1, 3, 4 |
| FR-5 | HP-1, HP-2 | NP-1, NP-2 | 9, 10, 11 |
| FR-6 | HP-1, HP-2 | NP-1, NP-2 | 10, 12, 13 |
| FR-7 | HP-1, HP-2 | NP-1, NP-2 | 26, 27 |
| FR-8 | HP-1, HP-2 | NP-1–NP-5 | 21, 22, 23, 24, 25, 26, 27, 29 |
| FR-9 | HP-1, HP-2 | NP-1, NP-2 | 21, 28, 29 |
| FR-10 | HP-1, HP-2 | NP-1, NP-2 | 7, 14, 15 |
| FR-11 | HP-1, HP-2 | NP-1, NP-2 | 7, 16 |
| FR-12 | HP-1, HP-2 | NP-1, NP-2 | 9, 11, 14, 17, 18 |
| FR-13 | HP-1, HP-2 | NP-1, NP-2 | 19 |
| FR-14 | HP-1, HP-2 | NP-1, NP-2 | 20, 24 |
| FR-15 | HP-1, HP-2 | NP-1, NP-2 | 2, 22, 23, 30 |

## Verification

- [x] All 15 stories contain concrete happy and negative paths.
- [x] Every acceptance criterion maps to at least one task.
- [x] Negative paths are explicit task scopes, not deferred cleanup.
- [x] Each task uses a focused RED/GREEN/commit loop sized for 2–5 minutes.
- [x] Every task declares repo-relative authoritative files, wiring, and dependencies.
- [x] Dependencies are explicit and acyclic.
- [x] The plan contains no ordinary documentation work.
- [ ] `/writing-system-tests` has generated and run the outer failing acceptance specs.
- [ ] Implementation and affected-test union pass after all tasks.
