# =============================================================================
# WORKTREE-SPECIFIC OVERRIDES
# =============================================================================
# This file is gitignored. Each worktree has its own copy.
# Values here override .env for this worktree only.

WORKTREE_DB_SUFFIX=_{{WORKTREE_SLUG}}
REDIS_NAMESPACE={{WORKTREE_SLUG}}
PORT={{WORKTREE_PORT}}
